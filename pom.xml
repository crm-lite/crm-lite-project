<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.crm</groupId>
    <artifactId>crm-lite-project</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>crm-lite-project</name>
    <description>Parent POM for the CRM Lite microservices monorepo</description>

    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <!-- Not part of Spring Boot's BOM (same situation as spring-cloud.version) -->
        <springdoc-openapi.version>2.8.6</springdoc-openapi.version>
        <!-- Centralized Testcontainers strategy (see docs/runbooks/testcontainers.md).
             Spring Boot 4's parent BOM does not manage Testcontainers versions, and
             Testcontainers 1.21.3 bundles docker-java 3.4.2, whose npipe probe uses a
             fallback API version the Docker 29 engine (Rancher Desktop) rejects
             ("client version 1.32 is too old"). 3.5.x negotiates correctly.
             Older modules (api-gateway, customer-service, lookup-service) still carry
             identical module-local copies of these pins predating this centralization;
             removing those duplicates is a separate cleanup — new modules must rely on
             THESE managed versions and add no module-local overrides. -->
        <testcontainers.version>1.21.3</testcontainers.version>
        <docker-java.version>3.5.1</docker-java.version>
    </properties>

    <modules>
        <module>backend/config-server</module>
        <module>backend/discovery-server</module>
        <module>backend/crm-security-starter</module>
        <module>backend/api-gateway</module>
        <module>backend/customer-service</module>
        <module>backend/lookup-service</module>
        <module>backend/mernis-stub</module>
        <module>backend/account-service</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc-openapi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-api</artifactId>
                <version>${docker-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-transport-zerodep</artifactId>
                <version>${docker-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-transport</artifactId>
                <version>${docker-java.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <!-- Testcontainers' shaded docker-java defaults to Docker API v1.32,
                         which Docker 29+ engines reject ("client version 1.32 is too old;
                         minimum is 1.41"). Pin a supported version for every test JVM so
                         `mvn test` works out of the box (part of the centralized
                         Testcontainers strategy above; harmless for modules without
                         Testcontainers tests). -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <argLine>-Dapi.version=1.44</argLine>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>