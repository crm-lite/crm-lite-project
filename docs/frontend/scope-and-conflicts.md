# Frontend Kapsam ve Çelişki Kaydı

Backend'in `docs/requirements/document-delta.md` disiplininin frontend
karşılığı (FE-ADR-013 §f). **Hiçbir kapsam dışı bırakma ve hiçbir mock/backend
çelişkisi sessizce çözülmez** — her biri burada bir satırla yaşar.

Son güncelleme: **2026-07-29**

## Durum etiketleri

| Etiket | Anlamı |
|---|---|
| 🔴 **karar bekliyor** | Karar verilmedi; geliştirmeyi bloke ediyor veya edebilir |
| 🟡 **analiste soruldu** | Analiste/ekibe iletildi, cevap bekleniyor |
| 🟢 **karara bağlandı** | Karar alındı ve ilgili ADR'ye/dokümana işlendi |
| ⚪ **kayıt** | Bilgi amaçlı; aksiyon gerektirmiyor |

---

## 1. Kapsam dışı bırakılanlar (FE-ADR-013 §b)

| # | Öğe | Domain | Backend durumu | UI davranışı | Durum |
|---|---|---|---|---|---|
| 1.1 | **Offer Selection** ekranı (tamamı) | FR-PROD + FR-SALE | Servis yok | Geliştirilmez | 🟢 karara bağlandı |
| 1.2 | **Product Configuration** ekranı (tamamı) | FR-PROD | Servis yok | Geliştirilmez | 🟢 karara bağlandı |
| 1.3 | **Submit Order** ekranı (tamamı) | FR-SALE | Servis yok | Geliştirilmez | 🟢 karara bağlandı |
| 1.4 | Customer Info → **hesap bölümü** (Account name/number/type/status, Create/Edit/Delete account, Billing address) | FR-ACCT | ~~Servis yok~~ → **account-service dev'de** (commit `635b9a2`; ADR-013/014, `docs/api/account-service.md`) | ~~Sekme hiç render edilmez~~ → **KAPSAMA ALINDI** (2026-07-24, FE-ADR-013 §Amendment A1; detay §1A) | 🟢 karara bağlandı (**güncellendi 2026-07-24**) |
| 1.5 | Customer Info → **ürün bölümü** (Product offer, Campaign, View/Deactivate product) | FR-PROD | Servis yok | ~~Render edilmez~~ → **"Coming soon"**: bölüm görünür ama **etkileşime kapalı** — API çağrısı yok, sahte veri yok, tüm metinler i18n anahtarından, `data-testid` yine var (FE-ADR-013 §Amendment A3) | 🟢 karara bağlandı (**güncellendi 2026-07-24**) |
| 1.6 | Customer Info → hesap satırı aksiyonları (Start new sale, Transfer, Service address change) | FR-SALE | Servis yok | Render edilmez — hesap bölümü kapsama girdi ama API bilinçli olarak `Action` alanı döndürmüyor (ADR-013 §3); FR-SALE aksiyonları yazılmaz | 🟢 karara bağlandı (teyit 2026-07-24) |
| 1.7 | Customer Search → `accountNumber` filtresi | FR-ACCT | Parametre **tanınıyor**, **hâlâ `501 MSG-FEATURE-NOT-IMPLEMENTED`** — account-service geldi ama customer-service'in bu filtreyi gerçek çağrıya çevirmesi **ayrı follow-up PR** (`docs/api/account-service.md` §Deliberate limitations) | Alan render edilir, **disabled** (değişmedi) | 🟢 karara bağlandı (teyit 2026-07-24) |
| 1.8 | Customer Search → `orderNumber` filtresi | FR-SALE | Parametre **tanınıyor**, `501` | Alan render edilir, **disabled** | 🟢 karara bağlandı |
| 1.9 | `PasswordInput` bileşeni | — | — | `shared/ui/`'da yazılmaz (FE-ADR-005 §P5) | 🟢 karara bağlandı |
| 1.10 | Mock **Login v2** ekranı | FR-AUTH | Keycloak teması tamamlandı | Angular'a port edilmez (FE-ADR-005 §P4) | 🟢 karara bağlandı |

**Kapsam içi:** Customer Search, Create Customer, Customer Info'nun demografik +
adres + iletişim bölümleri, **hesap bölümü (FR-ACCT — 2026-07-24'te girdi, §1A)**,
referans veri (cities/districts/lookups), auth kabuğu.

---

## 1A. Kapsam güncellemesi — account-service geldi (2026-07-24)

Backend'e `account-service` eklendi (dev commit `635b9a2`; ADR-013/014,
`docs/api/account-service.md`; FR-ACCT-01..04 + KR-11). Bağlayıcı frontend
kararı **FE-ADR-013 §Amendment (2026-07-24)** — buradaki satırlar özet + izleme.

| # | Konu | Karar | Durum |
|---|---|---|---|
| 1A.1 | **Hesap bölümü kapsamı** | Customer Info hesap bölümü geliştirilecek kapsama girdi → yeni kardeş feature **`features/account/`** (FE-ADR-003 §Consequences'ın öngördüğü evrim). Kontrat: `/api/accounts/**` — tam 5 endpoint, `docs/api/account-service.md`. ✅ **TAMAMLANDI (2026-07-25):** `features/account/section/` AccountSection + AccountFormDialog — liste/oluştur/güncelle/pasifleştir canlı (uygulama kayıtları §4.24) | 🟢 **tamamlandı** |
| 1A.2 | **K-8 / 223 Customer Account** | 223 hiçbir API yanıtında görünmez (liste 224-only; 223 numarası tekil endpoint'lerde 404 `MSG-ACCT-NOT-FOUND`). **UI yalnız 224 Billing Account bilir** — 223 için hiçbir ekran, etiket, satır veya özel durum yazılmaz | 🟢 karara bağlandı |
| 1A.3 | **KR-11 Account Number** | `VARCHAR(10)`, Luhn, immutable, asla yeniden kullanılmaz (ADR-014). UI'da **salt okunur metin** — hiçbir form kontrolüne bağlanmaz. `PUT` gövdesi tam olarak `{accountName, addressId}`; fazladan/immutable alan → **400 `MSG-ACCT-IMMUTABLE-FIELD`** (yani "objeyi olduğu gibi geri gönder" update formu kontrat ihlalidir) | 🟢 karara bağlandı |
| 1A.4 | **Silme = pasifleştirme** | `DELETE` → 204; satır listede **Passive olarak kalmaya devam eder** (AC-ACCT-04-02). Sıralama backend'de sabit: Active önce, sonra Passive; grup içinde `accountNumber` ASC (AC-ACCT-01-04) — **UI yeniden sıralamaz**. Aktif-ürün kilidi backend'de: 409 `MSG-ACCT-HAS-PRODUCTS`; pasif hesapta update/re-delete → 409 `MSG-ACCT-NOT-ACTIVE`. Silme onayı (`MSG-ACCT-DELETE-CONFIRM`) ve başarı mesajı (`MSG-ACCT-DELETED`) frontend-only | 🟢 karara bağlandı |
| 1A.5 | **Liste çağrısı şekli** | `GET /api/accounts?customerId={customerNumber}` — `customerId` **public müşteri numarasıdır** (1001), internal id değil. Yanıt düz JSON **dizi** (sayfalama zarfı YOK — `Page` zarfı tartışması §4.4 burada geçerli değil) | ⚪ kayıt |
| 1A.7 | **Account Description alanı kontratta YOK** | 🔴 **BLOKE (2026-07-29, D1 doğrulaması).** Talep: hesap oluşturma/düzenleme formuna AC-ACCT-02-02'nin "Account Description (opsiyonel)" alanı eklensin. **Üç kaynak da alanı tanımıyor:** (1) `docs/api/account-service.md` §Representation yanıt şeklini tam **6 anahtara** pinliyor (`accountNumber, accountName, accountTypeCode, accountTypeName, billingAddressId, accountStatus`) ve Postman testi `Object.keys().sort()` ile bunu iddia ediyor; (2) create gövdesi **tam olarak** `{customerId, accountName, addressId}`, update gövdesi **tam olarak** `{accountName, addressId}` — **fazladan herhangi bir özellik 400 `MSG-ACCT-IMMUTABLE-FIELD`** ile *reddediliyor* (yok sayılmıyor), yani `description` göndermek isteği bozar; (3) `docs/requirements/functional-requirements.md` FR-ACCT-02/03 ve `document-delta.md` #4 ("AC-ACCT-02-02: Account Name + Billing Address required") description'dan hiç söz etmiyor; mock §6.4 hesap tablosunda da böyle bir kolon/alan yok. **Frontend tek başına çözemez** — salt görsel bir alan eklemek ya sessizce kaybolan bir veri (FE-ADR-013 §a ihlali) ya da 400 üreten bir istek olurdu. **Gerekli:** analist AC-ACCT-02-02'nin description maddesini teyit ederse backend'de kolon + kontrat genişletmesi (yeni bir account-service PR'ı) gerekir; o zaman FE alanı 15 dakikada eklenir. Uygulanmadı, uydurulmadı | 🔴 **karar bekliyor — backend/analist** |
| 1A.6 | **i18n katalog eksiği** | Frontend kataloğunda yalnız analist üçlüsü var (`MSG-ACCT-HAS-PRODUCTS`, `MSG-ACCT-DELETE-CONFIRM`, `MSG-ACCT-DELETED`). **5 proje-ekli anahtar eksik:** `MSG-ACCT-NOT-FOUND`, `MSG-ACCT-NOT-ACTIVE`, `MSG-ACCT-IMMUTABLE-FIELD`, `MSG-ACCT-DUP-NUMBER`, `MSG-ACCT-NUMBER-CAPACITY-EXCEEDED`. EN/TR metinler hazır: `docs/architecture/account-service-decisions.md`. Hesap ekranından **önce** kataloğa + `i18n.spec.ts` bütünlük listesine eklenmeli. ✅ **UYGULANDI:** 8 `MSG-ACCT-*` anahtarının tamamı `messages.ts`'te (F3'te eklendi, 2026-07-25'te hesap bölümüyle doğrulandı) | 🟢 **tamamlandı** |

---

## 2. Mock ↔ Backend kontrat uyuşmazlıkları

Tam gerekçeler: `docs/frontend/mock-ui-analysis.md` §5A.
**Genel kural: çakışmada backend kontratı kazanır (FE-ADR-013 §e).**

| # | Konu | Mock | Backend | Karar | Durum |
|---|---|---|---|---|---|
| 2.1 | **"Second name" filtresi** | Ayrı filtre alanı **yok** (önceki analiz yanlış listelemişti); tabloda **kolon** olarak var | Tek `firstName` parametresi **First+Middle birleşiminde** kelime-başı arar (KR-01) | Tek kutu, etiketi **"Name"**; `middleName` yalnız tablo kolonu | 🟢 karara bağlandı |
| 2.2 | **"Role" filtresi** | Filtre **yok**; tabloda kolon var | Liste endpoint'inde `role` parametresi yok; response'ta `role` alanı var | **Role ile filtreleme yapılmaz**; yalnız kolon | 🟢 karara bağlandı |
| 2.3 | **Per page değerleri** | 15 / 25 / 50, varsayılan **15** | API varsayılanı **20**; KR-04 (analist) **15/30/50** — üç farklı liste | **Varsayılan 20.** Seçenek listesi 20'yi içerecek şekilde kurulur | 🟢 karara bağlandı |
| 2.4 | Per page **seçenek listesi** | 15/25/50 | Varsayılan 20; API **pozitif her `size` değerini kabul ediyor** (ADR-005) | **20 / 50 / 100**, varsayılan **20**. 50 ve 100 için backend tarafında ayrı bir hazırlık gerekmiyor — `size` parametresi serbest | 🟢 karara bağlandı |
| 2.5 | **Kriter birleşimi** | Dolu kriterler OR'lanır | Backend de OR'luyor (isim grubu içi AND) | Uyumlu, değişiklik yok | 🟢 karara bağlandı |
| 2.6 | **Doğum tarihi formatı** | `DD.MM.YYYY` (taşımada) | ISO `YYYY-MM-DD` | Taşıma **ISO**; gösterim `dd.MM.yyyy` | 🟢 karara bağlandı |
| 2.7 | **Cinsiyet değerleri** | `male` / `female` | `"Male"` / `"Female"` | Backend | 🟢 karara bağlandı |
| 2.8 | **Adres alanları** | `city`, `district` (string ad), `houseNo`, `description` | `cityId`, `districtId` (Long), `houseFlatNumber`, `addressDescription`, `primary` | Backend; Select'ler **id** taşır | 🟢 karara bağlandı |
| 2.9 | **Şehir/ilçe kaynağı** | Hardcoded (3 şehir, 10 ilçe) | `GET /api/cities`, `/api/cities/{id}/districts` | Backend | 🟢 karara bağlandı |
| 2.10 | **İletişim alanları** | `mobile`, `home` | `mobilePhone`, `homePhone` | Backend | 🟢 karara bağlandı |
| 2.11 | **Müşteri kimliği** | `customerId` (7 hane) | Response alanı **`customerNumber`**; query param adı `customerId` kalıyor | Backend; ikisi karıştırılmaz | 🟢 karara bağlandı |
| 2.12 | **Birincil adres** | `primaryIndex` (dizi indeksi) | `PATCH .../addresses/{addressId}/primary` | Backend (kalıcı id) | 🟢 karara bağlandı |
| 2.13 | **NATID tekilliği** | Client-side, 2 sabit ID | `409 MSG-CUST-DUP-NATID` (soft-deleted dahil, ADR-003) **+ MERNIS** (`400`/`503`) — mock'ta MERNIS hiç yok | Backend; üç hata da UI'da karşılanır | 🟢 karara bağlandı |
| 2.14 | **Türkçe diakritik** | Mock katlıyor (`yilmaz`→`Yılmaz` eşleşir) | Backend katlamıyor | Backend: **diakritik-duyarlı**; fold Angular'a taşınmaz | 🟢 karara bağlandı |
| 2.15 | **Pasif müşteriler** | Listeden çıkarılıyor | `status_id = ACTV AND deleted_date IS NULL` | Uyumlu | ⚪ kayıt |
| 2.16 | **z-index** | Modal `1000`, toast `1200` → toast modal'ın altında kalır | EDS token'ları: modal 1400, toast 1500 | Yalnız `--eds-z-*` token'ları; sayısal literal yasak | 🟢 karara bağlandı |
| 2.17 | **`data-testid`** | 7 ekranda da **0 adet** | — | Her etkileşimli elemanda zorunlu (FE-ADR-009) | 🟢 karara bağlandı |
| 2.18 | **Sidenav etiket boyutu** | `11px` (token dışı) | — | **Caption'a yuvarlanır: `--eds-type-caption-size` (12px)**. Token dışı değer yazılmaz (FE-ADR-011 §f) | 🟢 karara bağlandı |
| 2.19 | **Boş sonuç metni** | "No customer found" + "Use *Create new customer* above…" | Analist kataloğu: `MSG-CUST-NOT-FOUND` = "No customer found! Would you like to create the customer?" | Hangisi geçerli? | 🟡 **analiste soruldu** |
| 2.20 | **Header rol metni** | `"Mobility · Resp. Sales Rep."` | `/api/session/me` yalnız `username`, `subject`, `roles` döndürüyor | **Kaldırılır.** Header'da avatar + `/api/session/me`'den gelen gerçek `username` gösterilir. Mock'un metni prototip örnek verisidir; olmayan veri gösterilmez (FE-ADR-013 §a). Backend'e yeni alan **eklenmez** | 🟢 karara bağlandı |
| 2.21 | **Sidenav öğelerinin işlevi** | B2C/B2B/menu/thumbs-up yalnız görsel durum değiştiriyor | — | **Yalnız B2C aktif.** Diğer üç ikon görünür kalır ama **tıklama hiçbir şey tetiklemez** (navigasyon yok, aktif durum değişmez). `aria-disabled` + `cursor: default` | 🟢 karara bağlandı |
| 2.22 | **Header dil seçici** | Statik `EN` metni | — | AC-LANG-01-01 gereği **gerçek TR/EN değiştirici** olacak | 🟢 karara bağlandı |
| 2.23 | **Login ekranı** | Login v2 tasarımı | Keycloak `crm-lite` teması mock'u birebir uyguluyor | Angular'da yazılmaz | 🟢 karara bağlandı |
| 2.24 | **501 filtrelerinin ipucu metni** | — | — | Disabled alanların tooltip/helper metni ne diyecek? Öneri: *"Available when the account/order module is released."* | 🟡 **analiste soruldu** |

---

## 2A. Customer Search (golden path) — ekran bazlı uyuşmazlıklar

Ekran yazılmadan önce netleşmesi gerekenler. Analiz:
`docs/frontend/customer-search-analysis.md`.

> **Not:** 2A.1 ve 2A.2 için **proje kararı zaten var** (§2.1, §2.2 — 🟢) ve
> geliştirme bunlarla ilerler; buradaki satırlar o kararları geri almaz, analist
> **onayını** ister. Onay gelmezse karar aynen geçerli kalır — bu maddeler
> geliştirmeyi **bloke etmiyor**.

| # | Konu | Mock ne diyor | Backend ne veriyor | Analiste sorulan | Durum |
|---|---|---|---|---|---|
| 2A.1 | **"Second name" arama davranışı** | Tabloda **kolon** var; filtre alanı **yok** | Ayrı `middleName` parametresi **yok**; `firstName` parametresi First+Middle **birleşiminde** kelime-başı arıyor (KR-01) | Kullanıcı "Nur" yazıp **yalnız ikinci adı** "Nur" olanları bulmak isterse bu karşılanmalı mı? Bugünkü davranış: "Zeynep Nur" da "Nur Ali" de gelir, ayrım yapılamaz. Tek kutunun etiketi **"Name"** olarak mı kalsın, yoksa "Name / Second name" gibi mi ifade edilsin? | 🟡 **analiste soruldu** (proje kararı §2.1 geçerli) |
| 2A.2 | **"Role" ile filtreleme** | Tabloda **kolon** var; filtre alanı **yok** | Liste endpoint'inde `role` parametresi **yok**; yanıtta `role` alanı **var** | Role'e göre filtreleme bir gereksinim mi? Değilse kolon salt bilgi olarak kalır (bugünkü plan). Gereksinimse **backend'e yeni parametre** gerekir — bu bir backend iş kalemidir, frontend tek başına çözemez | 🟡 **analiste soruldu** (proje kararı §2.2 geçerli) |
| 2A.3 | **Boş sonuç: iki farklı durum** | Tek boş-durum metni var | Aynı 200 + boş liste, iki farklı anlam | Ekran **ilk açıldığında** (hiç arama yapılmadan) sistemde müşteri yoksa gösterilecek metin, **arama sonucu boş** metninden farklı olmalı. **UYGULANDI (2026-07-25):** arama-boş → `MSG-CUST-NOT-FOUND` (analist); browse-boş → proje-yazımı `UI-SEARCH-BROWSE-EMPTY-TITLE/-BODY` (işaretli, FE-ADR-008 §7 kalıbı). Analist metin verirse anahtar içeriği değişir, kod değişmez | 🟡 **analiste soruldu — proje metniyle uygulandı** |
| 2A.4 | **Sayfalama sayfa butonları** | `‹ 1 2 3 ›` — sabit 3 sayfa | `totalPages` 7+ olabilir (137 kayıt / 20) | **UYGULANDI (2026-07-25)** `mock-ui-analysis §6.2`'deki kayıtlı kesme kuralıyla: toplam ≤ 7 → hepsi; aksi halde `1 … (p-1) p (p+1) … son` (`…` tıklanamaz). Analist farklı bir kural verirse `pageItems()` computed'ı tek yerde değişir | 🟡 **analiste soruldu — mock kuralıyla uygulandı** |
| 2A.5 | **Sonuç satırına tıklama** | Satır tıklanınca Customer Info'ya gidiyor | Detay endpoint'i **var** (`GET /api/customers/{customerNumber}`) | ~~Customer ID hücresi link görünümünde ama etkileşimsiz~~ → **AKTİFLEŞTİ (2026-07-25):** Customer Info rotası (`/customers/:customerNumber`) yazıldı; hücre gerçek `routerLink`. `data-testid` (`…-open-link`) değişmedi | 🟢 karara bağlandı (**aktif**) |

---

## 3. Analist dokümanı ↔ backend çelişkileri (frontend'i etkileyenler)

Backend tarafındaki tam kayıt: `docs/requirements/document-delta.md` §Open
conflicts. Aşağıdakiler frontend'i doğrudan bağlar.

| # | Çelişki | Durum |
|---|---|---|
| 3.1 | **KR-04 sayfa boyutu:** analist UI varsayılanı 15 (15/30/50) vs API varsayılanı 20 | 🟢 **karara bağlandı** — 20 (bkz. 2.3) |
| 3.2 | **FR-AUTH-01 uygulama içi login formu varsayıyor** | 🟢 **karara bağlandı** — ADR-006 süperseder; Keycloak sayfası + tema. `document-delta.md` #6'da kayıtlı |
| 3.3 | **AC-CUST-01-02** ("LBL-SEARCH boşken pasif") ↔ **AC-CUST-01-00** (login sonrası tüm müşteriler) gerilimi | 🟢 **karara bağlandı** — ekran açılışında **otomatik kritersiz çağrı** (browse modu) yapılır; `Search` butonu yalnız **yeniden** aramayı tetikler ve tüm alanlar boşken pasif kalır. İki AC de karşılanır |
| 3.4 | **ADR-011 analist onayı** (workbook USERS tablosunun Keycloak lehine terk edilmesi) | 🟡 **analiste soruldu** — ADR-011 status hâlâ "Proposed" |
| 3.5 | Katalogda karşılığı olmayan 10 backend anahtarı (`MSG-VALIDATION-ERROR`, `MSG-AUTH-*`, `MSG-ADDR-LAST-DELETE`, …) | 🟡 **analiste soruldu** — metinleri biz mi yazacağız, analist mi verecek? Şimdilik proje-yazımı, işaretli |

---

## 4. Teknik açık kararlar

| # | Konu | Detay | Durum |
|---|---|---|---|
| 4.1 | **OAuth redirect origin** | 🟢 **ÇÖZÜLDÜ (2026-07-23)** — üç eşgüdümlü değişiklik: (1) `api-gateway.yml`'e `server.forward-headers-strategy: framework`; (2) realm'de `crm-bff` client'ına `:4200` redirect URI + webOrigin + post-logout URI eklendi; (3) `keycloak-init` her `up`'ta bunları yeniden uyguluyor (`--import-realm` yalnız ilk açılışta çalıştığı için mevcut `keycloak_db`'ler de kapsanıyor); (4) nginx `$host`'u olduğu gibi iletiyor. Çalışan stack'te doğrulandı: PKCE `S256` ve `directAccessGrants=false` korunuyor. Detay: FE-ADR-004 §Addendum | 🟢 **karara bağlandı** |
| 4.2 | **Frontend host portu** | **4200** (boşta; kullanılanlar 8888/8761/8080/5432/8180). 4.1 çözüldüğü için sabitlendi | 🟢 karara bağlandı |
| 4.3 | **Node sürümü** | Karar: **22.23.1** + npm **10.9.8**. Makinede **ZIP dağıtımı** ile kuruldu (`C:\tools\node-v22.23.1-win-x64`), MSI ile değil: MSI 22.23.1'i kurulu 23.11.1 üzerine bir *downgrade* sayıp `WIX_DOWNGRADE_DETECTED` ile reddediyor, kaldırma ise yönetici hakkı istiyor. PATH `~/.bashrc` ile önceliklendirildi. ⚠️ **Bu yalnız Git Bash içinde geçerli** — sistem PATH'indeki `C:\Program Files\nodejs` (v23.11.1) duruyor, cmd/PowerShell hâlâ onu görüyor. Container tarafı etkilenmez (`node:22.23.1-alpine`). ⚠️ Node 22 maintenance'ta, EOL **2027-04-30** | 🟢 **karara bağlandı ve doğrulandı** |
| 4.4 | **`Page` zarfının JSON şekli** | 🟢 **ŞEKİL DOĞRULANDI (2026-07-25, Swagger customer-controller — ekip üyesi sağladı):** düz `PageImpl` serileştirmesi — metadata üst seviyede (`totalElements/totalPages/size/content/number` + `first/last`; `pageable/sort/numberOfElements/empty` yok sayılıyor). Frontend tipi (`page.model.ts` `SpringPage`→`toPageResult`) birebir uyumlu çıktı; `first/last` sunucu bayrakları artık doğrudan kullanılıyor. ⚠ **Backend önerisi GEÇERLİ KALIR:** `serialization-mode` hâlâ pinli değil — doğrulanan şekil framework varsayılanı; Spring Boot yükseltmesi sessizce değiştirebilir. Pinleme/açık DTO önerisi backend'de bekliyor; frontend zarfı tek dosyada tuttuğu için değişiklik tek-dosya olur | 🟢 **şekil doğrulandı — pinleme önerisi backend'de** |
| 4.5 | **zone.js / zoneless** | 🟢 **UYGULANDI** — zoneless; `provideZonelessChangeDetection()` app.config'te açık, zone.js bağımlılık değil. `@angular/core@22.0.8` `peerDependenciesMeta: { zone.js: { optional: true } }` (FE-ADR-006 §7) | 🟢 karara bağlandı |
| 4.6 | **CI wiring** | **Ayrı iş.** Frontend job'ı backend Maven job'ından bağımsız çalışır; frontend lint hatası backend release'ini bloke etmez, tersi de geçerli (FE-ADR-001) | 🟢 karara bağlandı |
| 4.7 | **Import-boundary lint** | 🟢 **EKLENDI** — ESLint `no-restricted-imports` ile `core/`→`features/` ve `shared/`→`core/`|`features/` yasak; ihlal FE-ADR-003 mesajıyla reddediliyor (kanıtlandı 2026-07-23) | 🟢 karara bağlandı |
| 4.8 | **`data-testid` lint kuralı** | **Eklenmeyecek (bilinçli).** Angular şablonlarında "etkileşimli eleman" tespiti güvenilmez; false-positive üretip `eslint-disable` yorumlarının çoğalmasına ve kuralın değer kaybetmesine yol açar. Zorlama mekanizması: **PR review + E2E testlerin kendisi** (seçici yoksa test yazılamaz) | 🟢 karara bağlandı |
| 4.9 | **Katalog bütünlük testi** | 🟢 **YAZILDI** — `frontend/src/app/core/i18n/i18n.spec.ts`: her anahtarın EN+TR karşılığı dolu + dokümante 22 backend `messageKey` katalogda mevcut; eksikse test kırmızı. 6/6 test geçiyor | 🟢 karara bağlandı |
| 4.10 | **Tarih formatı istisnası** | FE-ADR-012 §e genel ilkesi "kültüre bağlı biçimlendirme yerelleştirilir"; tarih için **sabit `dd.MM.yyyy`** istisnası bilinçli alındı. Geri alınmak istenirse buradan izlenir | 🟢 **karara bağlandı** (istisna kayıtlı) |
| 4.12 | **`src/app/layout/` — dördüncü üst klasör** | Uygulama kabuğu (header + sidenav) FE-ADR-003 §1'in üç katmanının hiçbirine oturmuyor: `core/` değil (servis değil, bileşen), `shared/` **olamaz** (core'dan `AuthService`/`I18nService` enjekte ediyor; import-boundary lint'i bunu reddediyor), `features/` değil (kullanıcıya dönük bir "yetenek" değil, her ekranın çerçevesi). Bu yüzden tek bileşenli ince bir **`layout/`** klasörü eklendi. Değerlendirilen alternatifler: (a) kabuğu `shared/`e presentational yapıp core'u input/output ile geçirmek — iskelet aşamasında gereksiz dumb/smart ayrımı ve yine bir yerde container gerekiyor; (b) `features/shell/` demek — ADR'nin "yetenek" tanımını eğip bükmek. `layout/` yalnız aşağı doğru (`core` + `shared`) bakar; lint kuralları `core/` ve `shared/`i kısıtladığı için ihlal riski yok | 🟢 karara bağlandı |
| 4.14 | **`shared/ui` metin girdileri: çözülmüş string, anahtar DEĞİL** | 🟢 **KARAR (2026-07-25).** Tasarım notu §0 `ariaLabelKey` öngörüyordu ("bileşen anahtarı alır, kendi çözer") — ama `I18nService` `core/i18n`'de ve FE-ADR-003 lint'i `shared/`→`core/` importunu **mekanik olarak reddediyor**; iki bağlayıcı kural çelişti. Çözüm: shared/ui bileşenleri **çözülmüş metin** alır (`[ariaLabel]="'UI-…' | t"`, `[label]="'LBL-…' | t"`); çeviri daima çağıranın işi. Bu, Button metninin zaten `<ng-content>` + `{{ 'KEY' \| t }}` ile gelmesiyle de tutarlı — TÜM metinler bileşene çözülmüş girer, bileşen asla çeviri yapmaz. FE-ADR-012 §b korunuyor (literal metin yine yasak; `check:conventions` literal attribute'u yakalıyor, binding'i geçiriyor). Yan sonuç: Select'in `labelKey`/`label` ikiliği tek `label` alanına indi. Tasarım notu §0 güncellendi | 🟢 karara bağlandı |
| 4.15 | **DatePicker: custom panel (native değil)** | 🟢 **KARAR (2026-07-25, ekip talimatı).** Tasarım notu §7'deki açık soru kapandı: mock'un takvim paneli birebir uygulanır (maskeli `DD.MM.YYYY` yazımı + takvim popover, hafta Pazartesi başlar, panel 280px / gün hücresi 28px / `radius-lg` / `elevation-2`). Ay/gün adları katalogda tutulmaz — `Intl.DateTimeFormat` ile üretilir; `shared` `core`'a bakamadığından aktif dil bileşene **`locale` girdisi** olarak geçirilir (çağıran `i18n.lang()` bağlar). Form değeri daima ISO `yyyy-MM-dd` (FE-ADR-012 §e); dönüşüm bileşenin içinde | 🟢 karara bağlandı |
| 4.16 | **Tema eklemeleri (FE-ADR-011 §f süreci)** | 🟢 **KARAR (2026-07-25).** `shared/ui` için `styles.css` `@theme`'e, tamamı `mock-ui-analysis.md`'de belgeli gerçek değerlerle eklendi: (1) `--shadow-focus-ring` = `0 0 0 3px #FCDABC` (§2.6, orange-200 tabanlı form focus halkası); (2) `--shadow-focus-ring-offset` = `0 0 0 2px #FFF, 0 0 0 4px #F58220` (§2.6, buton focus'u); (3) `--color-secondary-action: #313152` (§2.7 `action-secondary-text`, ink-800 — temada yoktu); (4) `--animate-eds-spin` 900ms linear (§2.14 `--eds-duration-spin`, spinner `data-eds-motion-exempt` ile reduced-motion'dan muaf) ve `--animate-panel-in` 200ms ease-enter (§3.7 `eds-panel-in`). Keyfi değer değil, eksik token'ın temaya taşınması | 🟢 karara bağlandı |
| 4.20 | **Customer Search uygulama kayıtları (2026-07-25)** | ⚪ Ekran yazılırken alınan küçük kararlar: (1) **Durum 11/12 toast yerine bant** — `shared/patterns/toast` henüz yok; hata, sonuç kartı içinde `role="alert"` bandı olarak gösteriliyor (+ Tekrar dene), tablo son iyi haliyle kalıyor. Toast deseni gelince değerlendirilir; (2) **Skeleton'da shimmer animasyonu yok** — EDS `--eds-duration-shimmer` (1800ms) token'ı temaya taşınmadı; statik `bg-sunken` bloklar + `aria-busy`. Gerekirse §4.16 kalıbıyla eklenir; (3) **"Create new customer" disabled** — Create ekranı yokken sahte davranış üretilmedi (FE-ADR-013 §a); wizard gelince `routerLink('new')` olur; (4) **Button/IconButton'a `action` output'u eklendi** — host `(click)`'i hem `check:conventions`'ın testid şartına takılıyor hem `loading` guard'ını atlıyordu; `action` yalnız izinli aktivasyonda ateşlenir; (5) **Select'e `dropUp` girdisi** — mock §6.2 per-page paneli kart içinde kalsın diye yukarı açılıyor; (6) sayfalama ok ikonları mock'ta 18px → IconButton standardı 20px (FE-ADR-011 §f yuvarlaması, §2.18 emsali) | ⚪ kayıt |
| 4.21 | **shared/patterns uygulama kayıtları (2026-07-25)** | ⚪ Customer Search'teki inline desenler `shared/patterns/`a çıkarıldı (FE-ADR-011 §d / mock §7.2): **Table, EmptyState, Skeleton, Pagination** (Search bunları tüketiyor) + **Toast, Modal** (Create/Info/Account için önceden kuruldu, henüz tüketici yok). Sözleşme shared/ui ile birebir: saf sunum, metin çözülmüş girer (§4.14), signal input/output, `testId` zorunlu, yalnız EDS token. Alınan küçük kararlar: (1) **Tema eklemeleri (§4.16 süreci):** `--container-modal-sm/md/lg` = 420/560/680px (mock §7.2 belgeli değerleri) ve `--animate-toast-in` 200ms ease-enter/-8px (mock §4.3) `styles.css`'e taşındı; (2) **Toast konumu** mock'un 76px'i token dışı → `top-16` (64, header) + `mt-3` (12) bileşimiyle kuruldu, keyfi piksel yazılmadı; (3) **Toast kapatma butonu** mock'ta 24×24 → 32×32 IconButton standardına yuvarlandı (§4.20/6 emsali; kopya değil tüketim); (4) **Modal arka plan inert'liği** `aria-modal="true"` + odak tuzağı ile — gerçek `inert` attribute'u bileşen DOM'u dışına uzanmayı gerektirirdi, shared saflığıyla çelişir; Escape daima kapatır, backdrop tıklaması varsayılan olarak kapatMAZ (`closeOnBackdrop` girdisi); kapanma kararı daima çağırandadır (`closed` output, bileşen kendini kapatmaz); (5) **Table** kolon başına `appTableCell` şablonu zorunlu — eksik şablon sessiz boş hücre yerine hata fırlatır (FE-ADR-008 §3 ruhu); satır testid'i çağıranın iş-anahtarlı fonksiyonundan; (6) **Pagination** sayfayı 0-tabanlı emit eder (store/API sözleşmesi), aktif sayfa butonu inert, kesme kuralı artık tek yerde (§2A.4'ün öngördüğü gibi); (7) Skeleton yine shimmer'sız (§4.20/2 kararı değişmedi). Customer Search refactor'unda davranış/testid değişmedi — 14 ekran testi dokunulmadan geçti | ⚪ kayıt |
| 4.22 | **Adres cascade "şehir değişince alt alanların tümü sıfırlanır" yorumu** | 🟢 **KARAR (2026-07-25).** Mock §6.3 cümlesi geniş okunursa street/houseNo/description da silinir — bu, yanlış şehir tıklamasında kullanıcının yazdığı her şeyi kaybettirir. **Dar yorum uygulandı:** şehre gerçekten BAĞIMLI tek alan olan `districtId` sıfırlanır (ve yeni şehrin ilçeleri yüklenir); serbest metin alanları korunur. Analist geniş yorumu isterse tek subscribe bloğu değişir (`address-form-dialog.ts`) | 🟢 karara bağlandı |
| 4.23 | **Customer Info uygulama kayıtları (2026-07-25)** | ⚪ İkinci iş ekranı yazılırken alınan kararlar: (1) **Hesap sekmesi F6'ya rezerve** — mock'un 4 sekmeli düzeni korunuyor: sekme + kart başlığı (`Customer accounts` + disabled `Create new account`) render ediliyor, gövde proje-yazımı `UI-DETAIL-ACCOUNTS-PLACEHOLDER` metni taşıyor; **hiçbir `/api/accounts` çağrısı yok, sahte veri yok** (FE-ADR-013 §a; spec bunu iddia ediyor). F6 hesap tablosunu bu alana yerleştirir; (2) **Ürün "Coming soon" konumu** — mock ürünleri genişleyen hesap satırlarının İÇİNDE gösterir; hesap satırları F6'ya kadar olmadığından A3 bölümü hesap sekmesinde bölüm-düzeyi inert blok olarak duruyor (`customer-detail-products-coming-soon`); F6'da mock'taki yerine (satır-içi) taşınması değerlendirilir; (3) **Adres silme onay metni** — analist kataloğu `MSG-ADDR-DELETE-CONFIRM` kullanıldı; mock'un parametreli *"delete {district}, {city}?"* metni taşınmadı (analist kataloğu kazanır, FE-ADR-012 §c); (4) **Metrik yuvarlamaları** (FE-ADR-011 §f, §2.18 emsali): adres kartı min-height 146→144px (`min-h-36`), kart radio'su 18→16px (`size-4`), tabs underline radius 2px→`rounded-full` (2px yükseklikte görsel eşdeğer); (5) **Cinsiyet görüntüleme** `UI-GENDER-*` ile yerelleşir, tel değeri `"Male"/"Female"` değişmez (§2.7); doğum tarihi ekranda sabit `dd.MM.yyyy` (§4.10); (6) **Silme başarı akışı** — mock'un `eds_flash`'ının karşılığı `CustomerFlashService` (root-provided, read-and-clear): Detail anahtar kuyruklar, Search toast gösterir; (7) **Yeni desenler** — `Tabs` ve `ConfirmDialog` (iki durumlu: soru/backend-hata) `shared/patterns`'e eklendi; `Modal`'a `headerless` girdisi eklendi (ConfirmDialog başlık çubuğu taşımıyor, erişilebilir ad `aria-label`'a taşınıyor); (8) **PUT gövdeleri kontrat-birebir:** demografik güncelleme DÜZ gövde (doc §J), adres PUT'unda `primary` alanı YOK (birincillik yalnız PATCH); no-op backend kontrolleri (adres in-use, aktif ürün) istemcide taklit edilmiyor — 409 gelirse ConfirmDialog'un hata durumunda gösteriliyor | ⚪ kayıt |
| 4.24 | **Hesap bölümü uygulama kayıtları (2026-07-25, F6)** | ⚪ FR-ACCT-01..04 Customer Info'da canlı; alınan kararlar: (1) **Tek yönlü kompozisyon** — account feature `AccountSection`'ı expose eder, customer onu import eder (FE-ADR-003 §Consequences'ın açıkça öngördüğü TEK kardeş-feature importu; ESLint bunu bilinçle mekanik kısıtlamıyor, "review concern"). Ters yön sıfır: adres seçenekleri account'un kendi `BillingAddressOption {addressId, label}` tipiyle **yukarıdan girer**, account customer'ın `AddressResponse`'unu hiç görmez; (2) **KR-11 yapısal:** hesap numarası edit dialog'unda salt METİN (form kontrolü değil); `UpdateAccountRequest` tipi zaten alanı temsil edemiyor — spec PUT gövdesinin anahtar kümesini `['accountName','addressId']` olarak iddia ediyor; (3) **Pasif satır aksiyonları AÇIK bırakıldı** — istemci `MSG-ACCT-NOT-ACTIVE` kontrolünü taklit etmez; PUT/DELETE 409'u dialog'da messageKey ile gösterilir (AC-ACCT-04-03 ile aynı ilke); (4) **Satır genişletme + ürün alt tablosu ertelendi** — mock ürünleri genişleyen satır içinde gösterir ama product-service yok; A3 "Coming soon" bloğu bölüm düzeyinde duruyor (§4.23/2 devamı), product-service gelince satır-içine taşınır; (5) **Mock'un 4/8/12 sayfalaması UYGULANMADI** — backend listesi zarfsız düz dizi döndürür ("no pagination", account-service.md); istemci-tarafı sayfalama küçük listeler için bilinçle eklenmedi, analist isterse patterns/Pagination ile eklenebilir; (6) **Yeni desen `StatusBadge`** (mock §6.4 rozeti; §7.2 envanterinden) + `Table`'a opsiyonel `headerClass` (Actions kolonu sağa hizalı); (7) Başarı metinleri: silme **analist** `MSG-ACCT-DELETED`; oluştur/güncelle proje-yazımı `UI-ACCOUNT-TOAST-CREATED/-UPDATED` (işaretli); durum görüntüleme `UI-ACCOUNT-STATUS-*` ile yerelleşir, tel değeri değişmez; (8) F5'in `UI-DETAIL-ACCOUNTS-PLACEHOLDER` anahtarı kaldırıldı (görevi bitti); (9) **K-8 spec'te:** create POST gövdesi anahtar-küme iddiasıyla pinli (`accountType` gönderimi mümkün değil), 223'e dair hiçbir UI yüzeyi yok | ⚪ kayıt |
| 4.25 | **Create Customer wizard uygulama kayıtları (2026-07-25, F7)** | ⚪ Son kapsam-içi ekran (FR-CUST-03, KR-10). Kararlar: (1) **Atomiklik yapısal:** adresler adım 2'de YEREL taslaktır (`AddressDraft`, yerel `draftId` — tele asla çıkmaz); tek `POST /api/customers` gövdesi spec'te derin-eşitlikle pinli; adres API'sine hiçbir kısmi istek gitmez (spec iddia ediyor); (2) **Paylaşılan adres dialog'una `draft` modu eklendi** — FE-ADR-003 §3'ün "tek ortak bileşen" kararı korunarak: draft modunda kaydetme `draftSaved(AddressRequest)` emit eder, store çağrılmaz (store `optional` inject'e alındı); `AddressFormValue` yapısal tipi hem `AddressResponse`'u hem taslağı karşılar. Dialog + AddressCards `testIdBase` parametresi aldı (FE-ADR-009: testid'ler barındıran ekranın önekiyle — `customer-create-address-*`); Info sekmesi varsayılanlarla birebir aynı kaldı; (3) **Tam-biri-primary istemci değişmezi:** ilk taslak primary olur (kontratın "first becomes primary" davranışıyla uyumlu), radio bunu tek taslağa taşır; kart bileşeninin primary-silme kilidi taslaklar için de geçerli; (4) **Mock'un edit modu (`eds_edit_customer`) TAŞINMADI** — düzenleme Customer Info'nun dialoglarında yaşıyor (F5); wizard yalnız create. Mock'un hardcoded mükerrer-ID listesi de taşınmadı (gerçek cevap: 409 `MSG-CUST-DUP-NATID` + MERNIS zinciri); (5) **Hata → adım atlama:** 400 alan hataları `scope`'a göre sahibi olan adıma götürür (`demographic`→1, `addresses`→2, `contactMedium`→3); 409 dup-natid ve `MSG-CUST-NATID-VERIFICATION-FAILED` hem banner'da hem `nationalityId` alanında gösterilir (adım 1'e döner); 503'te adımda kalınır, veri korunur, Create yeniden denenebilir; (6) **Create akışında hesap alanı yok** — mock §6.3 alan listeleri doğrulandı, hesapla ilgili alan zaten yok; çelişki kaydı gerekmedi; (7) **Yeni desen `Stepper`** (§7.2 envanteri; pasif gösterge — gezinme footer'da, mock'la uyumlu); denetçi-dostu `stateOf()` yardımıcısı ile şablonda karşılaştırma operatörü yok; (8) Search'ün "Create new customer" butonu **aktifleşti** (§4.20/3'ün öngördüğü an); başarı akışı `CustomerFlashService` → Customer Info toast'ı (`UI-CREATE-TOAST-SUCCESS`, proje-yazımı) | ⚪ kayıt |
| 4.26 | **Hata düzeltme turu uygulama kayıtları (2026-07-29)** | ⚪ Altı bulgu; alınan kararlar: (1) **Browse ≠ arama sonucu (A1)** — Customer Search'ün sonuç kartı başlığı artık moda göre değişiyor: kriter uygulanmamışken **`UI-SEARCH-BROWSE-HEADING`** ("All customers" / "Tüm müşteriler", **proje-yazımı**, §2A.3 kalıbı), filtre uygulanınca `UI-SEARCH-RESULTS-HEADING`. Ayrım zaten var olan `hasAppliedCriteria()`'dan geliyor — boş-durum metinlerinin (browse vs search) kullandığı ayrımın aynısı, yeni kavram icat edilmedi (ADR-005 browse/filter). Analist metin verirse yalnız katalog değeri değişir; (2) **Adres kart yüksekliği (A2)** — `min-h-36` yalnız TABANI garanti ediyordu, `items-start` de grid'in satırları eşitlemesini engelliyordu; özet satırı saran bir kart 144px'i aşınca "Add new address" geride kalıyordu. Çözüm `auto-rows-fr` + varsayılan stretch: tüm döşemeler en uzunu kadar. Piksel uydurulmadı (FE-ADR-011 §f), mock §6.3'ün "aynı min-height" niyeti korundu; (3) **Liste iç scroll'u (A3)** — `Table` deseni bilinçle scroll'suz ("Scrolling stays with the caller", §4.21/5) ve ekran zaten `overflow-auto` sarmalayıcıya sahipti; kopan halka **component host'uydu**: `app-customer-search` elemanı shell'in `<main>` flex'inin çocuğu ama kendisi flex zincirini taşımıyordu, dolayısıyla şablondaki `flex-1 / min-h-0` zincirinin sınırlı bir atası yoktu ve kart satır sayısıyla büyüyordu. `host: { class: 'flex min-h-0 flex-1 flex-col' }` eklendi. **Desende değil ekranda çözüldü** — ortak desene yükseklik yönetimi koymak her tüketiciye sınırlı-yükseklik varsayımı dayatırdı (dialog içindeki tablolar bunu istemez). ⚠ Aynı host boşluğu Detail/Create'te de var ama oradaki içerik sayfa boyuna bağlı büyümüyor; kapsam dışı bırakıldı, gerekirse aynı tek satırla çözülür; (4) **numeric kısıt `shared/ui`'de (B1/B2)** — `inputMode="numeric"` yalnız klavye İPUCU; fiziksel klavye, yapıştırma ve otomatik doldurma onu yok sayıyor. `TextInput`'a **`digitsOnly`** girdisi eklendi: rakam olmayanlar hem modelden hem DOM'dan giriş anında siliniyor (DOM'a geri yazma şart — `[value]` bağı değişmeyen sinyalde boyamıyor). Ekran seviyesinde regex/valibrasyon **yazılmadı**: kısıt tek yerde, bileşenin kendi girdi hijyeni olarak yaşıyor; VALIDITY hâlâ çağıranın ve nihayetinde backend'in (FE-ADR-007 §3). Tüketiciler: Search'ün ID number/Customer ID/GSM, Create'in Nationality ID (+ mevcut `maxLength=11`, VR-NATID). ⚠ Create'in telefon alanları (mobile/home/fax) mock §6.3'e göre de "sadece rakam" ama **bu turda kapsam dışı** — istenirse aynı girdiyle tek satırda açılır; (5) **Mükerrer NAT ID çift kontrolü (C1)** — §4.27; (6) **Adres in-use kilidi (C2)** — §5.9; hesap Description alanı (D1) — §1A.7 | ⚪ kayıt |
| 4.27 | **Mükerrer NAT ID: erken (UX) + son (otorite) çift kontrolü** | 🟢 **KARAR (2026-07-29, C1).** Talep: mükerrer NAT ID wizard'ın sonunda değil, demografik adımın `LBL-NEXT`'inde bildirilsin. **Kontrat doğrulaması:** ayrı bir "exists" endpoint'i **yok** ve icat edilmedi; `GET /api/customers?nationalityId={değer}` kullanılıyor — `nationalityId` bu endpoint'te **tam eşleşme** yapıyor (customer-service.md §List) ve `GET /api/customers` zaten tek kanonik liste/filtre endpoint'i (ADR-005). Dolu sayfa ⇒ aktif bir müşteri bu ID'yi tutuyor ⇒ alan altında `MSG-CUST-DUP-NATID`, adım ilerlemez. **Neden çift kontrol yapısal bir zorunluluk:** liste endpoint'i **yalnız aktif** müşterileri görür, oysa NAT ID tekilliği kalıcı ve globaldir — **soft-deleted bir sahip de yaratmayı bloke eder (ADR-003)**. Böyle bir kayıt erken probe'tan geçer ve yalnız POST'un 409'u yakalar; bu yüzden create sonundaki 409 yolu **kaldırılmadı**, backend nihai otorite kaldı (FE-ADR-007 §3). **Probe başarısız olursa (5xx/kesinti) kullanıcı ENGELLENMEZ** — bozulan bir kolaylık kontrolü, backend'in doğru tamamlayabileceği bir akışı durdurmamalı; ilerlenir, karar POST'a bırakılır. Uygulama: `CustomerApiService.activeCustomerExistsWithNationalityId()` (probe niyeti isimde ve JSDoc'ta açık), `Next` süresince buton `loading`. **GÜNCELLEME (aynı gün):** ilk uygulama liste endpoint'ini kullanıyordu ve soft-deleted sahibi göremiyordu; bu boşluk **backend'e ince bir availability ucu eklenerek kapatıldı** (§5.10, ADR-005 §Addendum). Probe artık `CustomerApiService.nationalityIdIsTaken()` ile o ucu çağırıyor ve **soft-deleted dahil** her sahibi adım 1'de yakalıyor. Liste-tabanlı probe ve ona eşlik eden oturum-içi `rejectedNatIds` geçici çözümü **kaldırıldı**. Çift kontrol gerekçesi **değişmedi**, yalnız sebebi daraldı: probe artık kör değil ama hâlâ **anlık** — probe ile POST arasında başkası ID'yi alabilir, o yüzden 409 yolu otorite olarak duruyor | 🟢 karara bağlandı |
| 4.17 | **shared/ui uygulama kayıtları (2026-07-25)** | ⚪ Üç küçük kayıt: (1) **ESLint FE-ADR-003 deseni false-positive'di** — `**/core/**` glob'u npm paketi `@angular/core/testing`'i de yakalıyordu; ilk `shared/` dosyasıyla ortaya çıktı, desene `!@angular/core`/`!@angular/core/**` negasyonları eklendi (kuralın amacı değişmedi). (2) **CVA bileşenlerinde `disabled` girdisi yok** — `FormControlDirective`'in kendi `disabled` input'u ile çakışıyor; tek kaynak `control.disable()` (tasarım notu §8). (3) Select'te `searchable`/`clearable`/`loading`, FormField'da `hint` tooltip **bilinçle ertelendi** — kapsam içi tüketici yok (FE-ADR-011 §c) | ⚪ kayıt |
| 4.18 | **Veri katmanı: dokümante edilmemiş response şekilleri (2026-07-25)** | 🟡 **Üç kaynaklı doğrulama zinciriyle 4 açıktan 3'ü KAPANDI** (① docs/api/*.md; ② Postman test assertion'ları; ③ **çalışan stack'in Swagger şemaları — ekip üyesi sağladı, 2026-07-25**): (1) **AddressResponse** ✅ — Swagger: `{addressId, cityId, cityName, districtId, districtName, street, houseFlatNumber, addressDescription, primary}` — yanıt **hem id hem çözülmüş adları** taşıyor (adres kartı için ek lookup çağrısı gerekmez); PATCH primary de aynı şekli döndürüyor (yalnız terfi eden adres — düşürülen eski primary dönmez, liste yenilenir). ⚠ Swagger *request* şemasındaki `primaryRequested` bir serializer artefaktı — dokümante tel adı `primary`, istemci yalnız onu gönderir (test iddia ediyor); (2) **ContactMediumResponse** ✅ — Swagger: tam olarak `{email, homePhone, mobilePhone, fax}`; (3) **City/District** ✅ — Swagger: **`{cityId, name}` / `{districtId, name}`** — ilk varsayım (`id`) YANLIŞTI, hiçbir ekran tüketmeden `core/lookup/lookup.model.ts`'te düzeltildi; (4) **`Page<T>` zarfı** — ✅ **KAPANDI (2026-07-25, Swagger customer-controller):** düz `PageImpl` — metadata üst seviyede, `first/last` dahil (detay §4.4). **Veri katmanında doğrulanmamış alan kalmadı** | 🟢 **4/4 kapandı** |
| 4.19 | **account request/response alan asimetrisi** | ⚪ **Kayıt (dokümanda net, tahmin değil):** account-service create/update **request**'i `addressId` gönderir; **response** aynı alanı `billingAddressId` olarak döndürür (account-service.md §Representation vs §Semantics). İki alan `account.model.ts`'te doğru şekilde ayrı tiplendi; karıştırılmamalı | ⚪ kayıt |
| 4.13 | **Parametreli i18n** | 🟢 **ONAYLANDI (2026-07-24)** — `translate(key, params?)` + **isimli** yer tutucular (`{from}`, `{total}`; pozisyonel yasak — TR/EN kelime sırası farkı); ICU/plural motoru bilinçli reddedildi (bağımlılık bütçesi, FE-ADR-012 §a); eksik parametre → yer tutucu olduğu gibi kalır + `console.warn` (FE-ADR-008 §3 felsefesi); katalog şekli değişmez; `i18n.spec.ts`'e EN↔TR yer tutucu kümesi eşitliği iddiası eklenir. Detay: **FE-ADR-012 §h**. Uygulama ekran fazının ilk adımı — henüz yazılmadı | 🟢 karara bağlandı — **henüz uygulanmadı** |
| 4.11 | **`validationErrors` alan yolu tekdüze değil → frontend'de çözüldü** | Backend handler'ları farklı biçim üretiyor (kaynaktan doğrulandı 2026-07-24): body `@Valid` → **tam yol** (`demographic.firstName`, `addresses[0].cityId`, `contactMedium.email`); `@RequestParam` (`handleConstraintViolation:72`) → **yalnız son segment** (`firstName`); tip uyuşmazlığı → param adı. Ayrıca `demographic`/`addresses`/`contactMedium` **çıplak** gelebiliyor ve **hiçbir form kontrolüne karşılık gelmiyor** — naif "yaprak ada göre eşleştir" yaklaşımı bunları sessizce yutar (kullanıcı 400 alır, ekranda hiçbir şey görmez). **Backend'e dokunulmadan** çözüldü: `core/http` her yolu bir kez ayrıştırıp `leaf`/`scope`/`index`/`structural` üretiyor; `matchFieldErrors()` eşleşenlerin YANINDA **`unmatched`** döndürüyor, böylece sessiz yutma API düzeyinde imkânsız; `fieldErrorsAt(scope,index)` wizard'da doğru adres satırını hedefliyor. Ham İngilizce değerli alanlardan **anlamı tek olanlar** (`cityId`/`districtId`/`gender`/`addresses`/`demographic`/`contactMedium`) frontend anahtarına eşlendi; serbest metin alanları (`street`, `houseFlatNumber`, `addressDescription`) bilinçle generic bırakıldı — zarf hangi kısıtın patladığını söylemediği için "zorunludur" demek yanlış olabilirdi (`@NotBlank` + `@Size` birlikte). Bu bilinçli tercihler `INTENTIONALLY_GENERIC` listesinde **açıkça** duruyor; listede olmayan bir alan generic'e düşerse `console.warn` üretiliyor (FE-ADR-008 §3 "gap geliştiriciye görünür, kullanıcıya görünmez") — böylece backend'in ekleyeceği yeni bir alan sessizce generic'e düşmüyor. 22/22 test | 🟢 **karara bağlandı** |

---

## 5. Backend'e bildirilecek bulgular

| # | Bulgu | Detay | Durum |
|---|---|---|---|
| 5.1 | **`İ` ile başlayan isimler aramada bulunamıyor** | `CustomerSpecifications.wordStart` karşılaştırmanın iki tarafını farklı motorda küçültüyor: kolon tarafı SQL `lower()`, terim tarafı Java `toLowerCase()`. Ölçüm (2026-07-23, canlı stack): Postgres `lower('İ')` → `i` (U+0069); Java `"İ".toLowerCase()` → `i` + **U+0307** (iki kod noktası). Kanıt: `SELECT lower('İbrahim') LIKE 'i̇brahim%'` → **`false`**. **Hiçbir Java `Locale` değeri ikisini birden düzeltmez** (`ROOT` `I`'yı düzeltir `İ`'yi bozar, `tr` tam tersi). Doğru düzeltme: Java'da küçültmeyi bırakıp arama terimini de SQL `lower()`'dan geçirmek | 🟡 **backend'e bildirildi** |
| 5.2 | **Test verisi hatayı yakalayamıyor** | `ind` tablosundaki seed isimleri ASCII'ye sadeleştirilmiş (`Yildiz`, `Sahin`) — Türkçe büyük harf içeren tek kayıt yok, bu yüzden 21 entegrasyon testi 5.1'i kaçırıyor | 🟡 **backend'e bildirildi** |
| 5.3 | **`docs/runbooks/auth-testing.md` satır 106 bayat** | Satır 3.9 hâlâ *"no custom project theme exists"* diyor; tema commit `285815d` ile geldi | 🟡 **bildirildi, düzeltme onayı bekliyor** |
| 5.4 | **PROJECTBRAIN §9.2 madde 2 bayat** | "Keycloak proje teması" kutusu işaretsiz ama tema tamamlanmış ve doğrulanmış (AC-AUTH-01'in tüm UI kriterleri karşılanıyor) | 🟡 **bildirildi, düzeltme onayı bekliyor** |
| 5.5 | **`functional-requirements.md` iç çelişkisi** | §Message keys `MSG-CUST-HAS-PRODUCTS` ve `MSG-ADDR-IN-USE`'u *"as used by the backend"* listesine koyuyor; §Deferred aynı iki kontrolü **no-op** ilan ediyor. Mock'ta "aktif ürünü var, silinemez" ekranı var ama backend bu hatayı bugün döndürmüyor | 🟡 **bildirildi** |
| 5.10 | ~~**Soft-deleted NAT ID sahibi hiçbir okuma endpoint'inden görünmüyor**~~ → **KAPANDI** | **KAYNAKTAN DOĞRULANDI, SONRA ÇÖZÜLDÜ (2026-07-29).** Senaryo: müşteri oluşturuldu → **soft delete** edildi → aynı NAT ID ile yeni kayıt deneniyor. Kullanıcı wizard'ın 3 adımını da geçiyor, hata ancak **Create'e basınca** 409 `MSG-CUST-DUP-NATID` olarak geliyor. **Neden frontend erken yakalayamıyor:** (1) tekillik kontrolü `IndividualRepository.existsByNationalityId()` — soft-deleted dahil, doğru (ADR-003) — ama **yalnız `CustomerBusinessRules.checkNationalityIdIsUniqueForCreate()` içinde**, POST sırasında çağrılıyor; **hiçbir endpoint bu bilgiyi dışarı açmıyor**; (2) tek kanonik okuma yolu `GET /api/customers`, `CustomerSpecifications.search()` her sorguya `statusId = ACTV AND deletedDate IS NULL` ekliyor (satır 78-79) → soft-deleted sahip **yapısal olarak görünmez**. Yani bugünkü kontratla ilk denemede adım 1'de bunu bildirmek **imkânsız**; istemci tarafı tahmin **yazılmadı** (uydurma kontrol, FE-ADR-013 §a). 🟢 **ÇÖZÜLDÜ (2026-07-29, ekip onayıyla backend'e dokunuldu).** `GET /api/customers/nationality-id-availability?nationalityId={id}` → `200 {"available": bool}` eklendi (**ADR-005 §Addendum**). Uç, tekillik kuralını **create yolunun kullandığı metodun ta kendisiyle** yanıtlıyor (`CustomerBusinessRules.isNationalityIdAvailableForCreate`) — yani soft-deleted sahipler dahil; probe ile otorite ayrışamaz. Yanıt **tek alan** taşıyor: kural silinmiş kişileri de kapsadığı için müşteri no/ad döndürmek bunu "silinmiş kişi madenciliği" ucuna çevirirdi. **Advisory kalıyor** — `true` bir rezervasyon değil; POST'un 409'u ve DB UNIQUE kısıtı otorite olarak duruyor, wizard 409'u işlemeye devam ediyor. Frontend'de yalnız `CustomerApiService.nationalityIdIsTaken()` bu ucu çağırıyor; **oturum-içi `rejectedNatIds` geçici çözümü KALDIRILDI** (gerçek uç geldi, gereksiz kaldı). Yan düzeltme: eksik zorunlu `@RequestParam` artık 500 değil **400** (`MissingServletRequestParameterException` handler'ı — tip-uyuşmazlığı handler'ının emsaliyle) | 🟢 **karara bağlandı ve uygulandı** |
| 5.9 | **AC-ADDR-04-04 / `MSG-ADDR-IN-USE`: backend no-op, frontend hazır** | **DOĞRULANDI (2026-07-29, C2).** Beklenen davranış: aktif bir fatura hesabına bağlı adres silinemesin, `MSG-ADDR-IN-USE` gösterilsin. **Backend bugün bu kontrolü YAPMIYOR** — iki bağımsız kaynak: `docs/api/customer-service.md` §Known limitations *"Address in-use check (`MSG-ADDR-IN-USE`) → **no-op** until account/service addresses exist"*, ve `docs/api/account-service.md` §Deliberate limitations *"…the address `MSG-ADDR-IN-USE` in-use check remain customer-service TODOs — converting them to real calls against this service is a **separate follow-up PR**"*. Aynı doküman §K notu da hatayı *"once account/service addresses exist"* koşuluna bağlıyor. Yani `DELETE .../addresses/{id}` bağlı bir adres için bugün **204** döndürüyor ve adres siliniyor. **Bu bir frontend bug'ı DEĞİL, backend bağımlılığıdır.** İstemci tarafında "bağlı mı" tahmini **yazılmadı** (FE-ADR-013 §a / §4.24-3 ilkesi: istemci backend kontrolünü taklit etmez — hesap listesini tarayıp adres id'si eşleştirmek, 223 K-8 hesabını ve başka servislerin adres kullanımını göremediği için yanlış "silemezsiniz" üretirdi). **Frontend hata-gösterme yolu HAZIR ve testli:** `confirmDeleteAddress()` gelen `messageKey`'i ayırt etmeden ConfirmDialog'un hata durumunda gösteriyor, adres listeden kaldırılmıyor, liste yenilenmiyor (`customer-detail.spec.ts` bunu 409 `MSG-ADDR-IN-USE` ile iddia ediyor) ve `MSG-ADDR-IN-USE` EN/TR olarak katalogda. **Backend follow-up PR'ı geldiği an davranış kendiliğinden aktifleşir — frontend'de sıfır satır değişiklik gerekir** | 🟡 **backend'e bildirildi — follow-up PR bekleniyor** |

---

| 5.6 | **Swagger UI / api-docs anonim değil** | PROJECTBRAIN §4.7 *"Docs sayfası login'siz görülebilir"* diyor, ama çalışan stack'te `GET /swagger-ui.html` ve `GET /v3/api-docs/customer-service` **401 `MSG-AUTH-UNAUTHORIZED`** dönüyor (2026-07-23 ölçümü). Ya çalışan imajlar ADR-012 commit'inden eski, ya da `crm.security.permit-paths` beklendiği gibi uygulanmıyor. Frontend'i bloke etmiyor ama `Page` zarfının şemasını Swagger'dan okumayı engelledi (§4.4) | 🟡 **backend'e bildirildi** |
| 5.8 | **PROJECTBRAIN §10 "Compose 8 servis" notu bayat** | `infra/docker-compose.yml`'e `frontend` servisi eklendi (FE-ADR-010, 2026-07-24; `git diff` = 35 ekleme / 0 silme, mevcut hiçbir servise dokunulmadı). Servis sayısı artık **9**. PROJECTBRAIN backend dokümanı olduğu için frontend tarafından düzenlenmedi | 🟡 **backend'e bildirildi** |
| 5.7 | **Logout CSRF, tam-sayfa gezinme POST'u ile taşınamıyor** | FE-ADR-005 §3 "tam sayfa gezinme + `X-XSRF-TOKEN` header" öngörüyordu; ancak tarayıcıda gezinen bir POST yalnız `<form>` ile yapılır ve form, header değil `_csrf` **parametresi** taşır. Gateway'in `SpaCsrfTokenRequestHandler`'ı (kaynaktan doğrulandı 2026-07-24) parametre yolunu **XOR-decode** ediyor (BREACH koruması), header yolunu ise ham token ile karşılaştırıyor — `XSRF-TOKEN` çerezi **ham** token tuttuğundan yalnız **header (XHR)** yolu geçerli. **Karar:** logout `HttpClient` POST (header → CSRF geçerli) + ardından `/`'a tam-sayfa gezinme (`core/auth/auth.service.ts`). Keycloak `end_session` 302'sini XHR sürükleyemiyor, fakat SSO oturumu `id_token_hint` ile **sunucu tarafında** yine de sonlanıyor. **Kusursuz tarayıcı-yönlü RP-logout** (Keycloak tarayıcı çerezlerinin de temizlenmesi) isteniyorsa backend bir olanak eklemeli: (a) logout `end_session` URL'ini JSON'da döndürsün, Angular `window.location.assign` etsin; ya da (b) gezinilebilir bir GET logout. **(c)** `crm.security.post-logout-redirect-uri` hiçbir yml'de tanımlı değil → yalnız Java varsayılanı `http://localhost:8080/` geçerli; `:4200` olmalı (ya da `X-Forwarded-*`'tan türetilmeli).<br>**🔴 CANLIDA DOĞRULANDI (2026-07-24)** — iki somut belirti: (1) XHR, gateway'in cross-origin 302'sini takip ederken **askıda kalabiliyor**; sign-out navigasyonu yanıta zincirlendiği için buton *hiçbir şey yapmıyordu*. (2) SSO ölmediğinden `/`'a inince guard → Keycloak → **sessiz re-login** → `/customers`; kullanıcı çıkış yapamamış sanıyor. **Frontend azaltmaları (uygulandı):** `logout()` artık `timeout(3000)` ile sınırlı — yanıt gelmese de çıkış tamamlanır (regresyon testi var); ve varış noktası guard'ın dışındaki `/signed-out` sayfası (form değil, yalnız tam-sayfa "Giriş yap" bağlantısı). Bunlar belirtiyi görünür/deterministik yapar ama **SSO'yu öldürmez** — onun için (a) şart. **✅ ÇÖZÜLDÜ (2026-07-27), seçenek (a):** `KeycloakLogoutSuccessHandler` artık `302` yerine `200` + `{"logoutUrl": "..."}` JSON dönüyor; `AuthService.logout()` başarı yanıtında `window.location.assign(logoutUrl)` ile gerçek bir üst-seviye gezinme yapıyor, böylece tarayıcı Keycloak'ı fiilen ziyaret edip SSO çerezini temizliyor. **(c)** de bu vesileyle çözüldü: `post_logout_redirect_uri` artık statik (ve hep bayat kalan) `crm.security.post-logout-redirect-uri` yapılandırmasından değil, `{baseUrl}`'ın OAuth2 `redirect-uri`'de yaptığı gibi `ServletUriComponentsBuilder` ile istekten (forward-header-düzeltmeli) türetiliyor — config anahtarı tamamen kaldırıldı. **↻ REVİZE (2026-07-29):** akış sadeleştirildi — çıkış artık sidenav butonuyla değil, paylaşılan `ConfirmDialog` üzerinden onaylanınca başlıyor (`MSG-AUTH-LOGOUT-CONFIRM`, Evet/Hayır); onay zaten alındığı için araya "çıkış yapıldı" ekranı koymanın anlamı kalmadı ve `/signed-out` sayfası **tamamen kaldırıldı**. `post_logout_redirect_uri` artık gateway'in kendi `/oauth2/authorization/keycloak` ucu → Keycloak SSO çerezini temizleyip oraya döndürüyor, o uç taze bir authorization request başlatıyor ve SSO öldüğü için kullanıcı **doğrudan giriş formunda** kalıyor (döngü yok: o isteği sessizce tamamlayabilecek oturum, az önce yok edilen oturumun ta kendisi). Gezinmeler `assign` değil `replace` — aksi hâlde müşteri verisi taşıyan ekran geçmişte kalıp bfcache'ten geri geliyordu. Bkz. FE-ADR-005 §3, `docs/api/authentication.md` §Logout. | 🟢 **karara bağlandı** |

---

## 6. Bakım kuralı

Yeni bir kapsam dışı bırakma, mock/backend farkı veya karar ortaya çıktığında
**önce buraya bir satır eklenir**, sonra kod yazılır. Durumu değişen satırın
etiketi güncellenir; satır silinmez — kapanmış kararların izi kalır.
Bir satır 🟢 olduğunda ilgili ADR'ye veya
`docs/frontend/mock-ui-analysis.md`'ye de işlenmiş olmalıdır.
