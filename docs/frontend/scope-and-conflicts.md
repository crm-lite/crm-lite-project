# Frontend Kapsam ve Çelişki Kaydı

Backend'in `docs/requirements/document-delta.md` disiplininin frontend
karşılığı (FE-ADR-013 §f). **Hiçbir kapsam dışı bırakma ve hiçbir mock/backend
çelişkisi sessizce çözülmez** — her biri burada bir satırla yaşar.

Son güncelleme: **2026-08-05** (**§1.7c — §1.7/§1.8 KAPANDI:** backend bağımlılığı
giderildi, Account Number ve Order Number filtreleri **aktifleştirildi**; §2.24 de
onunla kapandı). Önceki: 2026-08-04 (§4.31 Product Configuration karakteristik
doğrulaması + §5.13 alan-başına format tanımının eksikliği; §4.30 + §4.30 EK
BUG-1/BUG-2 hesap modalı; §1.7a/§1.7b 501 filtrelerinin iki kez — son olarak
çalışan ikili üzerinde — doğrulanması)

## Durum etiketleri

| Etiket | Anlamı |
|---|---|
| 🔴 **karar bekliyor** | Karar verilmedi; geliştirmeyi bloke ediyor veya edebilir |
| 🟡 **analiste soruldu** | Analiste/ekibe iletildi, cevap bekleniyor |
| 🟢 **karara bağlandı** | Karar alındı ve ilgili ADR'ye/dokümana işlendi |
| ⚪ **kayıt** | Bilgi amaçlı; aksiyon gerektirmiyor |

---

## 1. Kapsam dışı bırakılanlar (FE-ADR-013 §b)

| # | Öğe | Domain | Backend durumu | UI davranışı | Durum |
|---|---|---|---|---|---|
| 1.1 | **Offer Selection** ekranı (tamamı) | FR-PROD + FR-SALE | ~~Servis yok~~ → **product-service + order-service dev'de** (commit `c89a6a9`; ADR-015/016) | ~~Geliştirilmez~~ → **KAPSAMA ALINDI** (2026-08-03; satış sihirbazının 1. adımı, detay §2B.14) | 🟢 karara bağlandı (**güncellendi 2026-08-03**) |
| 1.2 | **Product Configuration** ekranı (tamamı) | FR-PROD | ~~Servis yok~~ → **`GET /api/offers/{id}/characteristics` var** | ~~Geliştirilmez~~ → **KAPSAMA ALINDI** (2026-08-03; sihirbazın 2. adımı, detay §2B.14) | 🟢 karara bağlandı (**güncellendi 2026-08-03**) |
| 1.3 | **Submit Order** ekranı (tamamı) | FR-SALE | ~~Servis yok~~ → **`POST /api/orders` var** (order-service, ADR-016) | ~~Geliştirilmez~~ → **KAPSAMA ALINDI** (2026-08-03; sihirbazın 3. adımı + yerinde başarı durumu, detay §2B.8/§2B.14) | 🟢 karara bağlandı (**güncellendi 2026-08-03**) |
| 1.4 | Customer Info → **hesap bölümü** (Account name/number/type/status, Create/Edit/Delete account, Billing address) | FR-ACCT | ~~Servis yok~~ → **account-service dev'de** (commit `635b9a2`; ADR-013/014, `docs/api/account-service.md`) | ~~Sekme hiç render edilmez~~ → **KAPSAMA ALINDI** (2026-07-24, FE-ADR-013 §Amendment A1; detay §1A) | 🟢 karara bağlandı (**güncellendi 2026-07-24**) |
| 1.5 | Customer Info → **ürün bölümü** (Product ID/name, Campaign name/ID, Status, View product) | FR-PROD | ~~Servis yok~~ → **product-service dev'de** (commit `ec2e6cd`; `docs/api/product-service.md`, FR-PROD-01..02 salt-okunur dilim) | ~~Render edilmez~~ → ~~"Coming soon"~~ → **KAPSAMA ALINDI** (2026-07-31, FE-ADR-013 §Amendment B1; detay §1B) — genişleyen hesap satırının içinde canlı ürün alt tablosu + görüntüleme modal'ı | 🟢 karara bağlandı (**güncellendi 2026-07-31**) |
| 1.5a | Customer Info → **"Deactivate product"** | FR-PROD (yazma) | **Endpoint YOK** — product-service Faz A tamamen salt okunur; AC-PROD-01-04 Action kolonunun yalnız görüntüleme ikonu taşıdığını açıkça söylüyor | `ban` ikonu mock düzeni için render edilir ama **disabled**; erişilebilir adı katalogdan gelir ve "coming soon" der; hiçbir şeye bağlı değil (FE-ADR-013 §Amendment A3 kuralı tek kontrole uygulandı) | 🟢 karara bağlandı (2026-07-31) |
| 1.6 | Customer Info → hesap satırı aksiyonları (Start new sale, Transfer, Service address change) | FR-SALE | **Yalnız NEWSALE var** — Transfer/Cancel hiçbir FR/AC tarafından kapsanmıyor ve backend yazmadı (§3.9) | **`LBL-START-NEW-SALE` KAPSAMA ALINDI** (2026-08-03): genişleyen hesap satırında, Aktif hesapta etkin / Pasif hesapta disabled (AC-SALE-01-01, §2B.16). **Transfer ve Service address change üretilmez** — backend'i olmayan işleve buton konmaz | 🟢 karara bağlandı (**güncellendi 2026-08-03**) |
| 1.7 | Customer Search → `accountNumber` filtresi | FR-ACCT | Parametre **tanınıyor**, **hâlâ `501 MSG-FEATURE-NOT-IMPLEMENTED`**. 🔴 **YENİDEN DOĞRULANDI 2026-08-03** — "aktifleştirilebilir" bildirimi üzerine üç kaynak kontrol edildi, **üçü de 501 diyor**, filtre **AÇILMADI** (detay §1.7a) | Alan render edilir, **disabled** (değişmedi) | 🟢 **KAPANDI 2026-08-05 — bkz. §1.7c** |
| 1.8 | Customer Search → `orderNumber` filtresi | FR-SALE | Parametre **tanınıyor**, `501`. order-service **geldi** (`c89a6a9`) ama customer-service'in kuralı **aynı satırda ikisini birlikte reddediyor** — yeniden doğrulandı 2026-08-03 (§1.7a) | Alan render edilir, **disabled** | 🟢 **KAPANDI 2026-08-05 — bkz. §1.7c** |
| 1.7a | **§1.7/§1.8 aktifleştirme talebi — DURDURULDU** | FR-ACCT + FR-SALE | 🔴 **BULGU (2026-08-03).** Talep: iki filtre artık backend'de desteklendiği için açılsın. **Varsayılmadı, doğrulandı — destek YOK:** (1) **Kaynak kod, `origin/dev` HEAD'i (`407833a`):** `CustomerBusinessRules.checkNoUnsupportedCrossServiceSearchCriterion` **koşulsuz** `throw new BusinessException(HttpStatus.NOT_IMPLEMENTED, MSG-FEATURE-NOT-IMPLEMENTED)` — `StringUtils.hasText(accountNumber) \|\| hasText(orderNumber)` ile; `CustomerServiceImpl.search` bunu **ilk satırda** çağırıyor, yani hiçbir sorgu çalışmıyor. Kaynaktaki `// TODO (intentional)` yorumu **hâlâ yerinde**. (2) **`docs/api/customer-service.md` (origin/dev):** *"`accountNumber`/`orderNumber` ⇒ **501 `MSG-FEATURE-NOT-IMPLEMENTED`** until the account/order domains exist"* + §Deliberate limitations aynı şeyi tekrarlıyor + doküman hâlâ 501 bekleyen `curl` örnekleri taşıyor. (3) **dev'in `c89a6a9` sonrası commit'leri:** `407833a` yalnız product-service'e dokunuyor; `c89a6a9..origin/dev` aralığında `backend/customer-service` veya `docs/api/customer-service.md` değişikliği **yok**. Uzakta bunu içerebilecek başka bir dal da yok. (4) **Çalışan stack Swagger'ı denenemedi** — gateway ayakta değil (`:8080` bağlantı yok); ilk üç kaynak zaten kesin ve kod kontratın kendisidir. **Sonuç: filtreler AÇILMADI**, `MSG-FEATURE-NOT-IMPLEMENTED` istemci davranışı (alanlar `disabled`, `CustomerSearchCriteria`'da temsil edilemez olmaları, `UI-SEARCH-DEFERRED-HINT`) **olduğu gibi bırakıldı** — açılsaydı ekran ilk tuşta 501 alırdı. **Aktifleştirme için gereken:** customer-service'te bu kuralın kaldırılması + gerçek çapraz-servis çözümlemesi (bir backend iş kalemi). O geldiğinde FE tarafı küçüktür: iki alanın `disable()`'ı kalkar, iki alan `CustomerSearchCriteria`'ya eklenir, KR-01/KR-02 gereği parametreler **olduğu gibi** gönderilir (istemcide arama mantığı yeniden yazılmaz) | Değişiklik yapılmadı | 🟢 **çözüldü — §1.7c** |
| 1.7b | **§1.7a tekrar doğrulandı — ÇALIŞAN SİSTEM ÜZERİNDE** | FR-ACCT + FR-SALE | 🔴 **BULGU (2026-08-04).** Talep yinelendi: *"Account ve Order servisler tamamen tamamlandı, backendte mevcut"* + iki servisin Swagger'ı iletildi. **İki iddia da doğru ama 501'i açıklamıyor:** `account-service` ve `order-service` gerçekten tam ve **ayakta** (`podman ps`: 12 konteyner sağlıklı; `/v3/api-docs/account-service` ve `/order-service` canlı). **Ne var ki 501'i üreten yer bu iki servis DEĞİL, `customer-service`'tir** — ve ona `c89a6a9`'dan beri **hiç dokunulmamış**. API dokümanının *"until the account/order domains exist"* ifadesi bir **bağımlılık gibi okunuyor**, ama koddaki kapı **koşulsuz**: alan dolu mu diye bakar, doldurulmuşsa domainlerin var olup olmadığına **bakmadan** atar. <br>🔬 **Çalışan ikili üzerinde kanıt (kaynak koda değil, deploy edilmiş artefakta bakıldı):** `podman cp customer-service:/app/app.jar` ile jar host'a alındı, ZIP merkezî dizini ayrıştırılarak `BOOT-INF/classes/com/crm/customer/customer/rules/CustomerBusinessRules.class` (5319 bayt) çıkarıldı; sınıf sabitleri arasında **`"Search by accountNumber/orderNumber is not implemented yet"`**, **`MSG-FEATURE-NOT-IMPLEMENTED`** ve **`checkNoUnsupportedCrossServiceSearchCriterion`** üçü de **mevcut**. Yani **şu an çalışan** customer-service hâlâ 501 atıyor. <br>⚠️ **Swagger'ın yanıltıcı olduğu nokta (talebin muhtemel kaynağı):** canlı `/v3/api-docs/customer-service`, `GET /api/customers` için `accountNumber` ve `orderNumber` parametrelerini **listeliyor** — çünkü springdoc **controller imzasını** yansıtır; parametreler zaten ilk günden beri imzada var (§1.7: *"parametre tanınıyor"*). Swagger, **servis katmanındaki iş kuralını göremez**; nitekim dokümante edilen tek yanıt **`200`**, 501 hiç görünmüyor. **Parametrenin Swagger'da görünmesi desteklendiği anlamına gelmiyor.** <br>**Uçtan uca canlı çağrı yapılamadı** (denendi): gateway üzerinden 401, servise doğrudan (konteyner içi 8082) yine 401 — resource server token istiyor; realm'de tek istemci `crm-bff` **public**, `directAccessGrantsEnabled=false`, `serviceAccountsEnabled=false` → betikle token alınamıyor, tarayıcı OAuth akışı gerekir. Deploy edilmiş sınıf kanıtı zaten kesin olduğu için bu yol sürdürülmedi. <br>**Sonuç: filtreler yine AÇILMADI.** Açılsalardı kullanıcı ilk aramada 501 + `MSG-FEATURE-NOT-IMPLEMENTED` görürdü. <br>✅ **Bu turda yapılabilen kısım yapıldı:** iletilen iki Swagger, **canlı** `/v3/api-docs`'tan çekilip mevcut istemci kontratlarımızla karşılaştırıldı — **sapma yok**: `OrderCreateRequest` (`accountNumber, serviceAddressId, campaignId, items`), `OrderItemRequest` (`offerId, characteristics`), `OrderCharacteristicRequest` (`characteristicId, value`), `OrderItemResponse` (`offerId, productId, amount`), `OrderResponse` (`orderNumber, orderStatus, accountNumber, customerNumber, totalAmount, items`) ve `AccountResponse`'un **7 alanı** (`customerNumber` dahil) `features/order/model/order.model.ts` ile `features/account/model`'deki tiplerle **birebir**. Kod değişikliği gerekmedi. <br>**Tüketilmeyen iki account ucu (bilinçli):** `POST /api/accounts/{accountNumber}/product-involvements` ve `GET /api/accounts/{accountNumber}/product-ids`. `CLAUDE.md`'ye göre `cust_acct_prod_invl`'i **yalnız account-service yazar** ve sipariş tarafı oraya **servisler arası** entegre olur — bu bir backend-backend akışıdır; hiçbir ekran bu iki ucu göstermiyor, istemci sarmalayıcısı yazılmadı (FE-ADR-011 §c bütçe ilkesi, §1B.10 emsali) | Değişiklik yapılmadı | 🟢 **çözüldü — §1.7c** |
| 1.7c | **§1.7/§1.8 AÇILDI — backend bağımlılığı giderildi** | FR-ACCT + FR-SALE | 🟢 **KAPANDI 2026-08-05.** Üç turdur bloke eden şey `customer-service`'in koşulsuz 501 kuralıydı; **bu turda o kural silindi**: `CustomerBusinessRules.checkNoUnsupportedCrossServiceSearchCriterion` kaldırıldı, yerine gerçek çözümleme geldi — `accountNumber` → `GET /api/accounts/{n}` (account-service, ADR-013 §5), `orderNumber` → `GET /api/orders/{n}` (order-service, ADR-016 §3.2). §1.7b'nin **kontrat karşılaştırması burada işe yaradı**: iki yanıt da zaten `customerNumber` taşıyor, bu yüzden **hiçbir yeni uç açılmadı**. Kurallar: yalnız **Active** hesap / **MIDLWARE** sipariş eşleşir, müşteri de aktif olmalı, çözülemeyen numara **hiçbir şeyle** eşleşmez (browse'a genişlemez), sahip servis düşükse **503** (boş sayfa değil). Kayıt: ADR-005 §Addendum 2026-08-05 | **Frontend (bu turda yapıldı):** iki `FormControl`'ün `disabled: true`'su kaldırıldı ve ikisi de `FILTERABLE_CONTROLS`'a girdi (böylece Search'ü aktive ederler, istekte giderler, 400 alan hatası bağlanır, Clear temizler); `CustomerSearchCriteria`'ya `accountNumber`/`orderNumber` **string** olarak eklendi (KR-11/KR-12 sabit genişlikte, `Number()` baştaki sıfırı yerdi); rakam kısıtı **mevcut** `TextInput` `digitsOnly` girdisiyle (yeni klavye handler'ı yazılmadı, `type=number` kullanılmadı — yapıştırma ve mobil klavye de kapsanır); `UI-SEARCH-DEFERRED-HINT` **silindi** (§2.24). **İstemci hiçbir filtreleme yapmıyor** — KR-02 sunucu kuralıdır, yüklenmiş tablo üzerinde eleme yok | 🟢 **tamamlandı** |
| 1.9 | `PasswordInput` bileşeni | — | — | `shared/ui/`'da yazılmaz (FE-ADR-005 §P5) | 🟢 karara bağlandı |
| 1.10 | Mock **Login v2** ekranı | FR-AUTH | Keycloak teması tamamlandı | Angular'a port edilmez (FE-ADR-005 §P4) | 🟢 karara bağlandı |

**Kapsam içi:** Customer Search, Create Customer, Customer Info'nun demografik +
adres + iletişim bölümleri, **hesap bölümü (FR-ACCT — 2026-07-24'te girdi, §1A)**,
**ürün görüntüleme (FR-PROD-01..02 — 2026-07-31'de girdi, §1B)**,
**§2.7 ürün satış sihirbazı (FR-SALE-01/02 — 2026-08-03'te girdi, §2B.14)**,
referans veri (cities/districts/lookups), auth kabuğu.

---

## 1B. Kapsam güncellemesi — product-service geldi (2026-07-31)

Backend'e `product-service` eklendi (dev commit `ec2e6cd`;
`docs/api/product-service.md`; FR-PROD-01..02 **salt okunur** dilim, port 8086,
gateway `/api/products/**`, `/api/offers/**`, `/api/campaigns/**`). Bağlayıcı
frontend kararı **FE-ADR-013 §Amendment B (2026-07-31)** — buradaki satırlar
özet + izleme.

| # | Konu | Karar | Durum |
|---|---|---|---|
| 1B.1 | **Ürün görüntüleme kapsamı** | Customer Info ürün bölümü "Coming soon"dan çıkıp geliştirilecek kapsama girdi → yeni kardeş feature **`features/product/`**. Tüketilen kontrat **yalnız iki uç**: `GET /api/products?accountNumber=` (FR-PROD-01) ve `GET /api/products/{id}` (FR-PROD-02). ✅ **TAMAMLANDI (2026-07-31):** `features/product/section/` ProductSection + ProductDetailDialog (uygulama kayıtları §4.28) | 🟢 **tamamlandı** |
| 1B.2 | **Sayfalama YOK** | FR-PROD-01 hiçbir sayfalama kuralı tanımlamıyor ve uç **düz dizi** döndürüyor (zarf yok). İstemci ne `page`/`size` gönderir ne de `shared/patterns/Pagination` kullanır; mock'un 4/8/12 seçicisi **uygulanmadı** (hesap tablosundaki §4.24/5 kararının aynısı). Spec bunu `params.keys() === ['accountNumber']` ve "sayfalama kontrolü yok" iddialarıyla pinliyor | 🟢 karara bağlandı |
| 1B.3 | **Pasif ürünler listede kalır** | AC-PROD-01-03: liste hem Active hem Passive ürünleri taşır; `productStatus` backend'de `PROD.status_id`'den **türetilir**. İstemci **filtrelemez, yeniden sıralamaz** — hesap bölümündeki Active-then-Passive felsefesinin aynısı (§1A.4) | 🟢 karara bağlandı |
| 1B.4 | **Kampanya kimliği = `campaign_code`** | Satır yanıtındaki `campaignId` **public kampanya kodudur** (`cmpg.campaign_code`, ör. `CMP-ADSL-01`); iç `cmpg.id` hiçbir yanıtta yok. UI'da gösterilen/kullanılan tek kampanya kimliği budur. Kampanyasız üründe `campaignName` **ve** `campaignId` birlikte `null` gelir → `"-"` render'ı **açıkça frontend'in işi** (kontrat notu) | 🟢 karara bağlandı |
| 1B.5 | **`MSG-PROD-NONE` frontend-only** | Ürünsüz hesap **200 `[]`** döner; backend bu anahtarı **hiç üretmez** (AC-PROD-01-02 metnini frontend yazar). Katalogda zaten vardı (analist metni, `messages.ts`); `i18n.spec.ts`'in backend-anahtarları listesine **bilinçle eklenmedi** | 🟢 karara bağlandı |
| 1B.6 | **`MSG-PROD-NOT-FOUND` (404)** | Dokümante edilmiş **proje eklentisi** — analist kataloğunda yok, çünkü FR-PROD-02 her zaman kullanıcının az önce gördüğü bir satırdan açılır. Backend **döndürüyor**, dolayısıyla katalog + `i18n.spec.ts` backend listesine eklendi. EN/TR metin backend dokümanının kendi önerisi (*"Product not found." / "Ürün bulunamadı."*) — uydurulmadı | 🟢 karara bağlandı (2026-07-31 eklendi) |
| 1B.7 | **Türetilen alanlar backend'de** | Servis tipi (`PROD_SPEC.service_type_id` → GNL_TP), ürün durumu (Active/Passive) ve kampanya toplam fiyatı (üye teklif fiyatlarının toplamı) **sunucuda hesaplanır**; `CMPG`'nin fiyat kolonu bile yok. İstemci hiçbirini yeniden hesaplamaz, geleni gösterir | 🟢 karara bağlandı |
| 1B.8 | **Alt ürünün Service Address'i** | `prod.service_address_id` yalnız kurulumun **ana** ürününde dolu; alt ürün **ebeveyninin** adresini gösterir. Ebeveyn zinciri backend'de yürünür (`ProductBusinessRules.resolveEffectiveServiceAddressId`) ve adres customer-service'ten çözülür. **İstemci ek çağrı yapmaz** — detay modal'ı tek `GET`. Adres yoksa/silinmişse `serviceAddress: null` gelir ve modal yine render edilir | 🟢 karara bağlandı |
| 1B.9 | **Teklif fiyatları analist onayı bekliyor** | `PROD_OFR.product_offer_total_price` workbook'ta boş; backend AC-SALE-01-12 için **299.00 / 149.00 / 49.00** fixture'ları tohumladı ve bunları *"ANALYST APPROVAL PENDING"* olarak kaydetti (`docs/api/product-service.md` §Recorded deviations 1, `document-delta.md` P1). Kampanya toplamı (497.00) **türetilmiş**. **Karar: UI backend'in döndürdüğünü gösterir** — istemci ne fiyat uydurur ne de "onay bekliyor" uyarısı basar. ⚠ Bu turda hiçbir fiyat **ekranda görünmüyor**: fiyatlar yalnız `GET /api/offers` / `GET /api/campaigns` yanıtlarında var, o uçlar da tüketilmiyor (§1B.10). Analist fiyatları onaylayınca/değiştirince frontend'de **sıfır satır** değişir | 🟢 karara bağlandı (2026-07-31) |
| 1B.10 | **`GET /api/offers` + `GET /api/campaigns` tüketilmiyor** | İki katalog ucu hazır ve gateway'de route'lu, ama tek tüketicileri **Offer Selection** akışı — o da order-service'e bağlı, kapsam dışı (§1B.11). Kullanılmayan istemci kodu yazılmadı: `ProductApiService` yalnız iki GET taşıyor. Ürün **detay** modal'ındaki kampanya bilgisi `GET /api/products/{id}`'nin kendi `campaign` alanından gelir (kontrat doğrulandı) — katalog ucuna gerek yok | 🟢 karara bağlandı |
| 1B.11 | **Offer Selection / Product Configuration / Submit Order hâlâ kapsam dışı** | Mock'taki Offer Selection bir **seçim akışıdır**: sepet, ürün karakteristik konfigürasyonu ve sipariş gönderimi order-service'e bağlı ve **hiçbiri yok** (`docs/api/product-service.md` §Deliberate limitations: "no basket, no order, no involvement write command"). Üç ekran order-service ile **bir bütün olarak** yapılacak; bu turda başlatılmadı. §1.1–1.3 satırları **değişmedi** | 🟢 karara bağlandı (teyit 2026-07-31) |

---

## 1A. Kapsam güncellemesi — account-service geldi (2026-07-24)

Backend'e `account-service` eklendi (dev commit `635b9a2`; ADR-013/014,
`docs/api/account-service.md`; FR-ACCT-01..04 + KR-11). Bağlayıcı frontend
kararı **FE-ADR-013 §Amendment (2026-07-24)** — buradaki satırlar özet + izleme.

| # | Konu | Karar | Durum |
|---|---|---|---|
| 1A.1 | **Hesap bölümü kapsamı** | Customer Info hesap bölümü geliştirilecek kapsama girdi → yeni kardeş feature **`features/account/`** (FE-ADR-003 §Consequences'ın öngördüğü evrim). Kontrat: `/api/accounts/**` — tam 5 endpoint, `docs/api/account-service.md`. ✅ **TAMAMLANDI (2026-07-25):** `features/account/section/` AccountSection + AccountFormDialog — liste/oluştur/güncelle/pasifleştir canlı (uygulama kayıtları §4.24) | 🟢 **tamamlandı** |
| 1A.2 | **K-8 / 223 Customer Account** | 223 hiçbir API yanıtında görünmez (liste 224-only; 223 numarası tekil endpoint'lerde 404 `MSG-ACCT-NOT-FOUND`). **UI yalnız 224 Billing Account bilir** — 223 için hiçbir ekran, etiket, satır veya özel durum yazılmaz | 🟢 karara bağlandı |
| 1A.3 | **KR-11 Account Number** | `VARCHAR(10)`, Luhn, immutable, asla yeniden kullanılmaz (ADR-014). UI'da **salt okunur metin** — hiçbir form kontrolüne bağlanmaz. `PUT` gövdesi tam olarak `{accountName, addressId}`; fazladan/immutable alan → **400 `MSG-ACCT-IMMUTABLE-FIELD`** (yani "objeyi olduğu gibi geri gönder" update formu kontrat ihlalidir) | 🟢 karara bağlandı |
| 1A.4 | **Silme = pasifleştirme** | `DELETE` → 204; satır listede **Passive olarak kalmaya devam eder** (AC-ACCT-04-02). Sıralama backend'de sabit: Active önce, sonra Passive; grup içinde `accountNumber` ASC (AC-ACCT-01-04) — **UI yeniden sıralamaz**. Aktif-ürün kilidi backend'de: 409 `MSG-ACCT-HAS-PRODUCTS`; pasif hesapta update/re-delete → 409 `MSG-ACCT-NOT-ACTIVE`. Silme onayı (`MSG-ACCT-DELETE-CONFIRM`) ve başarı mesajı (`MSG-ACCT-DELETED`) frontend-only | 🟢 karara bağlandı |
| 1A.5 | **Liste çağrısı şekli** | `GET /api/accounts?customerId={customerNumber}` — `customerId` **public müşteri numarasıdır** (1001), internal id değil. Yanıt düz JSON **dizi** (sayfalama zarfı YOK — `Page` zarfı tartışması §4.4 burada geçerli değil) | ⚪ kayıt |
| 1A.7 | **Account Description alanı kontratta YOK** | 🔴 **BLOKE (2026-07-29, D1 doğrulaması).** Talep: hesap oluşturma/düzenleme formuna AC-ACCT-02-02'nin "Account Description (opsiyonel)" alanı eklensin. **Üç kaynak da alanı tanımıyor:** (1) `docs/api/account-service.md` §Representation yanıt şeklini tam **6 anahtara** pinliyor (`accountNumber, accountName, accountTypeCode, accountTypeName, billingAddressId, accountStatus`) ve Postman testi `Object.keys().sort()` ile bunu iddia ediyor; (2) create gövdesi **tam olarak** `{customerId, accountName, addressId}`, update gövdesi **tam olarak** `{accountName, addressId}` — **fazladan herhangi bir özellik 400 `MSG-ACCT-IMMUTABLE-FIELD`** ile *reddediliyor* (yok sayılmıyor), yani `description` göndermek isteği bozar; (3) `docs/requirements/functional-requirements.md` FR-ACCT-02/03 ve `document-delta.md` #4 ("AC-ACCT-02-02: Account Name + Billing Address required") description'dan hiç söz etmiyor; mock §6.4 hesap tablosunda da böyle bir kolon/alan yok. **Frontend tek başına çözemez** — salt görsel bir alan eklemek ya sessizce kaybolan bir veri (FE-ADR-013 §a ihlali) ya da 400 üreten bir istek olurdu. **Gerekli:** analist AC-ACCT-02-02'nin description maddesini teyit ederse backend'de kolon + kontrat genişletmesi (yeni bir account-service PR'ı) gerekir; o zaman FE alanı 15 dakikada eklenir. Uygulanmadı, uydurulmadı | 🔴 **karar bekliyor — backend/analist** |
| 1A.6 | **i18n katalog eksiği** | Frontend kataloğunda yalnız analist üçlüsü var (`MSG-ACCT-HAS-PRODUCTS`, `MSG-ACCT-DELETE-CONFIRM`, `MSG-ACCT-DELETED`). **5 proje-ekli anahtar eksik:** `MSG-ACCT-NOT-FOUND`, `MSG-ACCT-NOT-ACTIVE`, `MSG-ACCT-IMMUTABLE-FIELD`, `MSG-ACCT-DUP-NUMBER`, `MSG-ACCT-NUMBER-CAPACITY-EXCEEDED`. EN/TR metinler hazır: `docs/architecture/account-service-decisions.md`. Hesap ekranından **önce** kataloğa + `i18n.spec.ts` bütünlük listesine eklenmeli. ✅ **UYGULANDI:** 8 `MSG-ACCT-*` anahtarının tamamı `messages.ts`'te (F3'te eklendi, 2026-07-25'te hesap bölümüyle doğrulandı) | 🟢 **tamamlandı** |

---

## 2. Mock ↔ Backend kontrat uyuşmazlıkları

Tam gerekçeler: `docs/frontend/mock-ui-analysis.md` §5A.
**Genel kural: çakışmada backend kontratı kazanır (FE-ADR-013 §e).**

| # | Konu | Mock | Backend | Karar | Durum |
|---|---|---|---|---|---|
| 2.1 | **"Second name" filtresi** | Ayrı filtre alanı **yok** (önceki analiz yanlış listelemişti); tabloda **kolon** olarak var | Tek `firstName` parametresi **First+Middle birleşiminde** kelime-başı arar (KR-01) | Tek kutu, etiketi **"Name"**; `middleName` yalnız tablo kolonu | 🟢 karara bağlandı |
| 2.2 | **"Role" filtresi** | Filtre **yok**; tabloda kolon var | Liste endpoint'inde `role` parametresi yok; response'ta `role` alanı var | **Role ile filtreleme yapılmaz**; yalnız kolon | 🟢 karara bağlandı |
| 2.3 | **Per page değerleri** | 15 / 25 / 50, varsayılan **15** | ~~API varsayılanı 20~~ → **API varsayılanı 15** (ADR-005 §Amendment, 29.07.2026) | **Varsayılan 15** — KR-04 ile birebir. Eski "varsayılan 20" kararı geçersiz | 🟢 **revize edildi (29.07.2026)** |
| 2.4 | Per page **seçenek listesi** | 15/25/50 | API artık **yalnız 15/30/50** kabul ediyor; başka değer 400 (ADR-005 §Amendment) | **15 / 30 / 50**, varsayılan **15**. ~~20/50/100~~ geçersiz: **100 seçeneği kalktı**, artık gönderilebilir bir değer değil. `PAGE_SIZE_OPTIONS` ile backend whitelist'i birebir aynı kalmalı. **Tek kaynak: `shared/patterns/pagination/page-size.ts`** (02.08.2026, §4.29) | 🟢 **revize edildi (29.07.2026)** |
| 2.5 | **Kriter birleşimi** | Dolu kriterler OR'lanır | Backend de OR'luyor (isim grubu içi AND) | Uyumlu, değişiklik yok | 🟢 karara bağlandı |
| 2.6 | **Doğum tarihi formatı** | `DD.MM.YYYY` (taşımada) | ISO `YYYY-MM-DD` | Taşıma **ISO**; gösterim `dd.MM.yyyy` | 🟢 karara bağlandı |
| 2.7 | **Cinsiyet değerleri** | `male` / `female` | `"Male"` / `"Female"` | Backend | 🟢 karara bağlandı |
| 2.8 | **Adres alanları** | `city`, `district` (string ad), `houseNo`, `description` | `cityId`, `districtId` (Long), `houseFlatNumber`, `addressDescription`, `primary` | Backend; Select'ler **id** taşır | 🟢 karara bağlandı |
| 2.9 | **Şehir/ilçe kaynağı** | Hardcoded (3 şehir, 10 ilçe) | `GET /api/cities`, `/api/cities/{id}/districts` | Backend | 🟢 karara bağlandı |
| 2.10 | **İletişim alanları** | `mobile`, `home` | `mobilePhone`, `homePhone` | Backend | 🟢 karara bağlandı |
| 2.11 | **Müşteri kimliği** | `customerId` (7 hane) | Response alanı **`customerNumber`**; query param adı `customerId` kalıyor | Backend; ikisi karıştırılmaz | 🟢 karara bağlandı |
| 2.12 | **Birincil adres** | `primaryIndex` (dizi indeksi) | `PATCH .../addresses/{addressId}/primary` | Backend (kalıcı id) | 🟢 karara bağlandı |
| 2.13 | **NATID tekilliği** | Client-side, 2 sabit ID | `409 MSG-CUST-DUP-NATID` (soft-deleted dahil, ADR-003) **+ MERNIS** (`400`/`503`) — mock'ta MERNIS hiç yok | Backend; üç hata da UI'da karşılanır | 🟢 karara bağlandı |
| 2.14 | **Türkçe diakritik** | Mock katlıyor (`yilmaz`→`Yılmaz` eşleşir) | Backend katlamıyor | Backend: **diakritik-duyarlı**; fold Angular'a taşınmaz | 🟢 karara bağlandı |
| 2.15 | **Pasif müşteriler** | Listeden çıkarılıyor | `status_id = ACTV AND deleted_date IS NULL` | Uyumlu | ⚪ kayıt |
| 2.16 | **z-index** | Modal `1000`, toast `1200` → toast modal'ın altında kalır | EDS token'ları: modal 1400, toast 1500 | Yalnız `--eds-z-*` token'ları; sayısal literal yasak | 🟢 karara bağlandı |
| 2.17 | **`data-testid`** | 7 ekranda da **0 adet** | — | Her etkileşimli elemanda zorunlu (FE-ADR-009) | 🟢 karara bağlandı |
| 2.18 | **Sidenav etiket boyutu** | `11px` (token dışı) | — | **Caption'a yuvarlanır: `--eds-type-caption-size` (12px)**. Token dışı değer yazılmaz (FE-ADR-011 §f) | 🟢 karara bağlandı |
| 2.19 | **Boş sonuç metni** | "No customer found" + "Use *Create new customer* above…" | Analist kataloğu: `MSG-CUST-NOT-FOUND` = "No customer found! Would you like to create the customer?" | Hangisi geçerli? | 🟡 **analiste soruldu** |
| 2.20 | **Header rol metni** | `"Mobility · Resp. Sales Rep."` | `/api/session/me` yalnız `username`, `subject`, `roles` döndürüyor | **Kaldırılır.** Header'da avatar + `/api/session/me`'den gelen gerçek `username` gösterilir. Mock'un metni prototip örnek verisidir; olmayan veri gösterilmez (FE-ADR-013 §a). Backend'e yeni alan **eklenmez** | 🟢 karara bağlandı |
| 2.21 | **Sidenav öğelerinin işlevi** | B2C/B2B/menu/thumbs-up yalnız görsel durum değiştiriyor | — | **Yalnız B2C aktif.** Diğer üç ikon görünür kalır ama **tıklama hiçbir şey tetiklemez** (navigasyon yok, aktif durum değişmez). `aria-disabled` + `cursor: default` | 🟢 karara bağlandı |
| 2.22 | **Header dil seçici** | Statik `EN` metni | — | AC-LANG-01-01 gereği **gerçek TR/EN değiştirici** olacak | 🟢 karara bağlandı |
| 2.23 | **Login ekranı** | Login v2 tasarımı | Keycloak `crm-lite` teması mock'u birebir uyguluyor | Angular'da yazılmaz | 🟢 karara bağlandı |
| 2.24 | ~~**501 filtrelerinin ipucu metni**~~ | — | — | ✅ **KAPANDI 2026-08-05 — soru ortadan kalktı.** Alanlar artık disabled değil (§1.7c), dolayısıyla "modül yayınlandığında kullanılabilir" diyen bir ipucu **yanlış** olurdu. `UI-SEARCH-DEFERRED-HINT` katalogdan **silindi**; iki alan da diğer filtreler gibi yalnız `errorText` taşıyor. Analistten yanıt beklenmiyor | 🟢 **kapandı** |

---

## 2A. Customer Search (golden path) — ekran bazlı uyuşmazlıklar

Ekran yazılmadan önce netleşmesi gerekenler. Analiz:
`docs/frontend/customer-search-analysis.md`.

> **Not:** 2A.1 ve 2A.2 için **proje kararı zaten var** (§2.1, §2.2 — 🟢) ve
> geliştirme bunlarla ilerler; buradaki satırlar o kararları geri almaz, analist
> **onayını** ister. Onay gelmezse karar aynen geçerli kalır — bu maddeler
> geliştirmeyi **bloke etmiyor**.

| # | Konu | Mock ne diyor | Backend ne veriyor | Analiste sorulan | Durum |
|---|---|---|---|---|---|
| 2A.1 | **"Second name" arama davranışı** | Tabloda **kolon** var; filtre alanı **yok** | Ayrı `middleName` parametresi **yok**; `firstName` parametresi First+Middle **birleşiminde** kelime-başı arıyor (KR-01) | Kullanıcı "Nur" yazıp **yalnız ikinci adı** "Nur" olanları bulmak isterse bu karşılanmalı mı? Bugünkü davranış: "Zeynep Nur" da "Nur Ali" de gelir, ayrım yapılamaz. Tek kutunun etiketi **"Name"** olarak mı kalsın, yoksa "Name / Second name" gibi mi ifade edilsin? | 🟡 **analiste soruldu** (proje kararı §2.1 geçerli) |
| 2A.2 | **"Role" ile filtreleme** | Tabloda **kolon** var; filtre alanı **yok** | Liste endpoint'inde `role` parametresi **yok**; yanıtta `role` alanı **var** | Role'e göre filtreleme bir gereksinim mi? Değilse kolon salt bilgi olarak kalır (bugünkü plan). Gereksinimse **backend'e yeni parametre** gerekir — bu bir backend iş kalemidir, frontend tek başına çözemez | 🟡 **analiste soruldu** (proje kararı §2.2 geçerli) |
| 2A.3 | **Boş sonuç: iki farklı durum** | Tek boş-durum metni var | Aynı 200 + boş liste, iki farklı anlam | Ekran **ilk açıldığında** (hiç arama yapılmadan) sistemde müşteri yoksa gösterilecek metin, **arama sonucu boş** metninden farklı olmalı. **UYGULANDI (2026-07-25):** arama-boş → `MSG-CUST-NOT-FOUND` (analist); browse-boş → proje-yazımı `UI-SEARCH-BROWSE-EMPTY-TITLE/-BODY` (işaretli, FE-ADR-008 §7 kalıbı). Analist metin verirse anahtar içeriği değişir, kod değişmez | 🟡 **analiste soruldu — proje metniyle uygulandı** |
| 2A.4 | **Sayfalama sayfa butonları** | `‹ 1 2 3 ›` — sabit 3 sayfa | `totalPages` 7+ olabilir (137 kayıt / 15 → 10 sayfa) | **UYGULANDI (2026-07-25)** `mock-ui-analysis §6.2`'deki kayıtlı kesme kuralıyla: toplam ≤ 7 → hepsi; aksi halde `1 … (p-1) p (p+1) … son` (`…` tıklanamaz). Analist farklı bir kural verirse `pageItems()` computed'ı tek yerde değişir | 🟡 **analiste soruldu — mock kuralıyla uygulandı** |
| 2A.5 | **Sonuç satırına tıklama** | Satır tıklanınca Customer Info'ya gidiyor | Detay endpoint'i **var** (`GET /api/customers/{customerNumber}`) | ~~Customer ID hücresi link görünümünde ama etkileşimsiz~~ → **AKTİFLEŞTİ (2026-07-25):** Customer Info rotası (`/customers/:customerNumber`) yazıldı; hücre gerçek `routerLink`. `data-testid` (`…-open-link`) değişmedi | 🟢 karara bağlandı (**aktif**) |

---

## 3. Analist dokümanı ↔ backend çelişkileri (frontend'i etkileyenler)

Backend tarafındaki tam kayıt: `docs/requirements/document-delta.md` §Open
conflicts. Aşağıdakiler frontend'i doğrudan bağlar.

| # | Çelişki | Durum |
|---|---|---|
| 3.1 | ~~**KR-04 sayfa boyutu:** analist UI varsayılanı 15 (15/30/50) vs API varsayılanı 20~~ | 🟢 **kapandı (29.07.2026)** — analistler beş API bug'ı açtı (BUG-API-CUST-01-14/-16/-17/-18/-19); API **15 varsayılan + 15/30/50 whitelist** oldu, çelişki kalmadı (bkz. 2.3/2.4) |
| 3.2 | **FR-AUTH-01 uygulama içi login formu varsayıyor** | 🟢 **karara bağlandı** — ADR-006 süperseder; Keycloak sayfası + tema. `document-delta.md` #6'da kayıtlı |
| 3.3 | **AC-CUST-01-02** ("LBL-SEARCH boşken pasif") ↔ **AC-CUST-01-00** (login sonrası tüm müşteriler) gerilimi | 🟢 **karara bağlandı** — ekran açılışında **otomatik kritersiz çağrı** (browse modu) yapılır; `Search` butonu yalnız **yeniden** aramayı tetikler ve tüm alanlar boşken pasif kalır. İki AC de karşılanır |
| 3.4 | **ADR-011 analist onayı** (workbook USERS tablosunun Keycloak lehine terk edilmesi) | 🟡 **analiste soruldu** — ADR-011 status hâlâ "Proposed" |
| 3.5 | Katalogda karşılığı olmayan 10 backend anahtarı (`MSG-VALIDATION-ERROR`, `MSG-AUTH-*`, `MSG-ADDR-LAST-DELETE`, …) | 🟡 **analiste soruldu** — metinleri biz mi yazacağız, analist mi verecek? Şimdilik proje-yazımı, işaretli |
| 3.6 | **AC-SALE-01-12 Submit ekranında "Order ID" gösteriyor — ama submit'ten ÖNCE** | ✅ **ANALİST CEVAPLADI 10.08.2026 — çelişki KAPANDI, mock haklıydı.** Karar A1: *"Kullanıcı işleme başladığında ilgili tablolarda kayıtlar zaten oluşturulmalı… statüleri MIDLWARE değil… Dolayısıyla Order ID bu noktada gösterilebilir."* Yani AC editöryal hata DEĞİLDİ. Backend **ADR-018** ile buna göre değişti: `POST /api/orders/drafts` siparişi workbook'ta zaten var olan `GNL_ST WAIT` statüsüyle yaratıyor ve KR-12 numarasını **Submit'ten önce** tahsis ediyor. İki eski itiraz da cevaplandı: numara terk edilen satışta gerçekten yanıyor (kabul edildi — KR-12 kalıcı, tıpkı KR-11 gibi; terk edilmiş taslak numarasını denenmiş bir işlemin kaydı olarak saklıyor) ve AC-SALE-01-16 itirazı düşüyor çünkü taslak SEPETİ değil SİPARİŞİ yaratıyor: WAIT siparişinin kalemi, ürünü, saga'sı ve onu adlandıran komutu yok. **Frontend sonucu: seçenek (a)'nın em-dash'i artık gereksiz** — numara ekran açıldığında zaten var. Bu değişiklik **bir sonraki frontend PR'ının işi**; bu satır kaydı güncel tutmak için burada. Aşağıdaki tarihsel analiz olduğu gibi korunuyor. — Önceki durum: 🟢 **(a) UYGULANDI 03.08.2026 — analist onayı hâlâ bekleniyor.** `submit-order-step.html` "Order Number" satırını her zaman render ediyor; değer `201` gelene kadar em-dash (`—`), sonra `OrderResponse.orderNumber`. İstemci hiçbir koşulda numara üretmiyor. Analist (b)’yi isterse tek satırlık `@if` değişir. Aşağıdaki analiz kaydı korunuyor. — **analiste soruldu (backend kaydı: ADR-016 §8.3).** AC'nin sırası: *"Product Configuration'da LBL-NEXT → doğrulamalar başarılıysa Submit ekranı açılmalı; **Order ID**, tekliflerin ID/ad/kampanya/tutar bilgileri, Service Address ve Total Amount gösterilmelidir."* Backend bunu **editöryal sıralama hatası** okumuş: sepet hiç kalıcı değil (§2.7'nin temel kararı), dolayısıyla LBL-SUBMIT'e basılana kadar **hiçbir sipariş yoktur** ve gösterilecek bir numara üretilemez. Numara rezervasyonu **iki bağımsız gerekçeyle** reddedildi: (a) terk edilen her satış gapless bir KR-12 numarasını yakardı, (b) tam olarak AC-SALE-01-16'nın yasakladığı "tamamlanmamış satışın backend izi"ni yaratırdı. 🔴 **Mock, AC'yi doğruluyor — çelişki gerçek ve iki taraflı.** `Submit Order.dc.html` (satır 121–124) "Order details" panelinin **ilk satırı** olarak `Order Number` etiketini ve `{{ orderId }}` değerini **submit butonundan önce** render ediyor; mock bu numarayı `componentDidMount`'ta istemcide üretiyor (`'0' + rastgele 7 hane`). Yani AC-SALE-01-12 **editöryal hata değil**, mock'ta bilinçli bir tasarım — ama üretilebilmesinin tek sebebi mock'un numarayı **uydurması**. Backend'de bu mümkün değil: numara KR-12 dizisinden gelir ve ancak `POST /api/orders` işlendikten sonra vardır. **Frontend seçenekleri (karar analiste ait, uydurulmadı):** **(a)** satır render edilir, değer submit'e kadar `—` (em-dash) — mock'un düzeni birebir korunur, kullanıcı alanın var olduğunu ve dolacağını görür; **(b)** satır submit'e kadar hiç render edilmez, sonra belirir — düzen submit anında kayar; **(c)** istemci sahte numara üretir — **reddedildi**, FE-ADR-013 §a (backend'i olmayan veri üretilmez) ve KR-12'yi taklit eder. **Öneri: (a)** — mock'un grid düzenini bozmayan, hiçbir şey uydurmayan ve "numara submit sonrası gelecek" beklentisini dürüstçe kuran tek seçenek. Faz 2'de (a) uygulanacak; analist (b)'yi isterse tek satır değişir |
| 3.7 | **Mock sepeti `localStorage`'a yazıyor — AC-SALE-01-16 ile çelişiyor** | 🟢 **karara bağlandı (frontend).** Mock bundle'ın sıfırlama listesi satış durumunu tarayıcıda kalıcı tutuyor: `eds_sale_account`, `eds_sale_basket`, `eds_sale_address`, `eds_sale_config`, `eds_pending_sale`, `eds_open_summary` (bundle'da **fiilen doğrulandı**, 02.08.2026). AC-SALE-01-16 ise *"tamamlanmamış satış daha sonra işleme alınmamalıdır"* diyor ve backend bunu **yapısal olarak** çözmüş: sepet tablosu yok, `POST /api/orders` sepeti bir kez ve bütün olarak alıyor — terk edilecek bir şey yok. **Frontend aynı felsefeyi izler:** sepet YALNIZ oturum-içi Signal state'inde yaşar; `localStorage`/`sessionStorage`'a **asla** yazılmaz. Sayfa yenilenince sepet kaybolur — bu bir hata değil, gereksinimin ta kendisi. Mock'un kalıcılığı **taşınmaz** (mock görsel referanstır, davranış sözleşmesi değil — FE-ADR-013 §e) |
| 3.8 | **Sipariş ve hesap numaraları aynı değer uzayını paylaşıyor** | ⚪ **kayıt (analist notu, ADR-016 §8.1).** Seed sipariş numarası `1261000002` aynı zamanda bir hesap numarası; iki dizi normal işleyişte sürekli örtüşüyor. Sistemik maliyeti yok (ayrı veritabanı, ayrı servis, ortak isim uzayı yok) ve KR-02 müşteri aramasına **iki ayrı alan** verdiği için kullanıcı hangi tür numarayı yazdığını belirtiyor. **Frontend etkisi:** bağlamsız 10 haneli bir numara göz kararıyla sınıflandırılamaz → sipariş numarası gösterilen her yerde **etiketi ile birlikte** gösterilmeli (çıplak numara değil). Ekran içi bir "numara türünü tahmin et" mantığı **yazılmaz** |
| 3.9 | **Transfer / Service address change mock'ta var, FR/AC'de yok** | 🟡 **analiste soruldu (ADR-016 §8.2).** Mock hesap satırında *Start new sale* yanında *Transfer* ve *Service address change* aksiyonlarını sunuyor; katalogda `BSN_INTER_TYPE` TRANSFER(8)/CANCEL(9) değerleri var. **Hiçbir FR/AC ikisini de kapsamıyor**, backend yalnız NEWSALE'i uyguladı ve diğer ikisini yazmadı. **Frontend:** bu iki aksiyon **üretilmez** — backend'i olmayan işleve buton/placeholder konmaz (FE-ADR-013 §a, A3 kuralı). Yalnız `LBL-START-NEW-SALE` gerçekleşir |
| 3.10 | **KR-12 sipariş numarası formatı analist onayı bekliyor** | 🟡 **analiste soruldu (ADR-016 §4, document-delta P11).** Format `[T][YY][SSSSSS][C]` **proje önerisi**, analist kuralı değil. **Frontend etkisi yok ve olmamalı:** numara **salt okunur** gösterilir; istemci onu ne üretir, ne doğrular, ne ayrıştırır, ne de parçalarına anlam yükler. Format değişirse frontend'de **sıfır satır** değişir |
| 3.11 | **Teklif/kampanya fiyatları "load-bearing" oldu ama hâlâ onay bekliyor** | 🟡 **analiste soruldu (document-delta P1/P5/P13, açık çelişki #10).** Fiyatlar seed fixture; artık yalnız ekranda görünmüyorlar, **kalıcı sipariş geçmişine snapshot ediliyorlar** (`cust_ord.total_amount`, `cust_ord_item.amount`). §1B.9'daki eski karar (*"bu turda hiçbir fiyat ekranda yok"*) satış akışıyla **aşıldı**: AC-SALE-01-12 Total Amount'u ve teklif başına tutarı açıkça istiyor. **Frontend kararı:** backend ne dönerse **aynen** gösterilir — istemci fiyat **hesaplamaz**, toplamaz, yuvarlamaz, para birimi dönüştürmez. Sepet toplamı için bile: Submit ekranındaki otorite `POST /api/orders` yanıtının `totalAmount`'udur. (Sepet aşamasında henüz sipariş yokken gösterilen ara toplam bir **UX önizlemesidir** — bu ayrım Faz 2'de kayda geçecek) |

---

## 2B. Satış akışı (§2.7) — mock ↔ backend uyuşmazlıkları

| # | Konu | Durum |
|---|---|---|
| 2B.1 | **Üç satış ekranının mock markup'ı MEVCUT ve bağlayıcı** | 🟢 **doğrulandı (02.08.2026).** ⚠️ **Bu satırın ilk hali yanlıştı** ("markup bundle'da yok") ve düzeltildi: `Guncel_Etiya_CRM_Lite_Full_App.html` bir **self-extracting bundle**'dır — ekran markup'ları `<script type="__bundler/manifest">` içinde **base64 + gzip** olarak durur, bu yüzden düz `grep` hiçbir şey bulmaz. Doğru yöntem: manifest JSON'unu ayrıştır → `entry.data`'yı base64 çöz → `entry.compressed` ise `gunzip`. `__bundler/ext_resources` listesi üç ekranı adıyla zaten gösteriyor. Çıkarılan boyutlar `mock-ui-analysis.md` §1 tablosuyla **birebir**: `Offer Selection.dc.html` 52 356 B, `Product Configuration.dc.html` 35 323 B, `Submit Order.dc.html` 24 660 B. **Sonuç: §4.28/4'ün "mock geri getirilemez" emsali bu üç ekran için GEÇERSİZ** — düzen, boşluk, panel yapısı ve buton yerleşimi doğrulanabilir olduğundan FE-ADR-011 gereği **mock'a birebir uyulacak**; §6.5–6.7 özeti artık tek kaynak değil, gerçek markup esas. (Aynı çıkarma yöntemi ürün modalı için de denenmeli — §4.28/4 yeniden değerlendirilebilir.) |
| 2B.2 | **Sepet zorunlu olarak tam 3 tekliften oluşur** | 🟢 **kontrattan doğrulandı.** AC-SALE-01-08 + `BasketValidationRules`: sepette **tam bir INTERNET + tam bir RESOURCE + tam bir ACTIVATION** teklifi olmalı (eksik → `MSG-SALE-NO-*`, fazla → `MSG-SALE-MULTI-*`). Yani geçerli her sepet **tam 3 kalemdir**. Mock'un "Basket" paneli serbest bir alışveriş sepeti gibi görünüyor ama kontrat bunu sıkı biçimde kısıtlıyor. **Frontend:** kural istemcide **önizlenebilir** (hangi servis tipinin eksik olduğunu göstermek UX'tir), ama nihai otorite backend — istemci kendi kararıyla LBL-NEXT'i geçirmez/engellemez ötesinde bir şey yapmaz (FE-ADR-007 §3) |
| 2B.3 | **`GET /api/offers` yalnız AKTİF teklifleri döndürüyor → `MSG-SALE-OFFER-INACTIVE` katalogdan üretilemez** | ⚪ **kayıt.** Pasif teklif fixture'ı **var** (document-delta P10: `PROD_OFR` 11, `ADSL 4MB Offer (retired)`) ama uç ACTV-filtreli olduğu için katalog listesinde **görünmüyor**. Dolayısıyla kullanıcı normal akışta pasif bir teklifi sepete ekleyemez. **Yine de hata yolu ele alınır:** teklif, sepete eklendikten sonra (başka bir oturumda/işlemde) pasifleşebilir ve submit anında `400 MSG-SALE-OFFER-INACTIVE` dönebilir. Anahtar i18n'e eklenir ve gösterilir — "erişilemez" varsayımıyla atlanmaz |
| 2B.4 | **Satış uçlarının hiçbirinde sayfalama YOK** | 🟢 **kontrattan doğrulandı (02.08.2026).** `GET /api/offers`, `GET /api/offers/{id}/characteristics`, `GET /api/campaigns`, `GET /api/orders/{n}` → hepsi **düz dizi / tekil nesne**; `product-service` ve `order-service` kaynak ağacında **tek bir `Pageable`/`Page<` bile yok** (arama ile doğrulandı). Dolayısıyla bu ekranlar `page`/`size` **göndermez** ve `Pagination` desenini kullanmaz — §1B.2'deki ürün listesi kararının aynısı. Faz 0'da kurulan `PAGE_SIZE_OPTIONS` tek kaynağının burada **tüketicisi yok**; yeni bir sayfa boyutu değeri de **icat edilmez** |
| 2B.5 | **Sipariş listesi ekranı YAPILAMAZ** | 🟢 **karara bağlandı.** order-service'te **yalnız** `POST /api/orders` ve `GET /api/orders/{orderNumber}` var; liste ucu **bilinçle yok** (*"hiçbir FR istemiyor, liste kontratı icat etmek sıralama/filtreleme/sayfalama kurallarını icat etmek demek"*). **Frontend:** "Siparişlerim" ekranı, sipariş geçmişi tablosu veya sipariş arama **üretilmez**. Ayrıca `customer-service`'in KR-02 `orderNumber` filtresi hâlâ **501** — Customer Search'teki o filtre **disabled kalır** (§2A ve FE-ADR-013 §b'deki mevcut karar korunuyor) |
| 2B.6 | **Sipariş iptali YOK** | 🟢 **karara bağlandı.** İptal ucu yok (KR-7 kapsam dışı); `CANCELLED` durumuna yalnız orkestrasyonun **kendi telafisi** ulaşıyor. **Frontend:** iptal butonu/akışı **üretilmez** |
| 2B.7 | **Mock'tan doğrulanan bağlayıcı düzen (FE-ADR-011)** | 🟢 **kayıt — Faz 2'nin tasarım kaynağı.** **Ortak çerçeve (üç ekranda birebir aynı):** h1 (`h2-size` 20px) + opsiyonel `New sale · Account {n}` alt satırı; altında 3 adımlı **stepper** — etiketler **`Offer selection` / `Product configuration` / `Order summary`** (⚠️ 3. adımın stepper etiketi "Order summary", ama sayfa başlığı **"Submit order"** — ikisi farklı, ikisi de korunacak), 24px daire + `check` ikonu (tamamlanan) + esneyen 1px bağlayıcı çizgi; içerik `overflow-y:auto`, sayfa footer'ı sabit. **Offer Selection:** grid `minmax(0,1fr) 360px`; solda sekmeli kart (Product offers / Campaigns) → arama formu (katalog: `Category` Select + `Offer ID` + `Offer name` + `Search`; kampanya: `Campaign` Select + `Campaign ID` + `Campaign name` + `Search`) → sonuç tablosu (katalog kolonları **Prod offer ID / Prod offer name / Category / Price** + 40px seçim kolonu; kampanya kolonları **Campaign ID / Campaign / Includes / Price** + 40px) → kart footer'ı `{seçim sayısı}` + **`Add to basket`** (secondary, `shopping-cart`); boş sonuç: `search-x` 24px + *"No offers match your search."*. Sağda **Basket** paneli: başlık + adet; boşken 48px yuvarlak `shopping-cart` + **"Basket is empty"** + *"Search offers and add them to the basket."*; doluyken kalem listesi (id / ad / opsiyonel meta, bundle'da 2px sol çizgili alt kalemler, satır sonunda fiyat + `x` kaldırma), altında **kural uyarısı** (`alert-triangle` + warning metni), **`Total amount`** satırı, en altta **`Clear`** (secondary sm, `trash-2`). Sayfa footer'ı sağa yaslı: `Cancel` + `Next` (primary, `arrow-right`, sepet boşken disabled). **Product Configuration:** her ürün için kart — başlık grid'i `Prod Offer Name` / `Prod Offer ID`, gövde **2 kolonlu** (`1fr 1fr`) FormField grid'i; altında **`Address info`** kartı (*"Select a service address from existing addresses."*), `repeat(auto-fill,minmax(240px,1fr))` adres kartları (radyo + 3 satır) + **`Add new address`** döşemesi, hata satırı `alert-circle` + *"Please select or add a service address to continue."*; `Add address` **modal**'ı 5 zorunlu alanla (City/District/Street/House-flat number/Description). Footer: solda `Previous` (secondary, `arrow-left`), sağda `Cancel` (**ghost**) + `Next` (primary, `arrow-right`). **Submit Order:** tek **"Order details"** kartı, `160px minmax(0,1fr)` grid — sırayla **Order Number** (bkz. 3.6), **Order items** (çerçeveli tablo: id / ad + opsiyonel kampanya alt etiketi / sağa yaslı fiyat), **Service address** (satır + opsiyonel açıklama); kart altında ayrı **`Total amount`** şeridi. Footer: solda `Previous`, sağda `Cancel` + **`Submit`** (primary, `check`). Onay modalı 420px, `info` 20px, başlık + gövde + `No`/`Yes`. ⚠️ Mock'un `PasswordInput`'u (XDSL Password) `shared/ui`'de **yok** — karakteristik `dataType` kontrata göre NUMBER/BOOLEAN/TEXT/DATE, `password` yok; kontrat kazanır (FE-ADR-013 §e), parola alanı **üretilmez** |
| 2B.8 | **Mock'ta submit sonrası "başarı durumu" YOK — yönlendirme var** | ⚠️ **ADR-018 (10.08.2026) bu kararın zeminini değiştirdi — bir sonraki frontend PR'ında yeniden ele alınacak.** Submit artık **202** dönüyor ve satış asenkron: numara zaten Submit ekranında (bkz. §3.6), ama sipariş yanıt geldiğinde **tamamlanmış değil** — `GET /api/orders/{n}/status` `PROCESSING` → `COMPLETED`/`FAILED` diye poll ediliyor. Yani "201'den numarayı göster ve yönlendir" mekaniği artık eksik: yönlendirme sonrası satışın gerçekten bitip bitmediğini kullanıcı göremiyor. **AC-SALE-01-15'in işlem ekranı yorumu (ayrı ekran mı, satır içi durum mu, toast mı) PROJE YORUMU ve analistin nihai UX açıklamasını BEKLİYOR** — ADR-018 §6 bunu analist onaylı gereksinim olarak KAYDETMİYOR ve backend sözleşmesi bilerek UX-nötr. Aşağıdaki karar tarihsel kayıt olarak korunuyor. — Önceki durum: 🟢 **karara bağlandı.** Gerçek markup: `onConfirmSubmit` → `doSubmit()` → `eds_pending_sale`'i localStorage'a yazar ve **anında** `Customer Info`'ya gider (`goCustomer()`); Customer Info açılışta bu anahtarı okuyup siler ve siparişi ilgili hesaba işler. Yani mock'ta yerinde başarı ekranı **yok**, sipariş numarası submit'ten sonra **hiç gösterilmiyor**. Ama AC-SALE-01-15 *"Sipariş Alındı, İşleniyor…"* durumunu **açıkça istiyor** ve ADR-016 §8.3 numaranın ancak `201`'den sonra var olabileceğini söylüyor. **Karar (proje kararı, mock'ta karşılığı yok — kayıt altında):** Submit adımı **yerinde başarı durumuna** geçer; `MIDLWARE` durum metni + salt okunur sipariş numarası (`201`'den) gösterilir, birincil aksiyon **"Müşteriye dön"** → `/customers/:customerNumber` hesap sekmesi. Gerekçe: (1) mock'un "anında yönlendir" davranışı AC-SALE-01-15'i **karşılamıyor** — numarayı ve durumu kullanıcı hiç görmüyor; (2) mock'un localStorage köprüsü AC-SALE-01-16 gereği zaten taşınmıyor (bkz. 3.7), dolayısıyla mock'un yönlendirme mekanizması da taşınamaz; (3) varış noktası yine mock'unkiyle **aynı** (Customer Info), sadece kullanıcı numarayı gördükten sonra gidiyor. Mock burada **davranış** kaynağı değil, tasarım kaynağı (FE-ADR-013) |
| 2B.9 | **Sihirbaz `features/order/` yerine `features/customer/sales/` altında** | 🟢 **karara bağlandı ve uygulandı (03.08.2026).** Talimat sihirbazı `features/order/sale/` altına koymayı söylüyordu; **uygulanmadı, gerekçesi burada.** AC-SALE-01-11 adım 2'de müşterinin **adreslerini** ve FR-ADDR-02 **adres ekleme dialog'unu** kullanıyor. Sihirbaz `features/order/` altında olsaydı bu, FE-ADR-003'ün yasakladığı **yeni bir `order → customer` kardeş yönü** açardı; ADR yalnız **`customer → kardeş`** yönünü onaylıyor (emsaller: hesap §4.24, ürün §4.28/3). **Uygulanan:** sihirbaz + 3 adım `features/customer/sales/` altında; `features/order/` sipariş **domainini** (model, `OrderApiService`, `SaleBasketStore`, `OrderSubmitStore`) tutuyor ve müşteri onu tıpkı hesap/ürün gibi tüketiyor. `features/order/index.ts` bu yönü açıkça belgeliyor; order tarafı customer'dan **hiçbir şey** import etmiyor |
| 2B.10 | **Katalog verisi `core/catalog/` altında (kardeş-feature yasağı)** | 🟢 **karara bağlandı ve uygulandı (03.08.2026).** `GET /api/offers`, `/api/campaigns`, `/api/offers/{id}/characteristics` **iki** feature'ı besliyor: `features/product/` (okuma) ve satış sihirbazı. Feature'lar kardeş import edemediği için katalog `core/catalog/` altına kondu — emsal `core/lookup/` (çapraz-feature referans verisi, ince API istemcisi + tipli model, ekran mantığı yok). Kullanıcının ilk "features/product/'ı genişlet" talimatı FE-ADR-003 §Consequences'i gözden kaçırdığı için düzeltildi (kullanıcı onayladı) |
| 2B.11 | **Adres durumu sihirbazda `CustomerDetailStore` üzerinden** | 🟢 **karara bağlandı ve uygulandı (03.08.2026).** Adım 2 adresleri `CustomerApiService`'ten **doğrudan** çekmiyor; sihirbazın sağladığı `CustomerDetailStore` üzerinden okuyor. Sebep: FR-ADDR-02 dialog'u API modunda **o store'a** yazıyor (`addAddress` → `tap(reloadAddresses)`), dolayısıyla dialog'u yeniden kullanmak store kontratını da yeniden kullanmak demek — aksi halde dialog'un tam `validationErrors` işleme yolu kaybolurdu. Store'a bunun için **dar** bir `loadAddresses(customerNumber)` niyeti eklendi: `load()` üç okuma birden yapıyor (demografik + adres + iletişim); sihirbaz diğer ikisini **gösteremiyor**, dolayısıyla onları çağırmak hiç kullanılmayan iki istek ve kimsenin istemediği bir hata bandı üretirdi |
| 2B.12 | **`POST /api/orders` hata haritası HTTP STATUS'a göre — ⚠️ VARSAYIM, merge sonrası doğrulanmalı** | 🟡 **uygulandı ama doğrulanmadı (03.08.2026).** dev'e gelen bir commit (bu branch'e **merge edilmedi**) `POST /api/products` yolunun `serviceAddressId` müşteriye ait değilse **400**, customer-service'e ulaşılamazsa **503** döndürebildiğini söylüyor. **Gerçek response gövdesi ve error-code alan adı koddan doğrulanamadı.** Bu yüzden `OrderSubmitStore.errorKey` spesifik bir error-code string'ine **bağlanmadı**: önce backend'in kendi `messageKey`'i denenir (katalogda varsa aynen gösterilir), yoksa **HTTP status**'a göre dallanır — 400/409/422 → `MSG-VALIDATION-ERROR`, 0/502/503/504 → `MSG-SALE-SUBMIT-UNAVAILABLE`, diğer → `MSG-INTERNAL-ERROR`. `POST /api/orders` altı servise yayılıyor (ADR-016), yani hata yüzeyi herhangi bir servis kural eklediğinde büyüyor; status'a dallanmak bu durumda **zarifçe** bozuluyor. **Merge sonrası doğrulanacak:** zarf hâlâ `messageKey` taşıyor mu (taşıyorsa bu dallar hiç çalışmaz) ve genel fallback yerine özel bir katalog anahtarı mı gerekiyor. Kod içinde de aynı not var (`order-submit.store.ts`, `fallbackKeyFor` docblock'u) |
| 2B.13 | **Yeni frontend-only anahtar: `MSG-SALE-SUBMIT-UNAVAILABLE`** | ⚪ **kayıt (03.08.2026).** Hiçbir backend bunu döndürmez; genel `MSG-SERVICE-UNAVAILABLE`'ın söyleyemediği şeyi söylediği için var: **sipariş oluşmadı, hiçbir kayıt yapılmadı, tekrar denemek güvenli.** Bu ayrım burada kritik — `POST /api/orders` idempotent değil, dolayısıyla "acaba geçti mi?" diye düşünen kullanıcı yapması gereken retry'ı yapmaz. ADR-016 kısmi çalışmayı telafi ettiği için ifade doğrudur. HTTP katmanının **kendi** ürettiği `MSG-SERVICE-UNAVAILABLE`/`MSG-INTERNAL-ERROR` fallback'leri (zarf yokken) bu yolda "backend cevabı" sayılmaz ve üzerine yazılır |
| 2B.14 | **§2.7 uygulandı — Faz 2 kayıtları** | 🟢 **uygulandı (03.08.2026).** (1) **Tek route, üç adım:** `/customers/:customerNumber/sales/new?accountNumber=…`; `accountNumber` **query** param çünkü satışı niteliyor, kimliğin parçası değil. Ayrı route'lar AC-SALE-01-13/14'ü (Previous ile sepet + girilen yapılandırma korunur) yine paylaşılan store'dan kurardı ve adım 2'ye deep link boş sepetle açılırdı. (2) **Üç store da bileşen-kapsamlı** (`providers`, root **değil**) — akıştan çıkmak sepeti, submit sonucunu ve adres durumunu bileşenle birlikte yok eder (AC-SALE-01-16); yenileme adım 1'e döner, bu **gereksinim**. (3) **Karakteristik alanları Reactive Forms** (FE-ADR-007); istemci validator'ı **yok** — `dataType`/`mandatory` doğrulaması sunucunun (AC-SALE-01-19). Dokunulmamış **zorunlu** alanlar da boş olarak gönderilir ki sunucu `MSG-VAL-CHAR-REQUIRED` ile cevaplasın; dokunulmamış **opsiyonel** alanlar düşürülür (kullanıcının yapmadığı yapılandırma uydurulmaz). (4) **Filtreleme istemci tarafında** — katalog uçları query param almıyor ve sayfalamıyor (2B.4); `?name=` icat etmek kontrat icat etmek olurdu. (5) **Previous adım 1'e dönünce katalog yeniden çekiliyor** (adım bileşeni yeniden kuruluyor); **sepet** korunuyor — korunması gereken o. (6) **`Cancel` mock'ta adım 2'de `ghost`**; `shared/ui` Button'da ghost varyantı yok (yalnız IconButton'da), üç adımda da `secondary` render edilir — tasarım sistemi dışına varyant icat edilmedi (FE-ADR-011 §b). (7) **Sipariş kalemi adları sepetten**, fiyatlar `201`'den: `OrderItemResponse` yalnız `offerId`/`productId`/`amount` taşıyor — ad/kampanya/adres bilinçle geri dönmüyor (ADR-016 §3) |
| 2B.15 | **Yeni `shared/patterns` yeteneği: `ConfirmDialog[tone]`** | ⚪ **kayıt (03.08.2026).** Mock'un iki onay dialog'u aynı dialog değil: yıkıcı olanlar (sil/pasifleştir) danger daire + danger `Yes`; §2.7'nin "Submit this order?" onayı **info** daire + **primary** `Yes` (`Submit Order.dc.html`). `ConfirmDialog`'a `tone: 'danger' \| 'info'` girdisi eklendi (varsayılan `'danger'`, mevcut çağıranlar değişmedi). Yalnız bu iki boya kararını değiştirir; hata durumu her iki tonda da danger kalır — reddedilme reddedilmedir. İkinci bir dialog deseni **üretilmedi** |
| 2B.16 | **AC-SALE-01-02 "hesap yoksa" yolu** | 🟢 **uygulandı (03.08.2026).** Mock "Start new sale"ı genişleyen **hesap satırının içine** koyuyor; hesap yoksa satır da yok, dolayısıyla aksiyon **yapısal olarak** sunulmuyor. Kullanıcıya önce hesap açması gerektiğini söylemek için `AccountSection`'a opsiyonel `emptyHint` girdisi eklendi (**çözülmüş** metin, §4.14) ve Customer Info `UI-SALE-NO-ACCOUNT-HINT`'i geçiriyor. Hesap feature'ı satıştan haberdar olmaz — eline verilen metni render eder. Pasif hesapta buton **render edilir ama disabled** (mock da öyle çiziyor): kullanıcı satışın neden yapılamadığını görür (AC-SALE-01-01) |

---

## 4. Teknik açık kararlar

| # | Konu | Detay | Durum |
|---|---|---|---|
| 4.1 | **OAuth redirect origin** | 🟢 **ÇÖZÜLDÜ (2026-07-23)** — üç eşgüdümlü değişiklik: (1) `api-gateway.yml`'e `server.forward-headers-strategy: framework`; (2) realm'de `crm-bff` client'ına `:4200` redirect URI + webOrigin + post-logout URI eklendi; (3) `keycloak-init` her `up`'ta bunları yeniden uyguluyor (`--import-realm` yalnız ilk açılışta çalıştığı için mevcut `keycloak_db`'ler de kapsanıyor); (4) nginx `$host`'u olduğu gibi iletiyor. Çalışan stack'te doğrulandı: PKCE `S256` ve `directAccessGrants=false` korunuyor. Detay: FE-ADR-004 §Addendum | 🟢 **karara bağlandı** |
| 4.2 | **Frontend host portu** | **4200** (boşta; kullanılanlar 8888/8761/8080/5432/8180). 4.1 çözüldüğü için sabitlendi | 🟢 karara bağlandı |
| 4.3 | **Node sürümü** | Karar: **22.23.1** + npm **10.9.8**. Makinede **ZIP dağıtımı** ile kuruldu (`C:\tools\node-v22.23.1-win-x64`), MSI ile değil: MSI 22.23.1'i kurulu 23.11.1 üzerine bir *downgrade* sayıp `WIX_DOWNGRADE_DETECTED` ile reddediyor, kaldırma ise yönetici hakkı istiyor. PATH `~/.bashrc` ile önceliklendirildi. ⚠️ **Bu yalnız Git Bash içinde geçerli** — sistem PATH'indeki `C:\Program Files\nodejs` (v23.11.1) duruyor, cmd/PowerShell hâlâ onu görüyor. Container tarafı etkilenmez (`node:22.23.1-alpine`). ⚠️ Node 22 maintenance'ta, EOL **2027-04-30** | 🟢 **karara bağlandı ve doğrulandı** |
| 4.4 | **`Page` zarfının JSON şekli** | 🟢 **ŞEKİL DOĞRULANDI (2026-07-25, Swagger customer-controller — ekip üyesi sağladı):** düz `PageImpl` serileştirmesi — metadata üst seviyede (`totalElements/totalPages/size/content/number` + `first/last`; `pageable/sort/numberOfElements/empty` yok sayılıyor). Frontend tipi (`page.model.ts` `SpringPage`→`toPageResult`) birebir uyumlu çıktı; `first/last` sunucu bayrakları artık doğrudan kullanılıyor. ⚠ **Backend önerisi GEÇERLİ KALIR:** `serialization-mode` hâlâ pinli değil — doğrulanan şekil framework varsayılanı; Spring Boot yükseltmesi sessizce değiştirebilir. Pinleme/açık DTO önerisi backend'de bekliyor; frontend zarfı tek dosyada tuttuğu için değişiklik tek-dosya olur | 🟢 **şekil doğrulandı — pinleme önerisi backend'de** |
| 4.5 | **zone.js / zoneless** | 🟢 **UYGULANDI** — zoneless; `provideZonelessChangeDetection()` app.config'te açık, zone.js bağımlılık değil. `@angular/core@22.0.8` `peerDependenciesMeta: { zone.js: { optional: true } }` (FE-ADR-006 §7) | 🟢 karara bağlandı |
| 4.6 | **CI wiring** | **Ayrı iş.** Frontend job'ı backend Maven job'ından bağımsız çalışır; frontend lint hatası backend release'ini bloke etmez, tersi de geçerli (FE-ADR-001) | 🟢 karara bağlandı |
| 4.7 | **Import-boundary lint** | 🟢 **EKLENDI** — ESLint `no-restricted-imports` ile `core/`→`features/` ve `shared/`→`core/`|`features/` yasak; ihlal FE-ADR-003 mesajıyla reddediliyor (kanıtlandı 2026-07-23) | 🟢 karara bağlandı |
| 4.8 | **`data-testid` lint kuralı** | **Eklenmeyecek (bilinçli).** Angular şablonlarında "etkileşimli eleman" tespiti güvenilmez; false-positive üretip `eslint-disable` yorumlarının çoğalmasına ve kuralın değer kaybetmesine yol açar. Zorlama mekanizması: **PR review + E2E testlerin kendisi** (seçici yoksa test yazılamaz) | 🟢 karara bağlandı |
| 4.9 | **Katalog bütünlük testi** | 🟢 **YAZILDI** — `frontend/src/app/core/i18n/i18n.spec.ts`: her anahtarın EN+TR karşılığı dolu + dokümante 22 backend `messageKey` katalogda mevcut; eksikse test kırmızı. 6/6 test geçiyor | 🟢 karara bağlandı |
| 4.10 | **Tarih formatı istisnası** | FE-ADR-012 §e genel ilkesi "kültüre bağlı biçimlendirme yerelleştirilir"; tarih için **sabit `dd.MM.yyyy`** istisnası bilinçli alındı. Geri alınmak istenirse buradan izlenir | 🟢 **karara bağlandı** (istisna kayıtlı) |
| 4.12 | **`src/app/layout/` — dördüncü üst klasör** | Uygulama kabuğu (header + sidenav) FE-ADR-003 §1'in üç katmanının hiçbirine oturmuyor: `core/` değil (servis değil, bileşen), `shared/` **olamaz** (core'dan `AuthService`/`I18nService` enjekte ediyor; import-boundary lint'i bunu reddediyor), `features/` değil (kullanıcıya dönük bir "yetenek" değil, her ekranın çerçevesi). Bu yüzden tek bileşenli ince bir **`layout/`** klasörü eklendi. Değerlendirilen alternatifler: (a) kabuğu `shared/`e presentational yapıp core'u input/output ile geçirmek — iskelet aşamasında gereksiz dumb/smart ayrımı ve yine bir yerde container gerekiyor; (b) `features/shell/` demek — ADR'nin "yetenek" tanımını eğip bükmek. `layout/` yalnız aşağı doğru (`core` + `shared`) bakar; lint kuralları `core/` ve `shared/`i kısıtladığı için ihlal riski yok | 🟢 karara bağlandı |
| 4.14 | **`shared/ui` metin girdileri: çözülmüş string, anahtar DEĞİL** | 🟢 **KARAR (2026-07-25).** Tasarım notu §0 `ariaLabelKey` öngörüyordu ("bileşen anahtarı alır, kendi çözer") — ama `I18nService` `core/i18n`'de ve FE-ADR-003 lint'i `shared/`→`core/` importunu **mekanik olarak reddediyor**; iki bağlayıcı kural çelişti. Çözüm: shared/ui bileşenleri **çözülmüş metin** alır (`[ariaLabel]="'UI-…' | t"`, `[label]="'LBL-…' | t"`); çeviri daima çağıranın işi. Bu, Button metninin zaten `<ng-content>` + `{{ 'KEY' \| t }}` ile gelmesiyle de tutarlı — TÜM metinler bileşene çözülmüş girer, bileşen asla çeviri yapmaz. FE-ADR-012 §b korunuyor (literal metin yine yasak; `check:conventions` literal attribute'u yakalıyor, binding'i geçiriyor). Yan sonuç: Select'in `labelKey`/`label` ikiliği tek `label` alanına indi. Tasarım notu §0 güncellendi | 🟢 karara bağlandı |
| 4.15 | **DatePicker: custom panel (native değil)** | 🟢 **KARAR (2026-07-25, ekip talimatı).** Tasarım notu §7'deki açık soru kapandı: mock'un takvim paneli birebir uygulanır (maskeli `DD.MM.YYYY` yazımı + takvim popover, hafta Pazartesi başlar, panel 280px / gün hücresi 28px / `radius-lg` / `elevation-2`). Ay/gün adları katalogda tutulmaz — `Intl.DateTimeFormat` ile üretilir; `shared` `core`'a bakamadığından aktif dil bileşene **`locale` girdisi** olarak geçirilir (çağıran `i18n.lang()` bağlar). Form değeri daima ISO `yyyy-MM-dd` (FE-ADR-012 §e); dönüşüm bileşenin içinde | 🟢 karara bağlandı |
| 4.16 | **Tema eklemeleri (FE-ADR-011 §f süreci)** | 🟢 **KARAR (2026-07-25).** `shared/ui` için `styles.css` `@theme`'e, tamamı `mock-ui-analysis.md`'de belgeli gerçek değerlerle eklendi: (1) `--shadow-focus-ring` = `0 0 0 3px #FCDABC` (§2.6, orange-200 tabanlı form focus halkası); (2) `--shadow-focus-ring-offset` = `0 0 0 2px #FFF, 0 0 0 4px #F58220` (§2.6, buton focus'u); (3) `--color-secondary-action: #313152` (§2.7 `action-secondary-text`, ink-800 — temada yoktu); (4) `--animate-eds-spin` 900ms linear (§2.14 `--eds-duration-spin`, spinner `data-eds-motion-exempt` ile reduced-motion'dan muaf) ve `--animate-panel-in` 200ms ease-enter (§3.7 `eds-panel-in`). Keyfi değer değil, eksik token'ın temaya taşınması | 🟢 karara bağlandı |
| 4.20 | **Customer Search uygulama kayıtları (2026-07-25)** | ⚪ Ekran yazılırken alınan küçük kararlar: (1) **Durum 11/12 toast yerine bant** — `shared/patterns/toast` henüz yok; hata, sonuç kartı içinde `role="alert"` bandı olarak gösteriliyor (+ Tekrar dene), tablo son iyi haliyle kalıyor. Toast deseni gelince değerlendirilir; (2) **Skeleton'da shimmer animasyonu yok** — EDS `--eds-duration-shimmer` (1800ms) token'ı temaya taşınmadı; statik `bg-sunken` bloklar + `aria-busy`. Gerekirse §4.16 kalıbıyla eklenir; (3) **"Create new customer" disabled** — Create ekranı yokken sahte davranış üretilmedi (FE-ADR-013 §a); wizard gelince `routerLink('new')` olur; (4) **Button/IconButton'a `action` output'u eklendi** — host `(click)`'i hem `check:conventions`'ın testid şartına takılıyor hem `loading` guard'ını atlıyordu; `action` yalnız izinli aktivasyonda ateşlenir; (5) **Select'e `dropUp` girdisi** — mock §6.2 per-page paneli kart içinde kalsın diye yukarı açılıyor; (6) sayfalama ok ikonları mock'ta 18px → IconButton standardı 20px (FE-ADR-011 §f yuvarlaması, §2.18 emsali) | ⚪ kayıt |
| 4.21 | **shared/patterns uygulama kayıtları (2026-07-25)** | ⚪ Customer Search'teki inline desenler `shared/patterns/`a çıkarıldı (FE-ADR-011 §d / mock §7.2): **Table, EmptyState, Skeleton, Pagination** (Search bunları tüketiyor) + **Toast, Modal** (Create/Info/Account için önceden kuruldu, henüz tüketici yok). Sözleşme shared/ui ile birebir: saf sunum, metin çözülmüş girer (§4.14), signal input/output, `testId` zorunlu, yalnız EDS token. Alınan küçük kararlar: (1) **Tema eklemeleri (§4.16 süreci):** `--container-modal-sm/md/lg` = 420/560/680px (mock §7.2 belgeli değerleri) ve `--animate-toast-in` 200ms ease-enter/-8px (mock §4.3) `styles.css`'e taşındı; (2) **Toast konumu** mock'un 76px'i token dışı → `top-16` (64, header) + `mt-3` (12) bileşimiyle kuruldu, keyfi piksel yazılmadı; (3) **Toast kapatma butonu** mock'ta 24×24 → 32×32 IconButton standardına yuvarlandı (§4.20/6 emsali; kopya değil tüketim); (4) **Modal arka plan inert'liği** `aria-modal="true"` + odak tuzağı ile — gerçek `inert` attribute'u bileşen DOM'u dışına uzanmayı gerektirirdi, shared saflığıyla çelişir; Escape daima kapatır, backdrop tıklaması varsayılan olarak kapatMAZ (`closeOnBackdrop` girdisi); kapanma kararı daima çağırandadır (`closed` output, bileşen kendini kapatmaz); (5) **Table** kolon başına `appTableCell` şablonu zorunlu — eksik şablon sessiz boş hücre yerine hata fırlatır (FE-ADR-008 §3 ruhu); satır testid'i çağıranın iş-anahtarlı fonksiyonundan; (6) **Pagination** sayfayı 0-tabanlı emit eder (store/API sözleşmesi), aktif sayfa butonu inert, kesme kuralı artık tek yerde (§2A.4'ün öngördüğü gibi); (7) Skeleton yine shimmer'sız (§4.20/2 kararı değişmedi). Customer Search refactor'unda davranış/testid değişmedi — 14 ekran testi dokunulmadan geçti | ⚪ kayıt |
| 4.22 | **Adres cascade "şehir değişince alt alanların tümü sıfırlanır" yorumu** | 🟢 **KARAR (2026-07-25).** Mock §6.3 cümlesi geniş okunursa street/houseNo/description da silinir — bu, yanlış şehir tıklamasında kullanıcının yazdığı her şeyi kaybettirir. **Dar yorum uygulandı:** şehre gerçekten BAĞIMLI tek alan olan `districtId` sıfırlanır (ve yeni şehrin ilçeleri yüklenir); serbest metin alanları korunur. Analist geniş yorumu isterse tek subscribe bloğu değişir (`address-form-dialog.ts`) | 🟢 karara bağlandı |
| 4.23 | **Customer Info uygulama kayıtları (2026-07-25)** | ⚪ İkinci iş ekranı yazılırken alınan kararlar: (1) **Hesap sekmesi F6'ya rezerve** — mock'un 4 sekmeli düzeni korunuyor: sekme + kart başlığı (`Customer accounts` + disabled `Create new account`) render ediliyor, gövde proje-yazımı `UI-DETAIL-ACCOUNTS-PLACEHOLDER` metni taşıyor; **hiçbir `/api/accounts` çağrısı yok, sahte veri yok** (FE-ADR-013 §a; spec bunu iddia ediyor). F6 hesap tablosunu bu alana yerleştirir; (2) **Ürün "Coming soon" konumu** — mock ürünleri genişleyen hesap satırlarının İÇİNDE gösterir; hesap satırları F6'ya kadar olmadığından A3 bölümü hesap sekmesinde bölüm-düzeyi inert blok olarak duruyor (`customer-detail-products-coming-soon`); F6'da mock'taki yerine (satır-içi) taşınması değerlendirilir; (3) **Adres silme onay metni** — analist kataloğu `MSG-ADDR-DELETE-CONFIRM` kullanıldı; mock'un parametreli *"delete {district}, {city}?"* metni taşınmadı (analist kataloğu kazanır, FE-ADR-012 §c); (4) **Metrik yuvarlamaları** (FE-ADR-011 §f, §2.18 emsali): adres kartı min-height 146→144px (`min-h-36`), kart radio'su 18→16px (`size-4`), tabs underline radius 2px→`rounded-full` (2px yükseklikte görsel eşdeğer); (5) **Cinsiyet görüntüleme** `UI-GENDER-*` ile yerelleşir, tel değeri `"Male"/"Female"` değişmez (§2.7); doğum tarihi ekranda sabit `dd.MM.yyyy` (§4.10); (6) **Silme başarı akışı** — mock'un `eds_flash`'ının karşılığı `CustomerFlashService` (root-provided, read-and-clear): Detail anahtar kuyruklar, Search toast gösterir; (7) **Yeni desenler** — `Tabs` ve `ConfirmDialog` (iki durumlu: soru/backend-hata) `shared/patterns`'e eklendi; `Modal`'a `headerless` girdisi eklendi (ConfirmDialog başlık çubuğu taşımıyor, erişilebilir ad `aria-label`'a taşınıyor); (8) **PUT gövdeleri kontrat-birebir:** demografik güncelleme DÜZ gövde (doc §J), adres PUT'unda `primary` alanı YOK (birincillik yalnız PATCH); no-op backend kontrolleri (adres in-use, aktif ürün) istemcide taklit edilmiyor — 409 gelirse ConfirmDialog'un hata durumunda gösteriliyor | ⚪ kayıt |
| 4.24 | **Hesap bölümü uygulama kayıtları (2026-07-25, F6)** | ⚪ FR-ACCT-01..04 Customer Info'da canlı; alınan kararlar: (1) **Tek yönlü kompozisyon** — account feature `AccountSection`'ı expose eder, customer onu import eder (FE-ADR-003 §Consequences'ın açıkça öngördüğü TEK kardeş-feature importu; ESLint bunu bilinçle mekanik kısıtlamıyor, "review concern"). Ters yön sıfır: adres seçenekleri account'un kendi `BillingAddressOption {addressId, label}` tipiyle **yukarıdan girer**, account customer'ın `AddressResponse`'unu hiç görmez; (2) **KR-11 yapısal:** hesap numarası edit dialog'unda salt METİN (form kontrolü değil); `UpdateAccountRequest` tipi zaten alanı temsil edemiyor — spec PUT gövdesinin anahtar kümesini `['accountName','addressId']` olarak iddia ediyor; (3) **Pasif satır aksiyonları AÇIK bırakıldı** — istemci `MSG-ACCT-NOT-ACTIVE` kontrolünü taklit etmez; PUT/DELETE 409'u dialog'da messageKey ile gösterilir (AC-ACCT-04-03 ile aynı ilke); (4) **Satır genişletme + ürün alt tablosu ertelendi** — mock ürünleri genişleyen satır içinde gösterir ama product-service yok; A3 "Coming soon" bloğu bölüm düzeyinde duruyor (§4.23/2 devamı), product-service gelince satır-içine taşınır; (5) **Mock'un 4/8/12 sayfalaması UYGULANMADI** — backend listesi zarfsız düz dizi döndürür ("no pagination", account-service.md); istemci-tarafı sayfalama küçük listeler için bilinçle eklenmedi, analist isterse patterns/Pagination ile eklenebilir; (6) **Yeni desen `StatusBadge`** (mock §6.4 rozeti; §7.2 envanterinden) + `Table`'a opsiyonel `headerClass` (Actions kolonu sağa hizalı); (7) Başarı metinleri: silme **analist** `MSG-ACCT-DELETED`; oluştur/güncelle proje-yazımı `UI-ACCOUNT-TOAST-CREATED/-UPDATED` (işaretli); durum görüntüleme `UI-ACCOUNT-STATUS-*` ile yerelleşir, tel değeri değişmez; (8) F5'in `UI-DETAIL-ACCOUNTS-PLACEHOLDER` anahtarı kaldırıldı (görevi bitti); (9) **K-8 spec'te:** create POST gövdesi anahtar-küme iddiasıyla pinli (`accountType` gönderimi mümkün değil), 223'e dair hiçbir UI yüzeyi yok | ⚪ kayıt |
| 4.25 | **Create Customer wizard uygulama kayıtları (2026-07-25, F7)** | ⚪ Son kapsam-içi ekran (FR-CUST-03, KR-10). Kararlar: (1) **Atomiklik yapısal:** adresler adım 2'de YEREL taslaktır (`AddressDraft`, yerel `draftId` — tele asla çıkmaz); tek `POST /api/customers` gövdesi spec'te derin-eşitlikle pinli; adres API'sine hiçbir kısmi istek gitmez (spec iddia ediyor); (2) **Paylaşılan adres dialog'una `draft` modu eklendi** — FE-ADR-003 §3'ün "tek ortak bileşen" kararı korunarak: draft modunda kaydetme `draftSaved(AddressRequest)` emit eder, store çağrılmaz (store `optional` inject'e alındı); `AddressFormValue` yapısal tipi hem `AddressResponse`'u hem taslağı karşılar. Dialog + AddressCards `testIdBase` parametresi aldı (FE-ADR-009: testid'ler barındıran ekranın önekiyle — `customer-create-address-*`); Info sekmesi varsayılanlarla birebir aynı kaldı; (3) **Tam-biri-primary istemci değişmezi:** ilk taslak primary olur (kontratın "first becomes primary" davranışıyla uyumlu), radio bunu tek taslağa taşır; kart bileşeninin primary-silme kilidi taslaklar için de geçerli; (4) **Mock'un edit modu (`eds_edit_customer`) TAŞINMADI** — düzenleme Customer Info'nun dialoglarında yaşıyor (F5); wizard yalnız create. Mock'un hardcoded mükerrer-ID listesi de taşınmadı (gerçek cevap: 409 `MSG-CUST-DUP-NATID` + MERNIS zinciri); (5) **Hata → adım atlama:** 400 alan hataları `scope`'a göre sahibi olan adıma götürür (`demographic`→1, `addresses`→2, `contactMedium`→3); 409 dup-natid ve `MSG-CUST-NATID-VERIFICATION-FAILED` hem banner'da hem `nationalityId` alanında gösterilir (adım 1'e döner); 503'te adımda kalınır, veri korunur, Create yeniden denenebilir; (6) **Create akışında hesap alanı yok** — mock §6.3 alan listeleri doğrulandı, hesapla ilgili alan zaten yok; çelişki kaydı gerekmedi; (7) **Yeni desen `Stepper`** (§7.2 envanteri; pasif gösterge — gezinme footer'da, mock'la uyumlu); denetçi-dostu `stateOf()` yardımıcısı ile şablonda karşılaştırma operatörü yok; (8) Search'ün "Create new customer" butonu **aktifleşti** (§4.20/3'ün öngördüğü an); başarı akışı `CustomerFlashService` → Customer Info toast'ı (`UI-CREATE-TOAST-SUCCESS`, proje-yazımı) | ⚪ kayıt |
| 4.26 | **Hata düzeltme turu uygulama kayıtları (2026-07-29)** | ⚪ Altı bulgu; alınan kararlar: (1) **Browse ≠ arama sonucu (A1)** — Customer Search'ün sonuç kartı başlığı artık moda göre değişiyor: kriter uygulanmamışken **`UI-SEARCH-BROWSE-HEADING`** ("All customers" / "Tüm müşteriler", **proje-yazımı**, §2A.3 kalıbı), filtre uygulanınca `UI-SEARCH-RESULTS-HEADING`. Ayrım zaten var olan `hasAppliedCriteria()`'dan geliyor — boş-durum metinlerinin (browse vs search) kullandığı ayrımın aynısı, yeni kavram icat edilmedi (ADR-005 browse/filter). Analist metin verirse yalnız katalog değeri değişir; (2) **Adres kart yüksekliği (A2)** — `min-h-36` yalnız TABANI garanti ediyordu, `items-start` de grid'in satırları eşitlemesini engelliyordu; özet satırı saran bir kart 144px'i aşınca "Add new address" geride kalıyordu. Çözüm `auto-rows-fr` + varsayılan stretch: tüm döşemeler en uzunu kadar. Piksel uydurulmadı (FE-ADR-011 §f), mock §6.3'ün "aynı min-height" niyeti korundu; (3) **Liste iç scroll'u (A3)** — `Table` deseni bilinçle scroll'suz ("Scrolling stays with the caller", §4.21/5) ve ekran zaten `overflow-auto` sarmalayıcıya sahipti; kopan halka **component host'uydu**: `app-customer-search` elemanı shell'in `<main>` flex'inin çocuğu ama kendisi flex zincirini taşımıyordu, dolayısıyla şablondaki `flex-1 / min-h-0` zincirinin sınırlı bir atası yoktu ve kart satır sayısıyla büyüyordu. `host: { class: 'flex min-h-0 flex-1 flex-col' }` eklendi. **Desende değil ekranda çözüldü** — ortak desene yükseklik yönetimi koymak her tüketiciye sınırlı-yükseklik varsayımı dayatırdı (dialog içindeki tablolar bunu istemez). ⚠ Aynı host boşluğu Detail/Create'te de var ama oradaki içerik sayfa boyuna bağlı büyümüyor; kapsam dışı bırakıldı, gerekirse aynı tek satırla çözülür; (4) **numeric kısıt `shared/ui`'de (B1/B2)** — `inputMode="numeric"` yalnız klavye İPUCU; fiziksel klavye, yapıştırma ve otomatik doldurma onu yok sayıyor. `TextInput`'a **`digitsOnly`** girdisi eklendi: rakam olmayanlar hem modelden hem DOM'dan giriş anında siliniyor (DOM'a geri yazma şart — `[value]` bağı değişmeyen sinyalde boyamıyor). Ekran seviyesinde regex/valibrasyon **yazılmadı**: kısıt tek yerde, bileşenin kendi girdi hijyeni olarak yaşıyor; VALIDITY hâlâ çağıranın ve nihayetinde backend'in (FE-ADR-007 §3). Tüketiciler: Search'ün ID number/Customer ID/GSM, Create'in Nationality ID (+ mevcut `maxLength=11`, VR-NATID). ⚠ Create'in telefon alanları (mobile/home/fax) mock §6.3'e göre de "sadece rakam" ama **bu turda kapsam dışı** — istenirse aynı girdiyle tek satırda açılır; (5) **Mükerrer NAT ID çift kontrolü (C1)** — §4.27; (6) **Adres in-use kilidi (C2)** — §5.9; hesap Description alanı (D1) — §1A.7 | ⚪ kayıt |
| 4.27 | **Mükerrer NAT ID: erken (UX) + son (otorite) çift kontrolü** | 🟢 **KARAR (2026-07-29, C1).** Talep: mükerrer NAT ID wizard'ın sonunda değil, demografik adımın `LBL-NEXT`'inde bildirilsin. **Kontrat doğrulaması:** ayrı bir "exists" endpoint'i **yok** ve icat edilmedi; `GET /api/customers?nationalityId={değer}` kullanılıyor — `nationalityId` bu endpoint'te **tam eşleşme** yapıyor (customer-service.md §List) ve `GET /api/customers` zaten tek kanonik liste/filtre endpoint'i (ADR-005). Dolu sayfa ⇒ aktif bir müşteri bu ID'yi tutuyor ⇒ alan altında `MSG-CUST-DUP-NATID`, adım ilerlemez. **Neden çift kontrol yapısal bir zorunluluk:** liste endpoint'i **yalnız aktif** müşterileri görür, oysa NAT ID tekilliği kalıcı ve globaldir — **soft-deleted bir sahip de yaratmayı bloke eder (ADR-003)**. Böyle bir kayıt erken probe'tan geçer ve yalnız POST'un 409'u yakalar; bu yüzden create sonundaki 409 yolu **kaldırılmadı**, backend nihai otorite kaldı (FE-ADR-007 §3). **Probe başarısız olursa (5xx/kesinti) kullanıcı ENGELLENMEZ** — bozulan bir kolaylık kontrolü, backend'in doğru tamamlayabileceği bir akışı durdurmamalı; ilerlenir, karar POST'a bırakılır. Uygulama: `CustomerApiService.activeCustomerExistsWithNationalityId()` (probe niyeti isimde ve JSDoc'ta açık), `Next` süresince buton `loading`. **GÜNCELLEME (aynı gün):** ilk uygulama liste endpoint'ini kullanıyordu ve soft-deleted sahibi göremiyordu; bu boşluk **backend'e ince bir availability ucu eklenerek kapatıldı** (§5.10, ADR-005 §Addendum). Probe artık `CustomerApiService.nationalityIdIsTaken()` ile o ucu çağırıyor ve **soft-deleted dahil** her sahibi adım 1'de yakalıyor. Liste-tabanlı probe ve ona eşlik eden oturum-içi `rejectedNatIds` geçici çözümü **kaldırıldı**. Çift kontrol gerekçesi **değişmedi**, yalnız sebebi daraldı: probe artık kör değil ama hâlâ **anlık** — probe ile POST arasında başkası ID'yi alabilir, o yüzden 409 yolu otorite olarak duruyor | 🟢 karara bağlandı |
| 4.17 | **shared/ui uygulama kayıtları (2026-07-25)** | ⚪ Üç küçük kayıt: (1) **ESLint FE-ADR-003 deseni false-positive'di** — `**/core/**` glob'u npm paketi `@angular/core/testing`'i de yakalıyordu; ilk `shared/` dosyasıyla ortaya çıktı, desene `!@angular/core`/`!@angular/core/**` negasyonları eklendi (kuralın amacı değişmedi). (2) **CVA bileşenlerinde `disabled` girdisi yok** — `FormControlDirective`'in kendi `disabled` input'u ile çakışıyor; tek kaynak `control.disable()` (tasarım notu §8). (3) Select'te `searchable`/`clearable`/`loading`, FormField'da `hint` tooltip **bilinçle ertelendi** — kapsam içi tüketici yok (FE-ADR-011 §c) | ⚪ kayıt |
| 4.18 | **Veri katmanı: dokümante edilmemiş response şekilleri (2026-07-25)** | 🟡 **Üç kaynaklı doğrulama zinciriyle 4 açıktan 3'ü KAPANDI** (① docs/api/*.md; ② Postman test assertion'ları; ③ **çalışan stack'in Swagger şemaları — ekip üyesi sağladı, 2026-07-25**): (1) **AddressResponse** ✅ — Swagger: `{addressId, cityId, cityName, districtId, districtName, street, houseFlatNumber, addressDescription, primary}` — yanıt **hem id hem çözülmüş adları** taşıyor (adres kartı için ek lookup çağrısı gerekmez); PATCH primary de aynı şekli döndürüyor (yalnız terfi eden adres — düşürülen eski primary dönmez, liste yenilenir). ⚠ Swagger *request* şemasındaki `primaryRequested` bir serializer artefaktı — dokümante tel adı `primary`, istemci yalnız onu gönderir (test iddia ediyor); (2) **ContactMediumResponse** ✅ — Swagger: tam olarak `{email, homePhone, mobilePhone, fax}`; (3) **City/District** ✅ — Swagger: **`{cityId, name}` / `{districtId, name}`** — ilk varsayım (`id`) YANLIŞTI, hiçbir ekran tüketmeden `core/lookup/lookup.model.ts`'te düzeltildi; (4) **`Page<T>` zarfı** — ✅ **KAPANDI (2026-07-25, Swagger customer-controller):** düz `PageImpl` — metadata üst seviyede, `first/last` dahil (detay §4.4). **Veri katmanında doğrulanmamış alan kalmadı** | 🟢 **4/4 kapandı** |
| 4.19 | **account request/response alan asimetrisi** | ⚪ **Kayıt (dokümanda net, tahmin değil):** account-service create/update **request**'i `addressId` gönderir; **response** aynı alanı `billingAddressId` olarak döndürür (account-service.md §Representation vs §Semantics). İki alan `account.model.ts`'te doğru şekilde ayrı tiplendi; karıştırılmamalı | ⚪ kayıt |
| 4.13 | **Parametreli i18n** | 🟢 **ONAYLANDI (2026-07-24)** — `translate(key, params?)` + **isimli** yer tutucular (`{from}`, `{total}`; pozisyonel yasak — TR/EN kelime sırası farkı); ICU/plural motoru bilinçli reddedildi (bağımlılık bütçesi, FE-ADR-012 §a); eksik parametre → yer tutucu olduğu gibi kalır + `console.warn` (FE-ADR-008 §3 felsefesi); katalog şekli değişmez; `i18n.spec.ts`'e EN↔TR yer tutucu kümesi eşitliği iddiası eklenir. Detay: **FE-ADR-012 §h**. Uygulama ekran fazının ilk adımı — henüz yazılmadı | 🟢 karara bağlandı — **henüz uygulanmadı** |
| 4.11 | **`validationErrors` alan yolu tekdüze değil → frontend'de çözüldü** | Backend handler'ları farklı biçim üretiyor (kaynaktan doğrulandı 2026-07-24): body `@Valid` → **tam yol** (`demographic.firstName`, `addresses[0].cityId`, `contactMedium.email`); `@RequestParam` (`handleConstraintViolation:72`) → **yalnız son segment** (`firstName`); tip uyuşmazlığı → param adı. Ayrıca `demographic`/`addresses`/`contactMedium` **çıplak** gelebiliyor ve **hiçbir form kontrolüne karşılık gelmiyor** — naif "yaprak ada göre eşleştir" yaklaşımı bunları sessizce yutar (kullanıcı 400 alır, ekranda hiçbir şey görmez). **Backend'e dokunulmadan** çözüldü: `core/http` her yolu bir kez ayrıştırıp `leaf`/`scope`/`index`/`structural` üretiyor; `matchFieldErrors()` eşleşenlerin YANINDA **`unmatched`** döndürüyor, böylece sessiz yutma API düzeyinde imkânsız; `fieldErrorsAt(scope,index)` wizard'da doğru adres satırını hedefliyor. Ham İngilizce değerli alanlardan **anlamı tek olanlar** (`cityId`/`districtId`/`gender`/`addresses`/`demographic`/`contactMedium`) frontend anahtarına eşlendi; serbest metin alanları (`street`, `houseFlatNumber`, `addressDescription`) bilinçle generic bırakıldı — zarf hangi kısıtın patladığını söylemediği için "zorunludur" demek yanlış olabilirdi (`@NotBlank` + `@Size` birlikte). Bu bilinçli tercihler `INTENTIONALLY_GENERIC` listesinde **açıkça** duruyor; listede olmayan bir alan generic'e düşerse `console.warn` üretiliyor (FE-ADR-008 §3 "gap geliştiriciye görünür, kullanıcıya görünmez") — böylece backend'in ekleyeceği yeni bir alan sessizce generic'e düşmüyor. 22/22 test | 🟢 **karara bağlandı** |
| 4.28 | **Ürün bölümü uygulama kayıtları (2026-07-31)** | ⚪ FR-PROD-01..02 Customer Info'da canlı; alınan kararlar: (1) **Konum: genişleyen hesap satırının İÇİ** — §4.23/2 ve §4.24/4'ün ertelediği karar vadesi geldi. Mock §6.4 ürünleri hesap satırı genişleyince gösteriyor ve AC-PROD-01-01 zaten *"expandable per-account sub-table"* diyor; liste ucu da `accountNumber`'a bağlı, dolayısıyla bölüm-düzeyi bir blok hangi hesabı listeleyeceğini bilemezdi. Eski "Coming soon" bloğu (`customer-detail-products-coming-soon`) **kaldırıldı**; (2) **`shared/patterns/Table`'a opsiyonel satır genişletme** — yeni `TableExpansionDef` (`ng-template[appTableExpansion]`) + `isExpanded` predicate'i; **kesinlikle eklemeli**: şablon projelenmemişse desen eskisiyle birebir aynı markup'ı üretiyor (Customer Search dokunulmadı). Genişletme kontrolü ve açık satır kümesi **çağıranda** kaldı — desen yine hiçbir şeye karar vermiyor; (3) **İki kardeş feature birbirini GÖRMÜYOR** — `AccountSection` ürün markup'ı taşımıyor, `ProductSection` hesap tipi tanımıyor: account bir `rowExpansion` **TemplateRef** girdisi alıyor, context'i **yalnız `accountNumber` string'i**. `accountNumber` ürün feature'ının kendi kontrat parametresidir (`GET /api/products?accountNumber=`), ödünç alınmış bir hesap tipi değil. Kompozisyon **tek yerde**, Customer Info'da — FE-ADR-003 §Consequences'ın izin verdiği yön; (4) **FR-PROD-02 modal, ayrı ekran değil** — üç dayanak: AC-PROD-02-01 `traceability-matrix.md`'de *"detail modal"* olarak kayıtlı, `docs/api/product-service.md` ucu *"Product detail modal"* diye tanımlıyor, ve mock'ta tetikleyici satır-içi bir `eye` aksiyonu (Customer Info'daki her satır-düzeyi etkileşim modal). Rota icat etmek FR/AC/mock'ta karşılığı olmayan bir gezinme hikâyesi gerektirirdi. ⚠ **Mock'un ürün markup'ı geri getirilemez durumda:** `docs/source/mock-ui/…Full_App.html` bundle'ında "Campaign", "View product", "Product ID" gibi hiçbir ürün metni **yok** (2026-07-31 doğrulandı; ayrı `.dc.html` dosyaları repoda değil). Modal düzeni bu yüzden Customer Info'nun **dokümante okuma grid'ini** (mock §6.4 sekme 1) izliyor — uydurulmadı, kayıt altında; `mock-ui-analysis.md` §6.4'e eklendi; (5) **`ProductDetailStore` ayrı** — liste ve detay bağımsız okumalar; başarısız bir detay okuması ekrandaki listeyi bozmamalı. 404 `MSG-PROD-NOT-FOUND` `notFound` sinyaliyle ayrı durum olarak gösteriliyor (ayrım backend'in `messageKey`'inden, tahminden değil); (6) **`UI-DETAIL-PRODUCTS-HEADING` kaldırıldı** (görevi bitti — §4.24/8 emsali); canlı bölüm kendi feature önekiyle `UI-PRODUCT-HEADING` kullanıyor. `UI-COMMON-COMING-SOON` **korundu**: A3 kuralı bağlayıcı kalmaya devam ediyor; (7) **Fiyat gösterimi yok** — bu turda hiçbir ekranda fiyat görünmüyor (§1B.9/§1B.10) | ⚪ kayıt |
| 4.29 | **Sayfa boyutu tek kaynağa taşındı (`size` 400'ü)** | 🟢 **KARAR (2026-08-02).** **Belirti:** müşteri listesi ekranı canlıda hiç çalışmıyordu; `GET /api/customers?page=0&size=20` → **400 `MSG-VALIDATION-ERROR`**, `validationErrors.size = "must be one of 15, 30, 50"`. **Teşhis — kaynak kodda hata YOKTU:** `PAGE_SIZE_OPTIONS` zaten `[15,30,50]` ve `DEFAULT_PAGE_SIZE = 15` idi (29.07.2026, §2.3/§2.4 revizyonu; commit `dc7bcf5` / PR #19). Hatayı üreten şey **bayat bir derleme çıktısıydı**: çalışan `crm-lite/frontend:local` imajı **8 gün önce (25.07)**, yani revizyondan ÖNCE derlenmişti. Kanıt: container içindeki `/usr/share/nginx/html/chunk-D-TsK3Ti.js` `20,50,100` içeriyor, `15,30,50` **hiç geçmiyor**; host'taki `frontend/dist/` (25.07, git-ignored) aynı eski değerleri taşıyor. **Çözüm = imajı yeniden derle** (`podman compose -p crm-lite up -d --build frontend`); kod değişikliği gerekmiyordu. ⚠️ **Genel ders:** frontend davranışı doğrulanırken imaj yaşı kontrol edilmeli — kaynak yeşilken canlı kırmızı olabilir. **Yapılan asıl iş (nüksü önleme):** whitelist tek kaynağa taşındı — `features/customer/model/` içindeki tanım `shared/patterns/pagination/page-size.ts`'e alındı (`PAGE_SIZE_OPTIONS`, `PageSize`, `DEFAULT_PAGE_SIZE`, yeni `isPageSize()` tip-koruyucusu), `shared/patterns` barrel'ından yayınlandı; customer modeli artık yalnızca **yeniden ihraç** ediyor (tek tanım korunuyor). **Neden shared:** kardeş feature `features/customer/model`'i **import edemez** (FE-ADR-003) — satış akışı (§2.7) gibi yeni bir sayfalı liste, whitelist'i mecburen yeniden yazacaktı; eski 20/50/100 listesinin hayatta kalma biçimi tam olarak buydu. **Tipe gömülen kısıt:** `Pagination` deseninin `size`/`sizeOptions` girdileri ve `sizeChange` çıktısı artık `number` değil **`PageSize`** — whitelist dışı bir değeri hard-code eden ekran **derlenmiyor**, 400'ü çalışma zamanında almıyor; desen kendi CVA değerini `isPageSize` ile daraltıyor, böylece kontrol tek yerde (Customer Search'ün elle `find()` daraltması kaldırıldı). **Diğer listeler denetlendi:** hesap listesi (`GET /api/accounts?customerId=`) ve ürün listesi (`GET /api/products?accountNumber=`) **düz dizi** döndürüyor, `page`/`size` **hiç göndermiyor** (§1B.2) — ikisi de bu hatadan etkilenmiyordu, dokunulmadı. **Testler:** desen spec'inde whitelist iddiaları (yalnız 15/30/50 sunuluyor; 20 ve 100 yok; her emit `isPageSize`'ı geçiyor) + `CustomerApiService` spec'inde **tele çıkan** `size` değerinin whitelist'te olduğu iddiası (sabit doğru olup query string'e ulaşmaması da mümkün olduğu için asıl güvence bu) | 🟢 karara bağlandı |

| 4.30 | **BUG-1 + BUG-2: Create/Edit billing account modalı** | 🟢 **KARAR + UYGULAMA (2026-08-03).**<br>⚠️ **Önce bir kaynak düzeltmesi:** görev `docs/source/mock-ui/billing-account-modal.html` diye ayrı bir dosyaya atıf yaptı — **böyle bir dosya repoda yok ve hiç olmadı** (git geçmişinde de yok; `docs/source/mock-ui/` altında tek dosya `Guncel_Etiya_CRM_Lite_Full_App.html`). Markup **uydurulmadı**: bundle'ın `window.__resourceBlobs`'u gzip+base64 çözülüp Customer Info kaynağı (`4bc1814b…`) okundu ve **satır 288–345 gerçek modal markup'ı** doğrulandı. Aşağıdaki her değer oradan.<br><br>**BUG-1 — "Add new address" yoktu.** Mock (satır 303–307): Billing address FormField'ının **hemen altında**, yalnız panel kapalıyken, `Button variant="ghost" icon-leading="plus" size="sm"` → "Add new address". Tıklanınca (satır 308–337) **ayrı popup AÇILMAZ**; modalın içinde panel genişler: `border 1px`, `radius-lg`, `bg-page`, `padding 16`, `gap 16`; başlık "New address" (14px semibold) + sağda `IconButton icon="x"` (`aria-label` "Cancel new address"); alanlar 2 kolon City\|District (cascade: şehir seçilmeden district `disabled`), 2 kolon Street\|House no, sonra Description; sağ altta `Button variant="secondary" icon-leading="check" size="sm"` "Save address". **🔴 Bilinçli mock↔bug-raporu farkı:** hata raporu *"address creation popup açılmalı"* diyor, mock **inline panel** kullanıyor. **Mock tasarımda bağlayıcıdır (FE-ADR-011)** → inline panel uygulandı. Ek gerekçe: modal üstüne modal, iç içe odak tuzağı problemi doğururdu ve mock'un istemediği bir şey. <br>**Mock'tan devralınan iki davranış:** (a) panel açıkken modalın **Save'i disabled** (mock `accSaveDisabled` = `… \|\| accAddrOpen`) — yarım yazılmış adres, hesabı kaydederek sessizce kaybolmasın; (b) kaydedilen adres **otomatik olarak Billing address seçimi olur** (mock `accForm.address = son index`). <br>**Mimari (FE-ADR-003'e sadık):** adres oluşturma **FR-ADDR-02'dir ve customer-service'e aittir**; `features/account/` kardeş feature'ı import edemez. Bu yüzden panel yalnız **toplar**: `BillingAddressPanel` → `AccountFormDialog` (`newAddressRequested`) → `AccountSection` (pass-through) → **Customer Info yazar** ve bunu **mevcut `CustomerDetailStore.addAddress()` ile** yapar — yani Address sekmesinin ve §2.7 sihirbazının kullandığı **aynı tek yol**; ikinci bir adres-oluşturma servisi/uç sarmalayıcısı **yazılmadı**. Sonuç geri akışı üç girdi: `addressBusy` / `addressErrorText` / `createdAddressId`; **seçim yalnız `createdAddressId` gelince** yapılır (başarısız create hiçbir şeyi seçmez). Taşınan tip `NewBillingAddress` — `BillingAddressOption` ile birebir aynı gerekçe. Cascade `core/lookup`'tan (ADR-002; hiçbir şehir/ilçe hard-code edilmedi).<br>**Sapma (kayıt):** mock'un `IconButton size="28"`'i uygulanmadı — `shared/ui/IconButton` tek ölçülüdür (32) ve keyfi piksel yazmak FE-ADR-011 §f ihlali olurdu; DS bileşeni olduğu gibi kullanıldı.<br><br>**BUG-2 — modal dar, dropdown'da tüm adresler görünmüyordu.** **Kök neden GENİŞLİK DEĞİL.** Bizim modalımız `size="md"` = **560px**, yani mock'un **480px**'inden zaten *daha geniş*ti. Asıl sebep `shared/patterns/Modal`'ın gövde `div`'iydi: `overflow-y-auto`. Bu bir **scroll container** yaratır, scroll container da içindeki **mutlak konumlu** `Select` panelini (`absolute … top-full`) **kırpar** — panel modalın dışına taşamadığı için adres listesi modal gövdesinin iç scroll'una hapsoluyordu. Mock tam olarak bunu engelliyor: 480'lik iki modal (**billing account** + **start new sale**) `overflow:visible` ve **`max-height` taşımıyor**; buna karşılık uzun form modalları (680 müşteri bilgisi, 560 iletişim, 520 ürün) `max-height:calc(100vh - 48px)` + `overflow:hidden`/gövde `auto` kullanıyor. <br>**Karar — desende çözüldü, ekrana yama yazılmadı:** `Modal`'a `overflowVisible` girdisi eklendi (mevcut `size`/`headerless`/`closeOnBackdrop` ile aynı biçimde). Set edildiğinde gövde `overflow-visible` olur **ve panel `max-h-full`'ü bırakır** (scroll'suz gövdede yükseklik tavanı, içeriği erişilemez biçimde dışarıda bırakırdı). **Neden global bir flip değil:** mock'un kendisi bunu modal-başına bir özellik olarak ele alıyor; hepsini `visible` yapmak uzun dialogların kısa ekranlardaki scroll'unu **götürürdü**. Uzun form dialogları bunu kullanıyor: **adres dialogu** (2 Select) ve **demografik dialog** (Select + DatePicker). `ConfirmDialog` ve ürün detayı (dropdown'ı yok, scroll'u değerli) varsayılanda kaldı.

**§4.30 EK — 04.08.2026 (BUG-2 ikinci tur; ilk turda eksik kalanlar).** İlk turda düzeltme desende doğru kuruldu ama **asıl hedef dialoga uygulanmadı** — `account-form-dialog.html` `overflowVisible` almamıştı, yani hata raporundaki modal **kırpılmaya devam ediyordu** (uygulama hatası, teşhis değil). İkinci turda iki şey daha çıktı ve üçü birden çözüldü:<br>**(a) Alanlar alt alta olmalı.** Bizim şablonumuz `grid grid-cols-2 gap-x-6 gap-y-5` ile Account name \| Billing address'i **yan yana** koyuyordu. Mock gövdesi ise `display:flex; flex-direction:column; gap:16px` — iki alan da **tam genişlik ve alt alta** (bundle satır 296–302). Yani yan yana düzen mock'tan değil, bizdendi; `flex flex-col gap-4` yapıldı. Kullanıcının ilettiği ekran görüntüsü de alt alta düzeni gösteriyor (⚠ **görsel bu oturuma ulaşmadı** — karar mock markup'ının kendisine dayandırıldı, ki aynı şeyi söylüyor).<br>**(b) "Modal dropdown'u tümüyle gösterecek biçimde uzamalı."** `overflowVisible` kırpmayı bitirir ama paneli **mutlak konumlu** bırakır: liste dialogun kenarından taşarak footer'ın üstüne biner, dialog **uzamaz**. Talep edilen davranış bu değil. Bu yüzden `shared/ui/Select`'e **`flowPanel`** girdisi eklendi: panel mutlak yerine **akış içinde** (normal blok) render edilir → `FormField` büyür → **dialog büyür**, liste hiçbir kenarı aşmaz. `max-h-65` tavanı **korundu**, böylece büyüme sınırlı: çok uzun listede panel kendi içinde kayar, footer ekran dışına itilmez. `dropUp` akış modunda anlamsız olduğu için yok sayılıyor. Varsayılan **kapalı** — tam sayfada panel sayfanın düzenini itmemeli (mock'un `Select.jsx` davranışı ve diğer tüm tüketiciler böyle).<br>**Sonuç: hesap dialogu `overflowVisible` KULLANMIYOR** — `flowPanel` sayesinde kırpacak bir taşma kalmadığı için gövdenin `overflow-y-auto`'su ve panelin `max-h-full`'ü **emniyet olarak duruyor** (çok kısa ekranda dialog yine de kaydırılabilir). Billing address Select'i ve inline "New address" panelindeki City/District Select'lerinin **üçü de** `flowPanel` kullanıyor, böylece dialog içinde tek ve tutarlı bir açılma davranışı var.<br>**Adres sayısı:** hiçbir modda sınırlanmıyor — `select.spec` 12 seçenekle iki modu da, `account-section.spec` 9 adresle dialogu doğruluyor.<br>**Testler:** `select.spec` +1 (varsayılan `absolute top-full`; `flowPanel`'de ikisi de yok; her iki modda 12/12 seçenek), `account-section.spec` +1 (form `flex-col` ve `grid-cols-2` **değil**; panel `absolute` değil; 9/9 adres).<br>⚠️ **Doğrulama notu (§4.29 dersinin tekrarı):** çalışan `crm-lite/frontend:local` imajı **02.08 tarihli**; bu turun düzeltmeleri ancak `podman compose -p crm-lite up -d --build frontend` sonrası ekranda görünür.<br>**Genişlik:** mock'un 480'i eklendi. Ölçek **yeniden harflendi** (sm 420 / **md 480** / lg 560 / xl 680) ve mevcut tüm tüketiciler yeniden etiketlendi (adres·iletişim·ürün `md`→`lg`, demografik `lg`→`xl`), böylece **hesap dialogu dışında hiçbir dialogun render genişliği değişmedi** — 560→**480** tek kasıtlı değişiklik. `mock-ui-analysis §7.2` tablosu da düzeltildi (480 ve 520 eksikti).<br>**Select'te adres sayısı sınırı:** kod hiç sınırlamıyordu, sınırlamıyor — `@for (option of options())` tüm seçenekleri basar; `max-h-65` yalnız panelin kendi iç scroll'u (mock'un `Select.jsx` davranışı), kırpma değil.<br><br>**Ek DS değişikliği:** `Button`'a `ghost` varyantı yazıldı (mock'un `.eds-btn[data-variant="ghost"]` kuralı birebir). `shared-ui-design-notes` §2 "mock'un hiçbir ekranında kullanılmıyor" diyordu — **bu tespit yanlıştı**, bundle'da 14 kez geçiyor; not düzeltildi.<br>**Testler:** `modal.spec` (varsayılan gövde `overflow-y-auto` + `max-h-full`; `overflowVisible`'da `overflow-visible` ve `max-h-full` YOK; 4 boyut token'ı), `account-section.spec` **+5** (tetikleyici↔panel değişimi ve **tek** `role="dialog"`; panel açıkken Save disabled + district cascade disabled; trim'lenmiş draft **yukarı** emit edilir ve **hiçbir** adres API'si çağrılmaz; `createdAddressId` gelince panel kapanır + otomatik seçilir + hesap POST'u o `addressId` ile gider; create hatasında panel açık kalır), `customer-detail.spec` **+1** (uçtan uca: gerçek `POST /api/customers/1001/addresses` gövdesi 5 alan, `primary` yok → liste tazelenir → Select'te seçili) | 🟢 karara bağlandı |

**§4.31 — Karakteristik alan doğrulaması Product Configuration'a alındı (04.08.2026).** 🟢 **KARAR + UYGULAMA.**<br>**Bildirilen üç kusur:** (1) AC-SALE-01-12 — zorunlu karakteristik boş bırakılarak `LBL-NEXT` ile adım **atlanabiliyordu**; şikâyet ancak Submit ekranında (sunucu 400'ü ile) görünüyordu; (2) AC-SALE-01-17 — **hız alanına harf girilebiliyordu**; (3) AC-SALE-01-18/19 — `MSG-VAL-CHAR-REQUIRED` / `MSG-VAL-CHAR-FORMAT` **alanın altında** hiç gösterilmiyordu.<br>**Kök neden bir hata değil, bilinçli bir karardı ve o karar yanlıştı:** adım 2 kasten **hiç istemci doğrulayıcısı taşımıyordu** (`product-configuration-step.ts`'in eski yorumu: *"Client-side validators are deliberately absent — the server validates against `dataType`/`mandatory`"*) ve her karakteristik **düz metin girdisi** olarak basılıyordu. Gerekçe FE-ADR-007 §3 idi: `data_type`'ı yalnız product-service bilir, dolayısıyla kararı o verir. **Bu hâlâ doğru — ama AC'lerin sorduğu şey bu değil.** AC-SALE-01-12/18/19 "kim karar verir"i değil, **nerede ve ne zaman söylenir**i tarif ediyor: `LBL-NEXT` anında, **ilgili alanın altında**, Submit ekranına geçmeden. Yalnız `POST /api/orders`'tan gelen bir ret, kullanıcının artık göremediği bir alan için **iki ekran sonra** iner.<br>**Uygulanan — kural aynası, kopya değil:** `core/catalog/characteristic-validation.ts` product-service'in `CharacteristicValidationRules`'ının **birebir aynası**dır (TEXT hep geçerli; NUMBER = `new BigDecimal()` ayrıştırması; BOOLEAN = **katı** `true`/`false` — `1`/`0`/`yes` **değil**; DATE = katı ISO `LocalDate.parse`; **tanınmayan tip reddedilir**). Blank ölçütü backend'in `isBlank()`'i (yalnız boşluk = boş), karşılaştırma **trim'li**. **İkilik bilinçli ve kayıt altında:** ikisi ayrışırsa **backend haklıdır ve düzeltilecek dosya budur**; sunucu doğrulaması kaldırılmadı, 400'leri adım 3'te hâlâ render ediliyor. `core/`'da duruyor çünkü kural **kontratın kendisine** ait (`features/` içinden erişilebilir olması da FE-ADR-003 gereği).<br>**Tipe göre kontrol (AC-SALE-01-17):** NUMBER → `TextInput` + yeni **`numericOnly`** (rakam, tek başta `+/-`, ilk `.`; gerisi model'e **de** DOM'a **da** girmez — bildirilen "hıza harf" kusuru yapısal olarak kapandı); BOOLEAN → `true`/`false` **`Select`**'i (format hatası **erişilemez** hâle gelir); DATE → `DatePicker` (maskeli `gg.aa.yyyy` giriş, ISO `yyyy-MM-dd` form değeri, yarım yazılmış metin blur'da `invalidDate`); TEXT → `TextInput`. **`numericOnly` neden `digitsOnly` değil:** NUMBER kontratta BigDecimal'dir, `12.5` meşrudur; `digitsOnly` bunu keserdi. **Kabul edilen boşluk:** bilimsel gösterim (`1e5`) süzülüyor — `BigDecimal` kabul etse de "Download Speed" alanına üs yazmak gerçek bir kullanıcı yolu değil, `e`'ye izin vermek ise kapatılan kapıyı yeniden açardı.<br>**`LBL-NEXT` artık disabled DEĞİL:** adım 2'de buton **tıklanabilir**, doğrulamayı **tıklama** yapar (`SaleWizard.goNext()` → `ProductConfigurationStep.validate()`, `viewChild` ile). Gerekçe AC'nin kendi cümlesi: *"tıklandığında … gösterilmeli"* — **disabled bir buton kendini açıklayamaz**. Aynı gerekçeyle **servis adresi** kuralı da (AC-SALE-01-11) disabled olmaktan çıkıp tıklama-anı kontrolüne taşındı. Adım 1'in `AC-SALE-01-07` sepet-boş disabled'ı **korundu** (orada söylenecek bir kural yok, sepet görünür biçimde boş). Başarısız `LBL-NEXT` ilk kusurlu alanı **görünür alana kaydırıp odaklıyor** + üstte `UI-SALE-CONFIG-INVALID` şeridi çıkıyor; yazılmış hiçbir değer kaybolmuyor (AC-SALE-01-14).<br>**Zoneless notu:** Reactive Forms sinyal değil; `touched` blur'da **hiçbir observable'a** yaymadan değişir. Bu yüzden hata computed'ları bir `formRevision` sinyaliyle besleniyor (valueChanges + statusChanges + alan grid'inin `focusout`'u + `validate()`'in `markAllAsTouched`'ı).<br>**Testler (+18):** `characteristic-validation.spec` **yeni** (13 — tip başına sınır değerler, `0099-01-01` yıl tuzağı dâhil), `sale-wizard.spec` **+6** ve fikstürü **dört veri tipine** genişletildi (eski fikstür tek TEXT alanıydı, 17/18/19'u ifade **edemiyordu**); teklif 103 **hiç karakteristiği olmayan** ürün olarak AC-SALE-01-21'i taşıyor. ⚠️ **Eski 7 test kırıldı ve bu doğruydu** — zorunlu alan boşken `LBL-NEXT`'in geçmesini, yani düzeltilen kusuru sabitliyorlardı. `customer-detail.spec` **+3**: satış giriş noktası (AC-SALE-01-01 doğru `accountNumber` query param'ı ile yönlendirme, AC-SALE-02-01 Passive satırda disabled, AC-SALE-01-02 hesapsız müşteride aksiyon yok + yönlendirici metin) **hiç test edilmemişti**. | 🟢 karara bağlandı |

**§4.32 — Mock görsel uyum turu (05.08.2026): 11 uyuşmazlık, 4'ü paylaşılan katmanda.** 🟢 **KARAR + UYGULAMA.** Tur davranış değiştirmedi; istisnaları aşağıda tek tek işaretli. Tüm mock değerleri bundle'ın `__resourceBlobs`'u çözülerek **gerçek markup'tan** okundu (`Customer Search.dc.html`, `Create customer.dc.html`, `Customer Info v2.dc.html`, `Offer selection.dc.html`).

**(A1+A2) Sayfa başlığı 20px aşağıdaydı — kök neden `router-outlet`'in kendisiydi.** Teşhis edilen "h1'de sıfırlanmamış `margin-block-start`" hipotezi **yanlıştı**: Tailwind preflight zaten tüm margin'leri sıfırlıyor. Gerçek neden yerleşimde: Angular yönlendirilen bileşeni `<router-outlet>`'in **kardeşi** olarak basar, outlet elementi DOM'da **kalır**, ve kabuğun `<main class="flex flex-col gap-5">`'i içinde o boş element **hâlâ bir flex item'dır** — yani sayfanın ilk çocuğundan önce bir `gap` (20px) tüketir. Çözüm `styles.css` base katmanında tek kural: `router-outlet { display: none }`. Outlet kendisi hiçbir şey boyamadığı için kayıpsız, ve **tüm yönlendirilen ekranları aynı anda** düzeltir (ekran-başına yama değil). Ayrıca sale wizard'ın `h1`'ine eksik olan `tracking-heading` (-0.2px) eklendi — diğer iki ekran başlığı zaten taşıyordu.

**(A3) Logo.** Mock'un inline base64 SVG'si `frontend/src/assets/etiya-logo.svg` olarak repoda; header'daki `UI-COMMON-APP-NAME` metni onunla değiştirildi (`h-7` = 28px, `w-auto`). `angular.json`'a **ikinci bir asset kökü** eklendi (`src/assets` → `assets`), çünkü proje yalnız `public/` sunuyordu ve dosya aksi hâlde 404 verirdi. **`alt` boş bırakıldı** (`alt="" aria-hidden="true"`): bağlantı erişilebilir adını zaten `UI-NAV-GO-TO-SEARCH`'ten alıyor ve o, img'in alt'ını **zaten geçersiz kılar**; mock'un `alt="Etiya"`'sı yalnız ikinci ve çevrilmemiş bir duyuru eklerdi. Marka adı bilinçle katalog dışında (görev kuralı). ⚠️ **nginx notu:** `assets/` altındaki dosya adı **content-hash taşımıyor** ama `.svg` kuralı `max-age=31536000, immutable` veriyor — logo değişirse tarayıcılar eskiyi bir yıl tutar. Logo sabit olduğu için kabul edildi; değişirse dosya adı değiştirilmeli.

**(A4) Dil değişiminde kolon kayması — DÜZELTİLMEDİ, mock da böyle.** Teşhis: mock'un **hiçbir** tablosunda `table-layout: fixed` ya da açık kolon `width` yok (bundle genelinde `table-layout` **sıfır** eşleşme); tek genişlik ipuçları esnek kolondaki `width:100%` ve başlıklardaki `white-space:nowrap` — ikisi de **içeriğe bağlı** otomatik yerleşim. Bizim `shared/patterns/Table`'ımız birebir aynı. TR başlıklar EN'den kısa olduğu için kolonların yeniden ölçülmesi **tarayıcının doğru davranışıdır**, hata değil. Sabit genişlik dayatmak mock'tan sapma olurdu ve uzun TR metinleri kırpardı. Kayıt: değişiklik yok.

**(B1) Stepper bağlayıcıları eşit değildi — `shared/patterns/Stepper`, iki sihirbaz.** Kök neden: adım `<li>`'si `flex-1` (= `flex: 1 1 0%`) idi. Sıfır basis **adımları** eşitler; bağlayıcı da adımın içinde kalan alanı aldığı için genişliği "eşit adım − bu etiket" olur, yani **etiket uzunluğuna bağlıdır**. Mock ise adımları ve bağlayıcıları **kardeş** dizer: adım `flex-shrink:0`, bağlayıcı `width:28px; flex:1 1 auto; min-width:24px`. `<ol>`/`<li>` semantiği çıplak `<span>` kardeşine izin vermediği için aynı geometri bir seviye aşağıda kuruldu: `grow` (basis **auto**) her adıma **eşit pay** verir, bağlayıcı da adımın tek büyüyen çocuğu olduğu için payın tamamını yutar. Ayrıca daire↔etiket arası mock'un 8px'ine çekildi (`gap-2`; container `gap-3` kalıyor) ve daire yazı ağırlığı mock gibi **her durumda** 600 yapıldı. **Tüketiciler: Create Customer + satış sihirbazı — ikisi de doğrulandı** (`stepper.spec` +2, `customer-create.spec`/`sale-wizard.spec` yeşil).

**(C1) Hesap modalı dropdown açılınca genişliyordu — §4.30 EK'in `flowPanel` kararı GERİ ALINDI.** §4.30 EK panelin **akış içinde** render edilip dialogu **büyütmesini** kasten seçmişti; bu tur o davranışın kendisi kusur olarak bildirildi ve mock'un markup'ı bildirimi doğruluyor: mock'un iki kompakt dialogu da `max-width:480px` + **panel ve gövdede `overflow:visible`**, panel ise mutlak konumlu — yani liste dialogun dışına taşar ve **dialogun boyutuna dokunmaz**. Uygulanan: `account-form-dialog` artık `overflowVisible` kullanıyor (adres ve demografik dialogların zaten yaptığı gibi) ve üç `flowPanel` kullanımı (Billing address + panelin City/District'i) kaldırıldı. **`shared/ui/Select`'in `flowPanel` girdisi tümüyle silindi** — geriye tüketicisi kalmadı ve tek işlevi artık yanlış sayılan davranışı üretmekti; bırakmak kusuru yeniden üretmenin dokümante yolunu açık tutardı. Panel her zaman overlay; kırpılma riski olan ata **kendisi** `overflowVisible` ile çıkış yapar. Erişilebilirlik korundu (odak tuzağı, Escape, klavye, `aria-activedescendant` — hepsi Modal/Select'te, dokunulmadı). **Etkilenen dialoglar taranıp doğrulandı:** hesap (değişti), adres + demografik (zaten `overflowVisible`, davranış aynı), Customer Search filtreleri ve satış sihirbazı (tam sayfa Select'ler, hiç `flowPanel` kullanmıyorlardı). `select.spec` sadeleşti (tek overlay iddiası, 12/12 seçenek), `account-section.spec` tersine çevrildi (panel `absolute`, dialog gövdesi `overflow-visible`, `max-h-full` YOK, 9/9 adres).

**(C2) Ürünsüz hesabın boş durumu.** `shared/patterns/EmptyState` (ikon+başlık+gövde) mock'un burada çizdiği şey **değil**: genişleyen hesap satırının içinde mock **tek satırlık kesikli çerçeveli bir kutu** çiziyor (`1px dashed border-hover`, `radius-md`, `p-5`, ortalanmış `body`/`text-secondary`). Desen bu şekle bükülmedi — o sayfa seviyesinde, üç yuvalı, farklı bir desen; kutu ekran markup'ı olarak yazıldı ve `product-section` `EmptyState` importunu bıraktı. **Metin mock'un değil, analistin:** mock "No products on this account yet." diyor, katalogdaki `MSG-PROD-NONE` ise "No products are linked to this billing account." — `MSG-*` metni kontrattır (FE-ADR-012 §c/§f) ve bu, `MSG-CUST-NOT-FOUND` için zaten kayıtlı olan mock↔katalog çelişkisinin aynısı. **Analist metni korundu, sapma burada kayıtlı.** Ayrıca Pasif hesapta "Start new sale": mock disabled bir `<button>` değil, **odaklanılamayan `aria-disabled` bir `<span>`** çiziyor (sayfa rengi + varsayılan kenarlık + `cursor:not-allowed`; bizim Button'ımızın disabled paleti sunken dolgulu, farklı). Mock'a uyuldu; **`role="button"` eklendi** (mock'ta yok) — çıplak bir span devre dışı bir kontrol olarak duyurulmaz, disabled `<button>` duyurulur; odaklanabilirlik yine yok. `data-testid` korundu, `customer-detail.spec` etiket yerine **eylemsizliği** iddia edecek şekilde güncellendi.

**(D1) Sekme adları.** Mock: `Catalog` / `Campaign` (ikincisi **tekil** — `tabItems` satırı). `UI-SALE-TAB-OFFERS`/`-CAMPAIGNS` EN+TR düzeltildi (Katalog / Kampanya).

**(D2) Tik hücresi.** Mock gövde hücresinde 18×18 kutuyu **koşulsuz** basıyor, yalnız ikon koşullu. Bizde hücre boşken çöküyor ve ilk seçimde kolonlar sola kayıyordu. Kutu artık her satırda var. ⚠️ Mock'un 18px'i hem kutu hem ikon için EDS ölçeği dışında; **ölçeğe yuvarlandı** (`size-5` = 20px kutu, ikon 16) — `IconSize`'ın zaten uyguladığı 22→20 / 14→16 kuralının aynısı (FE-ADR-011 §f). Görsel etki yok: kutu görünmez bir yer tutucu.

**(D3) Sepetteki satır turuncu kalmıyordu.** Mock'un öncelik sırası **sepette → seçili → zebra**; sepetteki satır kalıcı vurgulu ve `cursor:default`, tıklama hiçbir şey yapmıyor. Mantık zaten doğruydu (`isSelected = !inBasket && …`), eksik olan **boyamaydı**: `bg-selected` yalnız `isSelected`'a bağlıydı, sepete eklenince düşüyordu. Üç durum tek `rowClasses(inBasket, selected, odd)` fonksiyonuna alındı, `transition-colors duration-100` ve mock'un "son satırda kenarlık yok" kuralı (`last:border-b-0`) da oraya girdi. **`bg-selected` token'ı:** mock iki durum için **aynı token adını** (`--eds-color-bg-selected`) ama iki farklı inline fallback'ini (`#FEF6EE` / `#FEF2E6`) yazıyor. Temamızda bu token **bir kez** tanımlı (`--color-selected: #fef6ee`), dolayısıyla **iki durum da onu kullanıyor**; yeni hex uydurulmadı. Fallback ikiliği mock'un kendi tutarsızlığı — FE-ADR-011 §b zaten "token tablosu bağlayıcı, mock'un inline fallback'i değil" diyor.

**(D4) Zebra.** Mock 0-tabanlı **tek** indeksli satırları `bg-page` yapıyor, çiftler saydam; zebra yalnız satır ne seçili ne sepetteyken uygulanır. Her iki sekmeye de eklendi (aynı `rowClasses`).

**(D5) Kampanya "Includes" kolonu.** İki ayrı hata vardı: üye teklifler **tek satırda virgülle** birleştiriliyordu ve ikinci parça teklif adı yerine **ham `serviceType` enum'u** basıyordu. Mock her üye teklifi **kendi satırında** çiziyor: `{offerId} · {offerName}` (body-sm, primary, tabular-nums) + servis tipi etiketi (caption, tertiary, nowrap), sarmalayıcı `flex-col gap-1`. Alanların üçü de `GET /api/campaigns` → `offers[].offerId/.offerName/.serviceType`'tan geliyor; **mock'un fixture id/adları (71233, "ADSL 8Mbps Offer" vb.) kodlanmadı.** Servis tipi artık üç yeni anahtardan çözülüyor (`UI-SALE-SERVICE-TYPE-INTERNET/RESOURCE/ACTIVATION`, EN mock'un kendi tip etiketlerinden tohumlandı: Internet package / Modem / Activation package). **Aynı ham enum Catalog sekmesinin Category kolonunda ve kategori filtresinin seçeneklerinde de ekrana basılıyordu** — aynı kusur, aynı kök, birlikte düzeltildi; filtre seçeneğinin **value**'su ham enum kaldı (karşılaştırma ona bakıyor), yalnız label çözülüyor, yani filtreleme davranışı aynı.

**(D6) Alt bilgi sayacı.** Mock: seçim varsa "{n} selected", **yoksa aktif sekmenin satır sayısı** ("{n} offers" / "{n} campaigns"). Bizde hiçbir şey seçili değilken "0 selected" yazıyordu. İki yeni parametreli anahtar (`UI-SALE-ROW-COUNT-OFFERS/-CAMPAIGNS`). **Tekil/çoğul ayrımı yok** — mock da yapmıyor (`rows.length + ' offers'`, yani "1 offers") ve FE-ADR-012 §h.3 ICU/plural motorunu bilinçle reddediyor; TR'de sayıdan sonra çekim değişmiyor. EN'deki "1 offers" bilinen ve kabul edilen kusur.

**Testler:** 417/417 (+7 net: `sale-wizard.spec` +6 yeni "mock'a uyuyor" bloğu, `stepper.spec` +2, `select.spec` −1 birleşti); lint + `check:conventions` (115 dosya) + prod build yeşil. ⚠️ **Doğrulama notu (§4.29/§4.30 EK dersinin tekrarı):** bunlar ancak `podman compose -p crm-lite up -d --build frontend` sonrası ekranda görünür.

**§4.33 — Satış akışı ikinci görsel uyum turu (05.08.2026): sepet, stepper, onay modalı, submit sonrası.** 🟢 **KARAR + UYGULAMA** (bir madde **KARAR BEKLİYOR**, aşağıda). Kaynak yine bundle'ın çözülmüş `__resourceBlobs`'u — ⚠ göreve konu olan `offer-selection.html` / `product-configuration.html` / `submit-order.html` dosyaları **repoda yok ve hiç olmadı** (§4.30'daki `billing-account-modal.html` durumunun aynısı); markup `Offer selection.dc.html`, `Product Configuration.dc.html`, `Submit Order.dc.html`, `Customer Info v2.dc.html` bloblarından okundu.

**(İŞ 1) Sepet paneli düz listeden GRUPLU yapıya geçti.** Mock'un sepeti iki satır tipi çiziyor: kampanya **tek bir giriş** (adı üstte, altında `{campaignId} · Campaign`, sonra üye teklifler soldan 2px çizgili girintili blokta — her biri `{offerId} · {offerName}` + servis tipi + kendi fiyatı; sağda **paket toplamı**) ve tekil teklif (id üstte, adı altında). Bizde her teklif ayrı satırdı ve kampanya kimliği alt satırda küçük metindi.<br>**Veri modeli DEĞİŞMEDİ, projeksiyon eklendi:** `SaleBasketStore` düz `BasketItem[]` tutmayı sürdürüyor — kablo (`POST /api/orders` düz `items[]` alır) ve AC-SALE-01-08 (servis tipi sayımı sepetin tamamına bakar) bunu gerektiriyor. Görünüm için `groups` computed'ı eklendi (`BasketGroup` = kampanya girişi | teklif girişi). Gruplama **`campaignId`'ye göre ve ilk-görülme sırasıyla**, bitişikliğe göre değil: `addCampaign` üyeleri bugün ardışık ekliyor ama bitişikliğe dayanan bir gruplama ileride sessizce bir kampanyayı ikiye bölerdi.<br>⚠️ **DAVRANIŞ DEĞİŞİKLİĞİ (kaçınılmaz, kayıt altında):** mock'un girintili üye bloğunda **üye başına kaldır düğmesi yok** — kampanya satırının TEK bir X'i var. Dolayısıyla artık bir kampanyanın tek üyesi sepetten çıkarılamıyor; X **paketin tamamını** kaldırıyor (`removeCampaign`). Bu, AC-SALE-01-04'ün "hep ya da hiç" eklemesinin simetriği ve AC-SALE-01-06'ya aykırı değil (o, çıkarılan teklifin sonraki adımlarda görünmemesini şart koşar, üye-bazlı çıkarma zorunluluğu getirmez).<br>**Sayaç ENTRY sayar:** üç teklifli bir kampanya "1 item". `count()` (teklif sayısı) korundu, yanına `entryCount` geldi — kural motoru teklif sayar, panel giriş sayar.<br>**Kaldır düğmesi:** mock burada DS'in `IconButton`'ını değil, elle yazılmış 24px şeffaf bir `<button>` kullanıyor (hover'da sunken). Yine de ekrana özel ham `<button>` yazılmadı — `shared/ui/IconButton`'a **`bare` varyantı** (durağan dolgu yok) + **`size` girdisi** (32 varsayılan / 24) + opsiyonel **`title`** girdisi eklendi; hepsi **eklemeli**, varsayılanlar birebir korundu, dolayısıyla mevcut 6 tüketicinin hiçbiri değişmedi (modal ✕, toast ✕, pagination okları, adres kartları, hesap satırı aksiyonları, ürün satırı aksiyonları — hepsi 32/ghost kaldı). Ham `<button>` alternatifi odak halkasını, disabled davranışını ve `ariaLabel` zorunluluğunu ekranda yeniden yazmak demekti.<br>**Boş durum** da mock'un kendi çizimi oldu (48px sunken **daire** + ikon, sonra medium birincil satır, sonra üçüncül ipucu) — `shared/patterns/EmptyState` deseni burada kullanılmıyor artık; o sayfa seviyesinde, ikon+başlık+gövde yuvalı farklı bir desen (aynı gerekçe §4.32'nin ürün boş durumunda kullanıldı).<br>⚠️ **Kural uyarısının rengi mock'tan alınmadı:** mock `#B45309`/`#92400E` yazıyor, temamızdaki `--color-warning`/`--color-warning-fg` `#ca8a04`/`#854d0e`. FE-ADR-011 §b bağlayıcı: **token tablosu geçerli, mock'un inline fallback'i değil.** Ayrıca bizim uyarı listemiz her kuralı **ayrı satırda** gösteriyor (mock hepsini `join(' ')` ile tek cümleye yapıştırıyor) — §2B'de alınmış, korunan karar.

**(İŞ 2) Satış sihirbazı stepper'ı ayrı bir varyant — ÇİZGİ EŞİTLİĞİ KARARI KULLANICIDA.** Doğrulandı: mock'ta **iki ayrı** step şeridi var ve yedi noktada ayrışıyorlar (numara `01` vs `1`; daire 28px dolgulu vs 24px **çerçeveli**; tamamlanmış = dolu turuncu vs **açık turuncu zemin + turuncu çerçeve + turuncu metin**; gelecek = sunken dolgu vs **beyaz + `border-strong` çerçeve**; bağlayıcı 2px ve tamamlanınca turuncuya döner vs **1px ve hiç renk değiştirmez**; tamamlanmış adımın etiketi birincil vs **üçüncül**; daire yazısı 13px vs 12px). Bu yüzden `Stepper`'a **`variant` girdisi** (`'wizard'` varsayılan | `'sale'`) eklendi — yalnız **boya**; semantik, `aria-current`, pasiflik ve bağlayıcı geometrisi ortak kaldı. Create Customer hiç etkilenmedi (varsayılan varyant, testleriyle doğrulandı).<br>🟡 **AÇIK KARAR — çizgi eşitliği.** Ölçüm: **bizim stepper'ımızda çizgiler ŞU AN EŞİT** (§4.32'de `flex-1` → `grow` düzeltmesiyle: her ara adım `flex-grow:1` + **auto basis** ile boş alandan **eşit pay** alır, payı da adımın tek büyüyen çocuğu olan bağlayıcı yutar). **Mock'ta ise EŞİT DEĞİL** ve bu yapısal: mock'un 2. ve 3. adım öğeleri `flex: 1 1 0` ile **eşit genişlik** alır, bağlayıcı da her öğe içinde etiketten **artan** alanı doldurur; "Product configuration" > "Order summary" olduğu için 2. çizgi 3. çizgiden kısa çıkar. İki seçenek kullanıcıya sunuldu (mock'a birebir uy = eşitsiz / mevcut eşit geometriyi koru = bilinçli sapma); **karar gelene kadar mevcut EŞİT geometri korunuyor** ve bu satır 🟡 kalıyor. Numara biçimi (`1/2/3`) kullanıcı onayıyla zaten uygulandı.

**(İŞ 3) "This product needs no configuration." kaldırıldı.** Mock'un `p.hasFields` dalının **else kolu yok**: karakteristiği olmayan teklifin kartı başlığında biter. Cümle mock'tan değil bizdendi; metin ve `UI-SALE-CONFIG-NO-FIELDS` anahtarı silindi. AC-SALE-01-21 hâlâ karşılanıyor — teklif **kendi kartını** koruyor (test artık kartın varlığını + içinde hiç girdi olmamasını iddia ediyor, eski `sale-config-no-fields-*` testid'i yerine).

**(İŞ 4) Onay modalı — desende düzeltildi, ekrana özel varyant yazılmadı.** Mock'un **iki** onay dialogu karşılaştırıldı ve ortaya çıkan şey beklenenden geniş: **ikisi de yatay** (ikon metnin SOLUNDA, `align-items:flex-start; gap:12px`) ve **ikisinde de buton çubuğunun üstünde ayırıcı çizgi YOK**. Bizim `ConfirmDialog`'umuz ikonu/başlığı/mesajı **dikey** yığıyordu ve butonları `Modal`'ın `appModalFooter` yuvasına veriyordu — o yuva `border-t` çiziyor. Yani bunlar tek bir ekranın kusuru değil, desenin mock'tan sapmasıydı; **desende düzeltildi** ve `tone` yalnız gerçekten tona bağlı iki şeyi taşıyor: **rozet** (danger = 40px açık kırmızı daire / info = **düz ikon, daire yok**) ve **onay butonu** (danger = `danger`, ikonsuz / info = `primary` + `check` ikonu).<br>`Modal` deseni **hiç değişmedi** — ConfirmDialog artık butonlarını gövdenin içinde çiziyor, `appModalFooter` kullanmıyor; böylece form dialoglarının (hesap, adres, demografik, ürün) mock'ta **gerçekten var olan** footer çizgisi olduğu gibi kaldı.<br>**Etkilenen 6 tüketici** (hesap silme, adres silme, müşteri silme, wizard iptali, çıkış onayı, sipariş onayı) tarandı: hepsi dikeyden yataya geçti — mock'un kendi danger dialogu da yatay olduğu için hepsinde **mock'a yaklaşma**; 427 test yeşil.<br>**Gövde metni parametreli oldu:** artık "emin misiniz?" değil, mock'un iki bilgisi — hangi fatura hesabı ve ne kadar (`UI-SALE-CONFIRM-BODY`, hesap yoksa `-NO-ACCOUNT`). Değerler satışın kendi state'inden (bağlam + sepet), mock'un fiyatlarından değil. ⚠ **Analist anahtarı `MSG-SALE-ORDER-CONFIRM` artık render edilmiyor** — sorduğu soruyu **başlık** (`UI-SALE-CONFIRM-TITLE` = mock'un "Submit this order?"ı) zaten soruyor. Anahtar **katalogda bırakıldı** (backend anahtar listesi testi onu bekliyor), yalnız ekrandan çıktı.<br>**Ek temizlik:** para biçimlendirmesi iki ekranda kopyalanmıştı ve onay modalı üçüncüsü olacaktı; `features/customer/sales/money.ts`'e **tek** `formatPrice` olarak çıkarıldı (davranış birebir aynı).

**(İŞ 5) Submit sonrası otomatik yönlendirme — §2B.8'in yerinde-başarı kararı GERİ ALINDI.** §2B.8 yerinde bir başarı bandı tutuyordu, gerekçesi "ayrılmak KR-12 sipariş numarasını kaybettirir" idi. **Bu gerekçe artık geçersiz:** numara **flash mesajıyla** taşınıyor ve hedefteki toast onu duyuruyor. 201 gelir gelmez sihirbaz `/customers/{n}?tab=account&account={accountNumber}`'a gidiyor; "Back to customer" butonu, yerinde başarı bandı ve `UI-SALE-SUCCESS-TITLE` / `-STATUS` / `UI-SALE-BACK-TO-CUSTOMER` anahtarları **silindi**. Bu ekranın tek çıktısı artık **hata** — o da kullanıcıyı sepetiyle birlikte yerinde tutuyor.<br>**Mekanizma — yeni bir şey icat edilmedi, ikiye bölündü:** *nerede olunacağı* **query param** (`tab` + `account`) ile, *ne söyleneceği* **mevcut `CustomerFlashService`** ile taşınıyor. Query param seçildi çünkü konum URL'e ait: yenilemeye dayanır, paylaşılabilir, ve tanınmayan değer sessizce varsayılan sekmeye düşer. Flash servisi **parametre taşıyacak şekilde genişletildi** (`{key, params}`) — anahtar hâlâ **çözülmemiş** gidiyor, çeviriyi okuyan ekran yapıyor (dil değişimi navigasyon ile gösterim arasında olsa bile doğru render eder). `AccountSection`'a opsiyonel **`initiallyExpanded`** girdisi geldi: değer başına **bir kez** ve yalnız o hesap gerçekten listedeyse uygulanıyor — sonrasında açık/kapalı durum **kullanıcınındır**, yeniden render onu geri açmaz.<br>🔴 **Mock'un localStorage enjeksiyonu TAŞINMADI** (FE-ADR-013 §e): mock yeni ürünleri `eds_pending_sale`'e yazıp istemci tarafında hesaba iliştiriyor. Bizde ürünler backend'de gerçekten oluştu; Customer Info hesabı açtığında `GET /api/products?accountNumber=` ile **sunucudan** okuyor (testle pinlendi). Sepet/satış state'i sihirbazla birlikte yok oluyor, hiçbir yere yazılmıyor (AC-SALE-01-16).<br>**Toast** parametreli ve tekil/çoğul iki ayrı anahtar (`UI-SALE-TOAST-ORDER-SUBMITTED-ONE` / `...-SUBMITTED`) — gönderen **anahtarı seçiyor**, metni değil; FE-ADR-012 §h.3'ün reddettiği şey ICU/plural **motoru**, iki anahtar değil.<br>ℹ️ **"Hesabın bulunduğu sayfaya atla" gereği bizde konusuz:** hesap listesi sayfalanmıyor (§4.24/5 — mock'un 4/8/12 seçicisi bilinçle taşınmamıştı), dolayısıyla atlanacak sayfa yok.

**Testler:** 427/427 (+10 net); lint + `check:conventions` (116 dosya) + prod build yeşil. Yeni/değişen kapsam: sepet gruplaması (4), stepper varyantları (3), `?tab=/?account=` inişi + parametreli toast (3), onay gövdesi, submit sonrası yönlendirme + flash içeriği, çift-submit guard'ı (artık butonun yokluğuna değil **kilide** dayanıyor). ⚠️ **Doğrulama notu (§4.29/§4.30/§4.32 dersinin tekrarı):** ekranda görmek için `podman compose -p crm-lite up -d --build frontend`.

**§4.34 — `secondary` butonun kenarlığı hiç çizilmiyordu + sekme kimliği (06.08.2026).** 🟢 **KARAR + UYGULAMA.**

**(1) Kenarlık — bildirilen tek buton değil, TÜM `secondary` butonlar.** Bildirim "Offer selection'daki `Add to basket` butonunda border eksik" idi; kök neden ekranda değil `shared/ui/Button`'da çıktı ve **uygulamanın her yerini** etkiliyordu. Taban sınıf listesi `border border-transparent` içeriyordu, `secondary` varyantı ise `border-line-strong`. **İkisi de düz, tek-sınıflı `border-color` yardımcıları**, yani aynı özgüllükte; kazanan, Tailwind'in üretilmiş CSS'te **hangisini sonra yazdığıdır**. Üretilen `styles-*.css`'te `.border-line-strong` **14459**, `.border-transparent` **14517** ofsetinde — yani transparan **kazanıyordu** ve hiçbir secondary buton kenarlık göstermiyordu. (Diğer secondary butonlarda göze batmamasının nedeni beyaz kart üzerinde durmaları; kart footer'ındaki `Add to basket` farkı görünür kıldı.)<br>**Düzeltme desende:** taban artık yalnız kenarlık **kalınlığını** veriyor, **rengini asla vermiyor**; her varyant kendi rengini bildiriyor (`primary`/`danger`/`ghost` → `border-transparent`, `secondary` → `border-line-strong`). Böylece aynı özgüllükte çakışma bir daha oluşamaz.<br>**Disabled davranışı değişmedi ve mock'la uyumlu:** `disabled:border-transparent`, sınıf + `:disabled` sözde-sınıfından oluşan **daha özgül** bir seçiciye derleniyor (üretilen CSS'te ofset 25501) ve düz sınıfı yener — mock'un kendi DS'i de aynısını yapıyor: `.eds-btn:disabled { … border-color: transparent !important }`. Yani **pasif bir butonun kenarlığı olmaması mock'un kuralıdır**, bizim kusurumuz değildi; düzelen şey **etkin** butonun kenarlığıdır.<br>**Etkilenen ekranlar:** `secondary` kullanan her yer (Search'ün Clear'ı, tüm dialog Cancel'ları, sepet Clear'ı, `Add to basket`, `Start new sale`, retry butonları, ürün/hesap aksiyonları…). Hepsi artık mock'un çizdiği kenarlığı gösteriyor; 428 test yeşil. `button.spec` +1 regresyon testi (token bazlı: `secondary`'de `border-line-strong` var + çıplak `border-transparent` **yok**; diğer üç varyantta tersi; `disabled:border-transparent` her varyantta duruyor).<br>⚠️ **Ders:** Tailwind'de aynı CSS özelliğini hedefleyen iki düz yardımcıyı aynı elemana yazmak, sınıf listesindeki sıraya bakılmaksızın **belirsiz** sonuç verir. `IconButton`'da aynı desen var (`border border-transparent`) ama varyantlarının hiçbiri `border-color` yazmadığı için çakışma yok — bilinçle öyle bırakıldı.

**(2) Sekme kimliği — favicon + başlık.** Keycloak giriş ekranı `infra/keycloak/themes/crm-lite/login/resources/img/favicon.svg`'yi sunuyordu, `:4200`'e geçince tarayıcı sekmesi Angular'ın varsayılan `favicon.ico`'suna ve `Frontend` başlığına düşüyordu — yani **login yönlendirmesi boyunca sekmenin kimliği değişiyordu**. Aynı SVG `frontend/public/favicon.svg`'ye **birebir** kopyalandı (byte-identical), `index.html` ona bağlandı (`type="image/svg+xml"`) ve başlık **`CRM Lite`** oldu. Angular'ın `favicon.ico`'su **silindi** — bırakılsaydı çıplak bir `/favicon.ico` isteği onu sunmaya devam ederdi.<br>**Neden kopya, referans değil:** `infra/` frontend imajına girmiyor (`frontend/Dockerfile` yalnız kendi build çıktısını taşır) ve CLAUDE.md `infra/`'ya dokunmayı yasaklıyor; iki dosya da 515 bayt ve marka logosu kadar durağan. Değişirse ikisi birlikte güncellenmeli — bu satır o bağı kayda geçiriyor.<br>**Başlık neden i18n değil:** `<title>` statik doküman markup'ı, runtime i18n katmanının hiç ulaşmadığı bir yer (FE-ADR-012 §a); ayrıca metin EN ve TR'de aynı (`UI-COMMON-APP-NAME` = "CRM Lite"), dolayısıyla çevrilecek bir şey yok.<br>⚠️ **nginx notu (§4.32'deki logo uyarısının aynısı):** `favicon.svg` içerik-hash'i taşımıyor ama `.svg` kuralı `max-age=31536000, immutable` veriyor — değişirse dosya adı değiştirilmeli.




---

## 5. Backend'e bildirilecek bulgular

| # | Bulgu | Detay | Durum |
|---|---|---|---|
| 5.1 | **`İ` ile başlayan isimler aramada bulunamıyor** | `CustomerSpecifications.wordStart` karşılaştırmanın iki tarafını farklı motorda küçültüyor: kolon tarafı SQL `lower()`, terim tarafı Java `toLowerCase()`. Ölçüm (2026-07-23, canlı stack): Postgres `lower('İ')` → `i` (U+0069); Java `"İ".toLowerCase()` → `i` + **U+0307** (iki kod noktası). Kanıt: `SELECT lower('İbrahim') LIKE 'i̇brahim%'` → **`false`**. **Hiçbir Java `Locale` değeri ikisini birden düzeltmez** (`ROOT` `I`'yı düzeltir `İ`'yi bozar, `tr` tam tersi). Doğru düzeltme: Java'da küçültmeyi bırakıp arama terimini de SQL `lower()`'dan geçirmek | 🟡 **backend'e bildirildi** |
| 5.2 | **Test verisi hatayı yakalayamıyor** | `ind` tablosundaki seed isimleri ASCII'ye sadeleştirilmiş (`Yildiz`, `Sahin`) — Türkçe büyük harf içeren tek kayıt yok, bu yüzden 21 entegrasyon testi 5.1'i kaçırıyor | 🟡 **backend'e bildirildi** |
| 5.3 | **`docs/runbooks/auth-testing.md` satır 106 bayat** | Satır 3.9 hâlâ *"no custom project theme exists"* diyor; tema commit `285815d` ile geldi | 🟡 **bildirildi, düzeltme onayı bekliyor** |
| 5.4 | **PROJECTBRAIN §9.2 madde 2 bayat** | "Keycloak proje teması" kutusu işaretsiz ama tema tamamlanmış ve doğrulanmış (AC-AUTH-01'in tüm UI kriterleri karşılanıyor) | 🟡 **bildirildi, düzeltme onayı bekliyor** |
| 5.5 | **`functional-requirements.md` iç çelişkisi** | §Message keys `MSG-CUST-HAS-PRODUCTS` ve `MSG-ADDR-IN-USE`'u *"as used by the backend"* listesine koyuyor; §Deferred aynı iki kontrolü **no-op** ilan ediyor. Mock'ta "aktif ürünü var, silinemez" ekranı var ama backend bu hatayı bugün döndürmüyor | 🟡 **bildirildi** |
| 5.10 | ~~**Soft-deleted NAT ID sahibi hiçbir okuma endpoint'inden görünmüyor**~~ → **KAPANDI** | **KAYNAKTAN DOĞRULANDI, SONRA ÇÖZÜLDÜ (2026-07-29).** Senaryo: müşteri oluşturuldu → **soft delete** edildi → aynı NAT ID ile yeni kayıt deneniyor. Kullanıcı wizard'ın 3 adımını da geçiyor, hata ancak **Create'e basınca** 409 `MSG-CUST-DUP-NATID` olarak geliyor. **Neden frontend erken yakalayamıyor:** (1) tekillik kontrolü `IndividualRepository.existsByNationalityId()` — soft-deleted dahil, doğru (ADR-003) — ama **yalnız `CustomerBusinessRules.checkNationalityIdIsUniqueForCreate()` içinde**, POST sırasında çağrılıyor; **hiçbir endpoint bu bilgiyi dışarı açmıyor**; (2) tek kanonik okuma yolu `GET /api/customers`, `CustomerSpecifications.search()` her sorguya `statusId = ACTV AND deletedDate IS NULL` ekliyor (satır 78-79) → soft-deleted sahip **yapısal olarak görünmez**. Yani bugünkü kontratla ilk denemede adım 1'de bunu bildirmek **imkânsız**; istemci tarafı tahmin **yazılmadı** (uydurma kontrol, FE-ADR-013 §a). 🟢 **ÇÖZÜLDÜ (2026-07-29, ekip onayıyla backend'e dokunuldu).** `GET /api/customers/nationality-id-availability?nationalityId={id}` → `200 {"available": bool}` eklendi (**ADR-005 §Addendum**). Uç, tekillik kuralını **create yolunun kullandığı metodun ta kendisiyle** yanıtlıyor (`CustomerBusinessRules.isNationalityIdAvailableForCreate`) — yani soft-deleted sahipler dahil; probe ile otorite ayrışamaz. Yanıt **tek alan** taşıyor: kural silinmiş kişileri de kapsadığı için müşteri no/ad döndürmek bunu "silinmiş kişi madenciliği" ucuna çevirirdi. **Advisory kalıyor** — `true` bir rezervasyon değil; POST'un 409'u ve DB UNIQUE kısıtı otorite olarak duruyor, wizard 409'u işlemeye devam ediyor. Frontend'de yalnız `CustomerApiService.nationalityIdIsTaken()` bu ucu çağırıyor; **oturum-içi `rejectedNatIds` geçici çözümü KALDIRILDI** (gerçek uç geldi, gereksiz kaldı). Yan düzeltme: eksik zorunlu `@RequestParam` artık 500 değil **400** (`MissingServletRequestParameterException` handler'ı — tip-uyuşmazlığı handler'ının emsaliyle) | 🟢 **karara bağlandı ve uygulandı** |
| 5.11 | **Login sonrası ham API URL'ine düşme (`?continue`)** | **CANLIDA GÖZLENDİ (2026-07-30, kullanıcı bildirimi).** Senaryo: `:4200`'de oturum açıkken yan sekmede `http://localhost:8080/api/products/2` açılıyor (200 JSON — doğru) → çıkış yapılıyor → o sekmeye oturum bittikten sonra bir kez daha istek gidiyor (F5/geri-ileri) → `:4200`'den tekrar giriş yapılınca kullanıcı **`http://localhost:8080/api/products/2?continue`** adresine düşüyor, `/customers` ekranına değil. **Kök neden (kaynaktan doğrulandı):** `ExceptionTranslationFilter`, entry point'i çağırmadan **önce** `requestCache.saveRequest()` yapıyor — `/api/**` için `ApiAuthenticationEntryPoint` 401 JSON dönse bile URL oturuma kaydediliyor. nginx `/api`, `/oauth2`, `/login`, `/logout`'u gateway'e proxy'lediği için (FE-ADR-004/010) `:4200` ile `:8080` **aynı JSESSIONID oturumunu** paylaşıyor, dolayısıyla yan sekmenin kaydı SPA'nın login'ini kaçırıyor. `?continue`, Spring Security 6.1+ `HttpSessionRequestCache.matchingRequestParameterName` varsayılanının parmak izi. `SecurityConfig`'te ne `successHandler` ne `requestCache` yapılandırılmıştı → tamamen Spring varsayılanı (`SavedRequestAwareAuthenticationSuccessHandler`). **Ürün servisiyle ilgisi yok:** `/api/customers/1001` veya `/api/accounts?customerId=1001` ile birebir aynısı olur; hatta SPA'nın kendi anonim `/api/session/me` XHR'ı da kaydedildiği için saf SPA akışı bile oturum JSON'una düşebiliyordu. **İLK DENEME EKSİK KALDI (aynı gün, kayda geçiyor):** yaklaşım A+B uygulandı — (A) `HttpSessionRequestCache`'e negatif matcher (`/api/**` + `/actuator/**` dışlandı), (B) `successHandler` ile istekten türetilen origin kökü. `/api/products/2` gerçekten kurtuldu, **ama kullanıcı aynı senaryoyu tekrarlayınca varış `http://localhost:8080/favicon.ico?continue` → 500 oldu.** Sebep: tarayıcı gösterdiği sekme için **alt-kaynak** istekleri de atıyor (favicon `Sec-Fetch-Dest: image`); `/favicon.ico` `anyRequest().authenticated()` altında korumalı olduğu için dışlama listesine girmediğinden kaydedildi. **Ders:** kara liste bu sınıfı kapatamaz (alt-kaynaklar açık uçlu), "yalnız üst-seviye navigasyon" filtresi de tek başına yetmez — bir insan adres çubuğuna API URL'i yazabiliyor, o da navigasyondur. 🟢 **NİHAİ ÇÖZÜM (2026-07-30, onaylı yaklaşım C+B):** **(C)** request cache tamamen kapatıldı (`NullRequestCache`) — bu gateway'in **hiç gezilebilir HTML sayfası yok**: `/api/**` ve `/actuator/**` JSON, `/` JSON session probe (ve zaten varsayılan hedef), Swagger `permitAll` (kaydedilmez), kalan her yol eşleşmeyen; SPA deep-link'leri gateway'e hiç uğramıyor (nginx `index.html`'i statik veriyor, URL'i Angular router yönetiyor). Yani cache'in **meşru girdisi yoktu**, kaldırılan bir yetenek değil bir yanlış-pozitif kaynağı. **REVİZE EDİLECEK koşullar (topolojiye bağlı karar):** (a) gateway gezilebilir HTML sunmaya başlarsa (SSR ya da SPA'yı nginx yerine gateway host ederse), (b) Swagger UI auth arkasına alınırsa. **(B) aynen korundu.** Regresyon testi: `GatewayBffIntegrationTest.postLoginLandingIsAlwaysTheApplicationRoot` (ham API sekmesi + anonim probe + favicon + actuator + sayfa navigasyonu hepsi tohumlanıp varışın daima uygulama kökü olduğu iddia ediliyor). Canlıda iki origin'de doğrulandı: `:8080` → `:8080/`, `:4200` → `:4200/` → `/customers`. **Not:** `:4200/favicon.ico` **200** döner (nginx statik) — bu yüzden son kullanıcılar bu hatadan hiç etkilenmedi, yalnız doğrudan `:8080` sekmesi açan geliştirici etkileniyordu | 🟢 **karara bağlandı ve uygulandı** |
| 5.12 | **Eşleşmeyen yolda 500 (`MSG-INTERNAL-ERROR`) — hata kontratı kusuru** | **5.11'i kovalarken ortaya çıktı, ondan bağımsız.** `GatewayExceptionHandler`'da yalnız üç handler vardı (`HttpStatusCodeException`, `ResourceAccessException`, `Exception` catch-all), gateway'de `/favicon.ico` için de hiçbir route/resource handler yok → oturumu **açık** bir kullanıcının `:8080/favicon.ico` isteği bile `NoResourceFoundException` üzerinden catch-all'a düşüp **500 + stack trace** üretiyordu (loglarda gerçek arıza gibi görünen gürültü). 🟢 **ÇÖZÜLDÜ (2026-07-30):** `NoResourceFoundException` + `NoHandlerFoundException` → **404 `MSG-NOT-FOUND`** (belgeli proje eklentisi; `docs/api/authentication.md` hata tablosunda kayıtlı). Regresyon testi: `GatewayBffIntegrationTest.unmappedPathIsNotFoundNotServerError`. Canlı doğrulama: oturum açıkken `:8080/favicon.ico` → `404 MSG-NOT-FOUND` | 🟢 **karara bağlandı ve uygulandı** |
| 5.9 | **AC-ADDR-04-04 / `MSG-ADDR-IN-USE`: backend no-op, frontend hazır** | **DOĞRULANDI (2026-07-29, C2).** Beklenen davranış: aktif bir fatura hesabına bağlı adres silinemesin, `MSG-ADDR-IN-USE` gösterilsin. **Backend bugün bu kontrolü YAPMIYOR** — iki bağımsız kaynak: `docs/api/customer-service.md` §Known limitations *"Address in-use check (`MSG-ADDR-IN-USE`) → **no-op** until account/service addresses exist"*, ve `docs/api/account-service.md` §Deliberate limitations *"…the address `MSG-ADDR-IN-USE` in-use check remain customer-service TODOs — converting them to real calls against this service is a **separate follow-up PR**"*. Aynı doküman §K notu da hatayı *"once account/service addresses exist"* koşuluna bağlıyor. Yani `DELETE .../addresses/{id}` bağlı bir adres için bugün **204** döndürüyor ve adres siliniyor. **Bu bir frontend bug'ı DEĞİL, backend bağımlılığıdır.** İstemci tarafında "bağlı mı" tahmini **yazılmadı** (FE-ADR-013 §a / §4.24-3 ilkesi: istemci backend kontrolünü taklit etmez — hesap listesini tarayıp adres id'si eşleştirmek, 223 K-8 hesabını ve başka servislerin adres kullanımını göremediği için yanlış "silemezsiniz" üretirdi). **Frontend hata-gösterme yolu HAZIR ve testli:** `confirmDeleteAddress()` gelen `messageKey`'i ayırt etmeden ConfirmDialog'un hata durumunda gösteriyor, adres listeden kaldırılmıyor, liste yenilenmiyor (`customer-detail.spec.ts` bunu 409 `MSG-ADDR-IN-USE` ile iddia ediyor) ve `MSG-ADDR-IN-USE` EN/TR olarak katalogda. **Backend follow-up PR'ı geldiği an davranış kendiliğinden aktifleşir — frontend'de sıfır satır değişiklik gerekir** | 🟡 **backend'e bildirildi — follow-up PR bekleniyor** |

---

| 5.13 | **Karakteristik kataloğunda alan-başına FORMAT tanımı yok** | §4.31 doğrulaması sırasında çıktı. `PROD_SPEC_CHAR` yalnız **`data_type`** taşıyor (NUMBER/BOOLEAN/TEXT/DATE); alan-başına bir **desen/aralık** kolonu (regex, min/max, birim) **yok**. Sonucu seed'de görünür: **`MAC Address`** `TEXT` olarak tanımlı, dolayısıyla `CharacteristicValidationRules` onu **her zaman geçerli** sayıyor — `"???"` de kabul ediliyor. Aynısı `Download Speed (Mbps)` için de geçerli: NUMBER ayrıştırması geçiyor ama **`-5` veya `999999`** de geçiyor. **Frontend bunu tek taraflı kapatmadı ve kapatmamalı:** alan **adına** bakıp istemcide MAC regex'i uydurmak, backend'in kabul ettiği değeri istemcide reddetmek olurdu (FE-ADR-013 §a / §4.24-3: istemci backend kontrolünü ne taklit eder ne de sıkılaştırır) ve `prod_spec_char` satırı yarın değiştiğinde sessizce yanlışa düşerdi. **Backend'den istenen:** `PROD_SPEC_CHAR`'a opsiyonel bir desen/aralık kolonu (+ `CharacteristicResponse`'a alanı) — geldiği an `MSG-VAL-CHAR-FORMAT` her iki tarafta da **aynı** kuralla çalışır ve frontend'de `characteristic-validation.ts`'e tek bir dal eklemek yeter | 🟡 **backend'e bildirilecek** |
| 5.6 | **Swagger UI / api-docs anonim değil** | PROJECTBRAIN §4.7 *"Docs sayfası login'siz görülebilir"* diyor, ama çalışan stack'te `GET /swagger-ui.html` ve `GET /v3/api-docs/customer-service` **401 `MSG-AUTH-UNAUTHORIZED`** dönüyor (2026-07-23 ölçümü). Ya çalışan imajlar ADR-012 commit'inden eski, ya da `crm.security.permit-paths` beklendiği gibi uygulanmıyor. Frontend'i bloke etmiyor ama `Page` zarfının şemasını Swagger'dan okumayı engelledi (§4.4) | 🟡 **backend'e bildirildi** |
| 5.8 | **PROJECTBRAIN §10 "Compose 8 servis" notu bayat** | `infra/docker-compose.yml`'e `frontend` servisi eklendi (FE-ADR-010, 2026-07-24; `git diff` = 35 ekleme / 0 silme, mevcut hiçbir servise dokunulmadı). Servis sayısı artık **9**. PROJECTBRAIN backend dokümanı olduğu için frontend tarafından düzenlenmedi | 🟡 **backend'e bildirildi** |
| 5.7 | **Logout CSRF, tam-sayfa gezinme POST'u ile taşınamıyor** | FE-ADR-005 §3 "tam sayfa gezinme + `X-XSRF-TOKEN` header" öngörüyordu; ancak tarayıcıda gezinen bir POST yalnız `<form>` ile yapılır ve form, header değil `_csrf` **parametresi** taşır. Gateway'in `SpaCsrfTokenRequestHandler`'ı (kaynaktan doğrulandı 2026-07-24) parametre yolunu **XOR-decode** ediyor (BREACH koruması), header yolunu ise ham token ile karşılaştırıyor — `XSRF-TOKEN` çerezi **ham** token tuttuğundan yalnız **header (XHR)** yolu geçerli. **Karar:** logout `HttpClient` POST (header → CSRF geçerli) + ardından `/`'a tam-sayfa gezinme (`core/auth/auth.service.ts`). Keycloak `end_session` 302'sini XHR sürükleyemiyor, fakat SSO oturumu `id_token_hint` ile **sunucu tarafında** yine de sonlanıyor. **Kusursuz tarayıcı-yönlü RP-logout** (Keycloak tarayıcı çerezlerinin de temizlenmesi) isteniyorsa backend bir olanak eklemeli: (a) logout `end_session` URL'ini JSON'da döndürsün, Angular `window.location.assign` etsin; ya da (b) gezinilebilir bir GET logout. **(c)** `crm.security.post-logout-redirect-uri` hiçbir yml'de tanımlı değil → yalnız Java varsayılanı `http://localhost:8080/` geçerli; `:4200` olmalı (ya da `X-Forwarded-*`'tan türetilmeli).<br>**🔴 CANLIDA DOĞRULANDI (2026-07-24)** — iki somut belirti: (1) XHR, gateway'in cross-origin 302'sini takip ederken **askıda kalabiliyor**; sign-out navigasyonu yanıta zincirlendiği için buton *hiçbir şey yapmıyordu*. (2) SSO ölmediğinden `/`'a inince guard → Keycloak → **sessiz re-login** → `/customers`; kullanıcı çıkış yapamamış sanıyor. **Frontend azaltmaları (uygulandı):** `logout()` artık `timeout(3000)` ile sınırlı — yanıt gelmese de çıkış tamamlanır (regresyon testi var); ve varış noktası guard'ın dışındaki `/signed-out` sayfası (form değil, yalnız tam-sayfa "Giriş yap" bağlantısı). Bunlar belirtiyi görünür/deterministik yapar ama **SSO'yu öldürmez** — onun için (a) şart. **✅ ÇÖZÜLDÜ (2026-07-27), seçenek (a):** `KeycloakLogoutSuccessHandler` artık `302` yerine `200` + `{"logoutUrl": "..."}` JSON dönüyor; `AuthService.logout()` başarı yanıtında `window.location.assign(logoutUrl)` ile gerçek bir üst-seviye gezinme yapıyor, böylece tarayıcı Keycloak'ı fiilen ziyaret edip SSO çerezini temizliyor. **(c)** de bu vesileyle çözüldü: `post_logout_redirect_uri` artık statik (ve hep bayat kalan) `crm.security.post-logout-redirect-uri` yapılandırmasından değil, `{baseUrl}`'ın OAuth2 `redirect-uri`'de yaptığı gibi `ServletUriComponentsBuilder` ile istekten (forward-header-düzeltmeli) türetiliyor — config anahtarı tamamen kaldırıldı. **↻ REVİZE (2026-07-29):** akış sadeleştirildi — çıkış artık sidenav butonuyla değil, paylaşılan `ConfirmDialog` üzerinden onaylanınca başlıyor (`MSG-AUTH-LOGOUT-CONFIRM`, Evet/Hayır); onay zaten alındığı için araya "çıkış yapıldı" ekranı koymanın anlamı kalmadı ve `/signed-out` sayfası **tamamen kaldırıldı**. `post_logout_redirect_uri` artık gateway'in kendi `/oauth2/authorization/keycloak` ucu → Keycloak SSO çerezini temizleyip oraya döndürüyor, o uç taze bir authorization request başlatıyor ve SSO öldüğü için kullanıcı **doğrudan giriş formunda** kalıyor (döngü yok: o isteği sessizce tamamlayabilecek oturum, az önce yok edilen oturumun ta kendisi). Gezinmeler `assign` değil `replace` — aksi hâlde müşteri verisi taşıyan ekran geçmişte kalıp bfcache'ten geri geliyordu. Bkz. FE-ADR-005 §3, `docs/api/authentication.md` §Logout. | 🟢 **karara bağlandı** |

---

## 6. Bakım kuralı

Yeni bir kapsam dışı bırakma, mock/backend farkı veya karar ortaya çıktığında
**önce buraya bir satır eklenir**, sonra kod yazılır. Durumu değişen satırın
etiketi güncellenir; satır silinmez — kapanmış kararların izi kalır.
Bir satır 🟢 olduğunda ilgili ADR'ye veya
`docs/frontend/mock-ui-analysis.md`'ye de işlenmiş olmalıdır.
