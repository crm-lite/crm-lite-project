spring:
  application:
    name: account-service
  config:
    # "optional:" so this service still starts on local defaults if config-server is unreachable.
    import: "optional:configserver:http://localhost:8888"
